# 던전 수사 조서 — 심문 엔진 일반화 기획서 v2.0

## 사건 독립 심문 엔진 아키텍처 및 구현 명세

| 항목 | 내용 |
|---|---|
| **문서 버전** | v2.0 |
| **기준일** | 2026-07-26 |
| **선행 문서** | 던전 수사 조서 통합 게임 기획서 v1.0 (증거대조형 심문전투, 2026-07-26) |
| **문서 성격** | v1.0에서 확정된 심문 전투 규칙을 **콘텐츠 독립적인 공통 엔진**으로 재정의하는 구현 기준 명세 |
| **대상 독자** | 클라이언트·서버 구현 담당, 시스템 기획, 콘텐츠 기획, QA |
| **핵심 변경** | 사건·용의자·증거를 코드에서 분리하고, 카드를 공통 Intent의 변형으로, 적 기믹을 Trigger·Effect 조합으로, 승리 조건을 Objective 데이터로 일반화 |
| **완료 판정** | 두 번째 사건을 **엔진 코드 수정 없이** 데이터 패키지만으로 플레이 가능해야 함 |

---

## 0. 이 문서를 읽는 법

### 0.1 v1.0과 v2.0의 관계

v1.0은 **무엇이 재미있는 게임인가**를 정의했다. 진술 토큰, 저항도, 증거 강도, 질문·확인·모순 카드의 역할 분리, 직접 모순과 간접 의심의 구분은 모두 v1.0에서 확정된 재미의 정의이며 v2.0에서 변경하지 않는다.

v2.0은 **그 규칙을 어떤 단위로 구현할 것인가**를 정의한다. v1.0의 서술은 드워프 감리사, 국밥집, 포장마차 결제 내역, 유도 신문 카드 같은 구체 콘텐츠에 묶여 있다. 이 상태로 구현하면 두 번째 사건을 만들 때 엔진 코드를 다시 써야 한다. v2.0은 같은 규칙을 사건 이름이 등장하지 않는 형태로 다시 쓴 문서다.

따라서 두 문서는 대체 관계가 아니라 다음 관계다.

```text
v1.0  게임 규칙의 정의        무엇이 재미있는가
v2.0  게임 규칙의 구현 단위    그 재미를 어떻게 데이터화하는가
```

세계관, 서사, 캐릭터, 에피소드, 아트 디렉션, 사운드, 로그라이크 맵 구조는 v1.0을 그대로 따른다. v2.0은 이를 재서술하지 않는다.

### 0.2 3부 구성

| 부 | 질문 | 내용 | 주 독자 |
|---|---|---|---|
| **제1부 일반화** | 이 게임은 무엇의 일반형인가 | 게임의 형식적 정의, 5계층 아키텍처, 공통 객체, Claim 3축 상태 모델, 일반화 완료 기준 | 전원 |
| **제2부 세분화** | 각 모듈을 어떻게 구현하는가 | 전체 JSON 스키마, 필드 정의표, 판정 축과 조합표, 의사코드, Trigger·Effect 카탈로그, AI 계약 | 구현 담당 |
| **제3부 검증** | 일반화가 실제로 되었는가 | 드워프 사건의 데이터 패키지화, 구조가 다른 두 번째 사건, 콘텐츠 제작 절차, 자동 검증 규칙, 수용 테스트 | 콘텐츠 기획, QA |

제1부만 읽으면 구조를 이해할 수 있고, 제2부까지 읽으면 구현할 수 있으며, 제3부까지 읽으면 새 사건을 만들 수 있다.

### 0.3 확정도 표기

v1.0의 표기 체계를 그대로 계승한다.

| 표기 | 의미 |
|---|---|
| **[확정]** | 구현 기준으로 사용. 변경하려면 이 문서를 개정해야 함 |
| **[밸런스 초안]** | 구조는 확정, 수치는 플레이테스트 후 조정 |
| **[MVP 축소]** | 정식 버전에서는 확장하되 해커톤에서는 단순 구현 |
| **[검증안]** | 채택 여부 미정. 구현 전 별도 결정 필요 |
| **[후속 확장]** | 해커톤 수직 슬라이스 이후 |

### 0.4 표기 규약

- 코드 식별자, 열거형 값, 필드명은 `고정폭`으로 표기한다.
- 열거형 값은 대문자 스네이크 케이스를 사용한다. 예: `DIRECT_CONTRADICTION`
- 데이터 ID는 소문자 스네이크 케이스를 사용한다. 예: `claim_ep004_location_01`
- JSON 예시의 주석은 실제 데이터에 포함하지 않는다. 설명용으로만 병기한다.
- 본문에서 특정 사건명·용의자명이 등장하는 경우는 **제3부 검증 예시에 한한다.** 제1부와 제2부에 고유 콘텐츠명이 등장하면 그 자체가 일반화 실패다.

---

## 목차

**제1부 — 일반화**

1. 이 문서가 해결하는 문제
2. 게임의 일반 정의
3. 5계층 아키텍처
4. 공통 객체 카탈로그
5. Claim 모델
6. Claim 상태 3축 모델
7. 일반화 완료 판정 기준

**제2부 — 세분화**

8. 모듈 구성과 책임
9. CaseDefinition 데이터 명세
10. InquiryGraph 명세
11. Evidence 명세
12. ProofRule 명세
13. Action 시스템 명세
14. Argument Resolver 명세
15. EncounterModifier 명세
16. EncounterFlow 명세
17. Objective·Outcome 명세
18. AI 표현 계층 계약
19. Presentation DTO 경계
20. 런타임 처리 흐름

**제3부 — 검증**

21. 검증 사건 A — 알리바이·소유 구조
22. 검증 사건 B — 순서·지시 구조
23. 콘텐츠 제작 파이프라인
24. 자동 검증 규칙
25. 구현 순서와 수용 테스트
26. 위험 요소와 대응
27. 남은 의사결정 항목

**부록**

- 부록 A. v2.0 핵심 원칙 요약
- 부록 B. 열거형 총람
- 부록 C. ID 명명 규칙
- 부록 D. v1.0 → v2.0 용어 대응표
- 부록 E. CaseDefinition 전체 스키마
- 부록 F. 판정 조합 전체표
# 제1부 — 일반화

> 이 게임은 무엇의 일반형인가

---

## 1. 이 문서가 해결하는 문제

### 1.1 v1.0의 상태 진단

v1.0은 심문 전투의 **규칙**을 충분히 정의했다. 다음 항목은 이미 확정되어 있다.

- 진술을 4~8개의 검증 가능한 단위로 분해한다.
- 방어막은 거짓말 표시가 아니라 저항도다.
- 증거는 결론이 아니라 입증 범위를 가진 기록이다.
- 카드는 질문·정리·확인·모순·압박으로 목적이 분리된다.
- 판정은 직접 모순·간접 의심·합리적 불충분·진실 공격·무관 증거로 나뉜다.
- 사건 진실은 엔진이 보유하고 AI는 표현만 담당한다.

문제는 규칙이 아니라 **구현 단위**다. v1.0의 서술은 다음처럼 특정 콘텐츠에 결합되어 있다.

| v1.0 서술 | 결합된 콘텐츠 |
|---|---|
| “드워프 감리사는 부실 자재 반입 시간에 현장에 없었다고 주장한다” | 특정 용의자, 특정 사건 |
| “E1 포장마차 카드 결제 내역” | 특정 증거 인스턴스 |
| “C3 장소: 국밥집에 있었다” | 특정 Claim 인스턴스 |
| “유도 신문 2장, 재질문 2장, 확인 질문 2장, 모순 지적 2장” | 특정 카드 이름 |
| “취중 진술 교란” | 특정 적 기믹 |
| “필수 주장 해결 + 평정심 1~30%” | 특정 승리 조건 수치 |

이 상태로 코드를 작성하면 다음과 같은 구조가 만들어진다.

```text
DwarfEncounter.cs
  - if (targetToken == "C3" && evidence.Contains("E2") && evidence.Contains("E3"))
        → DirectContradiction()

유도신문Card.cs
재질문Card.cs
확인질문Card.cs
모순지적Card.cs
동기추궁Card.cs
과거행적들추기Card.cs
...
```

이 구조의 비용은 두 번째 사건에서 발생한다. 새 사건은 새 판정 분기를 요구하고, 새 카드는 새 클래스를 요구하며, 새 적은 새 기믹 코드를 요구한다. 콘텐츠 수량이 늘어날 때 코드량이 비례해서 늘어난다.

### 1.2 v2.0의 목표 문장

> **하나의 심문 게임 엔진 + 교체 가능한 사건 데이터 패키지**

드워프 사건을 코드로 구현하는 것이 아니라, 다음 공식을 처리하는 공통 엔진을 만든다.

```text
사건 모델
+ 공개된 주장
+ 플레이어 행동
+ 제출 근거
+ 판정 규칙
= 지식 상태와 전투 상태의 변화
```

### 1.3 잘못된 구현과 올바른 구현

이 문서 전체를 관통하는 판단 기준은 하나다. **엔진 코드에 고유명사가 등장하면 실패다.**

**잘못된 예**

```text
if (suspect.id == "dwarf_inspector") {
    shuffleStatements();
}

if (evidence.id == "evidence_stall_cctv") {
    return DIRECT_CONTRADICTION;
}

if (card.name == "유도 신문") {
    revealMotiveSlot();
}
```

**올바른 예**

```text
if (encounter.hasModifier("statement_distortion")) {
    modifierSystem.execute(EffectType.DISTORT_CLAIM_VIEW);
}

resolution = argumentResolver.resolve(submission, knowledgeState);

if (card.intent == ActionIntent.QUERY) {
    inquiryGraph.openRoute(submission.target);
}
```

차이는 문법이 아니라 **판단의 위치**다. 잘못된 예는 코드가 콘텐츠를 알고 있고, 올바른 예는 코드가 규칙만 알고 있다.

### 1.4 v1.0 → v2.0 개념 대응

v2.0은 v1.0의 개념을 버리지 않는다. 같은 개념을 콘텐츠 독립적인 이름과 구조로 옮긴다.

| v1.0 개념 | v2.0 대응 | 변경 성격 |
|---|---|---|
| 진술문 | `Statement` | 명칭 정리 |
| 진술 토큰 | `Claim` + `ClaimToken` | **분리.** 의미 단위와 화면 표시 단위를 나눔 |
| 복합 토큰 | `Claim.is_compound` + `PresentationState.COMPOUND` | 상태로 이동 |
| 미진술 슬롯 | `QuestionRoute` + `InquiryGraph` | **구조화.** 빈칸이 아니라 질문 경로 그래프 |
| 저항도 방어막 | `Claim.resistance` | 유지 |
| 증거 | `Evidence` + `Observation[]` | **구조화.** 관찰값 단위로 분해 |
| 증거 세트 | `ProofRule.guaranteed_evidence_sets` | 유지, 의미 조건 추가 |
| 논리 주장 | `ActionSubmission` | 일반화 |
| Truth Graph | `TruthGraph` | 유지 |
| Proof Rule | `ProofRule` | **확장.** 증거 ID 열거 + 의미 기반 조건 |
| 확인 잠금 | `CommitmentState.COMMITTED` **와** `EpistemicState.SUPPORTED` | **분리.** 6.1 참조 |
| 의심 마커 | `EpistemicState.SUSPECTED` | 상태로 통합 |
| 모순 확정 | `EpistemicState.REFUTED` | 상태로 통합 |
| 교란 | `PresentationState.DISTORTED` | 표시 축으로 분리 |
| 심문 카드 | `ActionIntent` + `CardDefinition` | **일반화.** 카드는 Intent의 변형 |
| 결재 도장·포스트잇·병합 | `CardModifier` | 성장 시스템도 Modifier로 통합 |
| 적 기믹 | `EncounterModifier` (Trigger·Condition·Effect) | **일반화** |
| 필수 주장 목표 | `Objective` | 데이터화 |
| 완전 진술 / 강제 자백 / 실패 | `OutcomeRule` | 데이터화 |
| 사건 진행 | `EncounterFlow` 노드 그래프 | 데이터화 |
| LLM 표현 계층 | `Dialogue Renderer` + AI 계약 | 계약 명세화 |

### 1.5 이 문서가 바꾸지 않는 것

다음은 v1.0의 결정을 그대로 유지한다. v2.0은 이 항목들의 **표현 방식만** 바꾼다.

- 스트레스·강압 수사·평정심·CP의 4대 자원 구조
- 평정심 스위트 스팟 개념
- 증거 4등급 체계
- 판정 결과의 의미론(직접 모순은 대안 가설이 제거되어야 성립한다)
- 정답 비노출 원칙
- AI는 심판이 아니라 배우라는 원칙
- 적 기믹은 추리를 삭제하지 않고 비용을 바꾼다는 원칙

---

## 2. 게임의 일반 정의

### 2.1 지식 상태 변환 게임

이 게임은 구현 관점에서 다음과 같이 정의된다.

> **숨겨진 사건 모델을 기반으로, 플레이어가 제한된 행동과 근거를 사용해 공개 지식 상태를 변화시키는 결정론적 심문 엔진이다.**

핵심은 “용의자의 체력을 깎는다”가 아니라 “정보의 상태를 바꾼다”이다. 플레이어가 하는 모든 유효 행동은 다음 상태 축 위에서 정보를 이동시킨다.

```text
숨겨짐 → 진술됨 → 의심됨 → 고정됨 → 입증됨 / 반박됨
```

이 축이 게임의 진행이며, 평정심 감소는 그 결과로 따라오는 부산물이다. 이 순서가 뒤집히면 — 즉 평정심 감소가 목적이 되면 — 게임은 압박 카드만 반복하는 구조로 붕괴한다. v1.0이 “압박 카드는 필수 주장을 해결하지 못한다”고 규정한 이유가 여기에 있다.

### 2.2 사건의 일반화 공식

사건은 다음 다섯 요소의 합이다.

```text
사건
= 실제 사건 구조
+ 공개 가능한 주장
+ 질문으로 열리는 정보
+ 획득 가능한 증거
+ 주장별 입증 조건
+ 심문 진행 규칙
+ 결과 분기
```

데이터 구조에서는 다음처럼 나눈다.

```text
CaseDefinition
├─ TruthGraph          실제로 무슨 일이 있었는가
├─ InquiryGraph        무엇을 질문하면 무엇이 드러나는가
├─ ProofGraph          어떤 근거가 어떤 주장을 입증·반박하는가
├─ EncounterFlow       심문이 어떤 단계로 진행되는가
└─ OutcomeRules        어떤 조건에서 어떤 결과가 발생하는가
```

이 다섯 그래프가 하나의 사건 패키지를 구성한다. 새 사건을 추가한다는 것은 이 다섯 그래프를 작성한다는 뜻이며, 그 외의 어떤 작업도 요구해서는 안 된다.

#### 2.2.1 다섯 그래프의 관계

```text
        TruthGraph  (진실. 플레이어에게 직접 노출되지 않음)
             │
             │ 어떤 Claim이 진실과 일치하는가
             ▼
   ┌──── ProofGraph ────┐        어떤 근거가 어떤 Claim을
   │   Claim ←→ Evidence │        지지·반박하는가
   └─────────┬───────────┘
             │
             │ Claim 상태 변화
             ▼
        InquiryGraph                무엇을 물으면 무엇이 열리는가
             │
             ▼
       EncounterFlow                어떤 조건에서 다음 페이즈로 가는가
             │
             ▼
        OutcomeRules                어떤 상태가 어떤 결말인가
```

### 2.3 플레이어 행동의 일반화 공식

```text
플레이어 행동
= 행동 목적
+ 대상
+ 제출 근거
+ 카드·파트너·유물 보정
```

구현 객체로는 다음과 같다.

```text
ActionSubmission
├─ intent              질문 / 명확화 / 확인 / 반박 / 압박 / 정비
├─ target              주장 / 미공개 슬롯 / 증거 / 이전 진술
├─ supports            증거, 이전 진술, 다른 용의자 진술
└─ modifiers           카드 강화, 파트너, 유물, 상태 효과
```

v1.0의 “심문 카드 + 대상 진술 토큰 + 증거 0~2장”은 이 구조의 특수한 경우다. 카드는 `intent`와 `modifiers`를 함께 제공하는 컨테이너일 뿐이며, 기본 행동(카드 없이 사용 가능한 행동)도 동일한 `ActionSubmission`으로 표현된다.

### 2.4 판정의 일반화 공식

판정은 단일 조건이 아니라 여러 축의 곱이다.

```text
판정
= 행동 적합성
× 근거 관련성
× 주장과 근거의 관계
× 증거 충분성
× 증거 독립성
× 대안 가설 제거 여부
× 수사 절차의 적절성
```

코드에서 `드워프에게 CCTV를 쓰면 성공`이라고 작성하면 안 된다. 다음 순서로 판정해야 한다.

```text
1. 이 행동을 현재 대상에 사용할 수 있는가?
2. 제출된 근거가 대상 주장과 관련 있는가?
3. 근거가 주장을 지지하는가, 충돌하는가?
4. 필요한 시간·장소·인물 범위를 모두 다루는가?
5. 증거 출처가 독립적인가?
6. 남아 있는 다른 설명을 제거했는가?
7. 결과에 따라 어떤 상태를 변화시킬 것인가?
```

이 7단계를 통과한 결과가 v1.0의 판정 결과표(직접 모순, 간접 의심, 합리적 불충분, 진실 공격, 무관 증거)를 그대로 재생산한다. 즉 v2.0은 v1.0의 판정표를 **하드코딩된 표에서 계산 결과로** 바꾼다.

### 2.5 게임 진행의 일반화 공식

```text
게임 진행
= Claim 공개
→ 질문
→ 발언 고정
→ 근거 수집
→ 지지·반박
→ 후속 Claim 공개
→ 목표 해결
→ 심리 상태 조절
→ 사건 결과
```

이 순환이 한 번의 심문 안에서 여러 번 반복된다. 한 번의 모순 확정이 새로운 Claim을 공개하고, 그 Claim이 다시 질문 대상이 되는 구조가 v1.0에서 서술한 “초기 알리바이 → 세부 질문 → 첫 모순 → 변명 → 교차 검증 → 지시자 공개”의 일반형이다.

### 2.6 세 가지 최상위 구현 원칙

이 문서의 모든 세부 규칙은 다음 세 원칙에서 파생된다.

1. **자연어가 아니라 Claim ID가 게임 규칙의 기준이다.**
   AI가 같은 주장을 몇 가지 문장으로 표현하든 판정 결과는 동일해야 한다.

2. **카드는 개별 로직이 아니라 공통 Intent의 변형이다.**
   카드가 100장이 되어도 Intent는 10종을 넘지 않아야 한다.

3. **새 사건은 코드가 아니라 데이터 패키지로 추가한다.**
   사건 추가에 필요한 작업은 JSON·대사·이미지·밸런스 데이터 작성뿐이어야 한다.

---

## 3. 5계층 아키텍처

### 3.1 계층 분리 원칙

시스템은 다섯 계층으로 나뉘며, 의존성은 **아래에서 위로 단방향**이다. 상위 계층은 하위 계층을 참조할 수 없다.

```text
  ┌─────────────────────────────────────┐
  │ 5. UI·연출 계층                      │  공개 정보만 표시
  │    Presentation Layer                │
  └──────────────▲──────────────────────┘
                 │ PublicDTO (정답 필드 제거됨)
  ┌──────────────┴──────────────────────┐
  │ 4. AI 표현 계층                       │  결정된 것을 연기
  │    Dialogue Renderer                 │
  └──────────────▲──────────────────────┘
                 │ allowed_claims / reaction_key
  ┌──────────────┴──────────────────────┐
  │ 3. 게임 규칙 계층                     │  자원·카드·상태 변화
  │    GameRule Engine                   │
  └──────────────▲──────────────────────┘
                 │ resolution / state delta
  ┌──────────────┴──────────────────────┐
  │ 2. 지식 상태 계층                     │  플레이어가 아는 것
  │    KnowledgeState                    │
  └──────────────▲──────────────────────┘
                 │ 조회만 가능 (쓰기 금지)
  ┌──────────────┴──────────────────────┐
  │ 1. 사건 논리 계층                     │  실제 진실
  │    TruthGraph Engine                 │
  └─────────────────────────────────────┘
```

#### 3.1.1 의존성 규칙 [확정]

| 규칙 | 내용 |
|---|---|
| **R-1** | TruthGraph는 어떤 계층도 참조하지 않는다. 순수 데이터다. |
| **R-2** | KnowledgeState는 TruthGraph를 **읽을 수 있지만 쓸 수 없다.** |
| **R-3** | GameRule Engine은 TruthGraph와 KnowledgeState를 읽고 KnowledgeState를 쓴다. TruthGraph는 쓰지 않는다. |
| **R-4** | Dialogue Renderer는 GameRule Engine이 전달한 허용 목록만 받는다. TruthGraph에 직접 접근할 수 없다. |
| **R-5** | Presentation Layer는 PublicDTO만 받는다. 어떤 엔진 객체도 직접 참조하지 않는다. |
| **R-6** | 어떤 계층도 사건 ID·용의자 ID·증거 ID를 코드에 상수로 갖지 않는다. |

이 여섯 규칙은 코드 리뷰의 체크리스트이자 아키텍처 테스트의 대상이다.

### 3.2 제1계층 — 사건 논리 계층 (TruthGraph Engine)

사건의 실제 진실을 보유한다.

**담당 범위**

- 실제 사건 타임라인
- 인물의 실제 위치와 행동
- 소유·지시·목격·이동 관계
- 주장과 실제 사건의 일치 여부
- 가능한 대안 가설
- 사건의 인과 관계

**불변식**

- 런타임에 변경되지 않는다. 사건 시작 시 로드되고 종료까지 읽기 전용이다.
- UI나 AI가 직접 수정할 수 없다.
- 적 기믹은 이 계층을 바꿀 수 없다. 기믹이 진실을 바꾸면 플레이어가 이미 수행한 추리가 무효가 되며, 이는 공정성 위반이다.
- 세이브 데이터에는 TruthGraph 자체가 아니라 `truth_graph_id`와 버전만 저장한다.

**대표 질의 인터페이스**

```text
TruthGraph.getClaimTruthRelation(claim_id) → TruthRelation
TruthGraph.getObservations(evidence_id)     → Observation[]
TruthGraph.getHypotheses(claim_id)          → Hypothesis[]
TruthGraph.getProofRules(claim_id, direction) → ProofRule[]
TruthGraph.isRouteAvailable(route_id, state)  → bool
```

### 3.3 제2계층 — 지식 상태 계층 (KnowledgeState)

플레이어가 현재 무엇을 알고 있는지를 보유한다.

**담당 범위**

- 공개된 주장
- 아직 묻지 않은 질문
- 의심 중인 주장
- 발언이 고정된 주장
- 증거로 지지된 주장
- 반박이 완료된 주장
- 이전 진술 이력
- 플레이어가 발견한 사건 관계

**핵심 원칙: 진실과 지식의 분리 [확정]**

사건 내부에서 어떤 주장이 거짓이어도, 플레이어가 아직 증명하지 못했다면 지식 상태는 여전히 `UNKNOWN` 또는 `SUSPECTED`여야 한다.

```text
TruthGraph:      claim_location.truth_relation = CONTRADICTED_BY_WORLD
KnowledgeState:  claim_location.epistemic_state = UNKNOWN
```

이 두 값이 동시에 존재하는 것이 정상이다. 두 값을 하나로 합치는 순간 UI가 정답을 누설한다.

**금지 사항**

- KnowledgeState가 TruthGraph의 `truth_relation`을 복사해 보관하지 않는다.
- 어떤 상태 필드도 `truth_relation`과 통계적 상관을 갖지 않는다. 예를 들어 거짓 Claim의 초기 저항도를 항상 높게 설정하면, 플레이어는 저항도만 보고 정답을 맞힐 수 있다.

### 3.4 제3계층 — 게임 규칙 계층 (GameRule Engine)

카드·자원·상태 변화를 처리한다.

**담당 범위**

- CP 소비
- 평정심 변화
- 강압 수사 변화
- 스트레스 변화
- 카드 드로우·버리기
- 저항도 변화
- 증거 손상
- 턴·제한 시간
- 승리·패배 판정

**구성 하위 모듈**

```text
ActionValidator          행동과 대상의 호환성 검사
EvidenceRelationEvaluator 근거와 주장의 관계 평가
ProofEvaluator           충분성·독립성·대안 가설 평가
ArgumentResolver         위 결과를 조합해 ResolutionCode 결정
ResolutionEffectApplier  결과를 상태 변화로 적용
ModifierSystem           적 기믹 Trigger·Effect 실행
ObjectiveEvaluator       목표 달성 검사
OutcomeEvaluator         결말 등급 판정
ResourceSystem           CP·평정심·강압·스트레스 관리
```

### 3.5 제4계층 — AI 표현 계층 (Dialogue Renderer)

결정된 사건 정보를 자연어로 표현한다.

**담당 범위**

- 용의자 말투
- 종족·직업·성격 표현
- 같은 주장의 문장 변주
- 질문에 대한 반응
- 판정 결과에 대한 감정 표현
- 비전투 이벤트 문장

**핵심 원칙**

AI가 사건의 진실이나 판정 결과를 만드는 것이 아니라, 엔진이 선택한 주장과 결과를 **연기**한다.

```text
엔진이 결정:   ResolutionCode = INDIRECT_SUSPICION
              ReactionKey = reaction_defensive_insufficient
              캐릭터 프로필 = 방어적, 격식체

AI가 표현:     “그 카드가 내 거라는 것만으로 내가 썼다는 증거가 됩니까?”
```

AI를 완전히 제거해도 게임은 폴백 문장으로 끝까지 진행 가능해야 한다. 이것이 계층 분리의 실질적 검증 방법이다.

### 3.6 제5계층 — UI·연출 계층 (Presentation Layer)

공개 가능한 정보만 표시한다.

**담당 범위**

- 진술문과 토큰
- 증거 카드
- 논리 조립 트레이
- 사건 기록
- 판정 연출
- 자원 게이지
- 적 행동 의도

**전달 금지 필드 [확정]**

이 계층에 다음 필드가 도달하면 구현 실패로 간주한다.

```text
truth_relation
truth_value
correct_evidence
required_proof_rule
guaranteed_evidence_sets
hidden_claim
is_decoy
is_lie
eliminate_hypotheses
```

이 목록은 자동 검사 대상이다. 24.3절의 공정성 검증에서 DTO 필드를 정적으로 검사한다.

### 3.7 계층 책임 매트릭스

| 질문 | 답하는 계층 |
|---|---|
| 실제로 무슨 일이 있었는가 | 1. TruthGraph |
| 이 주장이 진실인가 | 1. TruthGraph |
| 이 증거가 무엇을 관찰했는가 | 1. TruthGraph |
| 플레이어가 이것을 아는가 | 2. KnowledgeState |
| 이 주장이 화면에 보이는가 | 2. KnowledgeState |
| 용의자가 이 말을 했는가 | 2. KnowledgeState |
| 이 행동을 지금 쓸 수 있는가 | 3. GameRule |
| 이 근거가 충분한가 | 3. GameRule |
| CP가 얼마나 소비되는가 | 3. GameRule |
| 다음 페이즈로 가는가 | 3. GameRule |
| 이 주장을 어떤 문장으로 말하는가 | 4. Dialogue Renderer |
| 어떤 말투로 반응하는가 | 4. Dialogue Renderer |
| 화면 어디에 무엇을 그리는가 | 5. Presentation |

### 3.8 계층 위반의 대표 증상

| 증상 | 위반 계층 | 원인 |
|---|---|---|
| AI가 사건에 없는 증인을 언급한다 | 4 → 1 | 허용 Claim 목록이 아니라 사건 설명을 프롬프트에 넣음 |
| 거짓 진술의 문체가 미묘하게 다르다 | 4 → 1 | 프롬프트에 진실값이 전달됨 |
| 저항도만 보고 거짓을 맞힐 수 있다 | 2 → 1 | 저항도를 진실값에서 파생시킴 |
| 적 기믹 후 이전 추리가 무효가 된다 | 3 → 1 | 기믹이 Claim 의미를 변경함 |
| 특정 사건에서만 카드가 다르게 동작한다 | 3에 콘텐츠 결합 | 사건 ID 분기 존재 |
| UI 애니메이션이 정답에서만 다르다 | 5 → 1 | DTO에 정답 파생 필드 존재 |

---

## 4. 공통 객체 카탈로그

### 4.1 전체 객체 일람

| 객체 | 역할 | 소유 계층 |
|---|---|---|
| `CaseDefinition` | 사건 전체 데이터 패키지 | 1 |
| `Entity` | 사람·용의자·장소·물건·조직 | 1 |
| `Event` | 특정 시간과 장소에서 발생한 실제 사건 | 1 |
| `Claim` | 검증 가능한 최소 주장 | 1 (정의) / 2 (상태) |
| `Statement` | 여러 Claim을 자연어로 묶은 진술 | 2 |
| `ClaimToken` | Claim의 화면 표시 단위 (문장 내 span) | 2 |
| `QuestionRoute` | 질문을 통해 새 Claim을 공개하는 경로 | 1 (정의) / 2 (개방 여부) |
| `Evidence` | 사건에 관한 관찰 기록 | 1 (정의) / 2 (보유·손상) |
| `Observation` | 증거가 직접 관찰한 원자 사실 | 1 |
| `Hypothesis` | 증거만으로 배제되지 않은 대안 설명 | 1 |
| `ProofRule` | 주장을 지지하거나 반박하기 위한 조건 | 1 |
| `ActionDefinition` | 기본 심문 행동의 규칙 | 3 |
| `CardDefinition` | 카드 = Intent + 비용 + 대상 조건 + Modifier | 3 |
| `CardModifier` | 도장·포스트잇·병합이 부여하는 보정 | 3 |
| `ActionSubmission` | 플레이어가 실제로 제출한 행동 | 3 |
| `Resolution` | 판정 결과 | 3 |
| `EncounterModifier` | 적 전용 기믹 (Trigger·Condition·Effect) | 3 |
| `EncounterFlowNode` | 심문 진행 단계 | 1 (정의) / 3 (실행) |
| `Objective` | 전투에서 해결해야 하는 목표 | 1 (정의) / 3 (평가) |
| `OutcomeRule` | 완전 해결·부분 해결·강제 자백·실패 조건 | 1 (정의) / 3 (평가) |
| `SpeakerProfile` | 종족·직업·성격·말투 | 4 |
| `PublicDTO` | UI 전달용 정제 데이터 | 5 |

### 4.2 객체 관계

```text
CaseDefinition
 ├─ metadata
 ├─ Entity[]                    인물·장소·물건·조직
 ├─ Event[]                     실제로 일어난 일
 │    └─ participants → Entity
 ├─ Claim[]                     검증 가능한 주장
 │    ├─ subject/object → Entity
 │    ├─ facet: ClaimFacet
 │    └─ truth_relation → Event 정합성
 ├─ QuestionRoute[]             질문 경로
 │    ├─ preconditions → Claim 상태
 │    └─ reveals → Claim[]
 ├─ Evidence[]
 │    ├─ Observation[]          직접 관찰한 것
 │    ├─ not_proven[]           관찰하지 못한 것
 │    └─ alternative_hypotheses → Hypothesis[]
 ├─ Hypothesis[]                남아 있는 대안 설명
 ├─ ProofRule[]
 │    ├─ target_claim → Claim
 │    ├─ requirements           의미 기반 조건
 │    └─ guaranteed_evidence_sets → Evidence[][]
 ├─ EncounterFlowNode[]
 ├─ EncounterModifier[]
 ├─ Objective[]
 └─ OutcomeRule[]
```

### 4.3 런타임 객체와 정의 객체의 구분

혼동을 막기 위해 정의(Definition)와 상태(State)를 명확히 구분한다.

| 정의 객체 (읽기 전용) | 런타임 상태 객체 (변경 가능) |
|---|---|
| `Claim` | `ClaimState` (3축 상태) |
| `Evidence` | `EvidenceState` (보유·손상·감식 여부) |
| `QuestionRoute` | `RouteState` (개방·사용 완료) |
| `CardDefinition` | `CardInstance` (도장·포스트잇 장착) |
| `EncounterModifier` | `ModifierState` (발동 횟수) |
| `Objective` | `ObjectiveState` (달성 여부) |

정의 객체는 `CaseDefinition`에서 로드되고, 상태 객체는 `KnowledgeState` 또는 `EncounterState`에 존재한다. 세이브 데이터에는 상태 객체만 저장한다.

---

## 5. Claim 모델

### 5.1 Claim은 자연어가 아니라 의미 단위

자연어 문장 하나는 여러 개의 검증 가능한 주장을 포함한다.

**자연어**

> “어제 밤에는 창고에서 혼자 재고를 확인하고 바로 숙소로 갔습니다.”

**내부 데이터**

```text
Claim A: 용의자는 어제 밤 창고에 있었다.       facet = LOCATION
Claim B: 용의자는 혼자였다.                   facet = COMPANION
Claim C: 용의자는 재고를 확인했다.             facet = ACTION
Claim D: 용의자는 이후 숙소로 갔다.            facet = AFTERMATH
```

AI는 이 Claim들을 다양한 문장으로 표현하지만, 게임 규칙은 자연어 문장이 아니라 `claim_id`를 대상으로 처리한다.

### 5.2 원자성 원칙 [확정]

| 원칙 | 내용 |
|---|---|
| **A-1** | 하나의 Claim은 하나의 `facet`만 가진다. |
| **A-2** | 하나의 Claim은 독립적으로 참·거짓 판정이 가능해야 한다. |
| **A-3** | 두 Claim이 항상 함께 참이거나 함께 거짓이면 하나로 합친다. |
| **A-4** | 시간·장소·동행자·행동·이후 행동은 반드시 분리한다. |
| **A-5** | 복합 Claim은 정의 단계에서 만들지 않는다. 복합은 **표시 상태**이지 정의 상태가 아니다. |

A-5의 의미: “혼자 밥을 먹고 바로 잤다”를 하나의 Claim으로 정의하면 안 된다. 이는 세 개의 Claim(`COMPANION`, `ACTION`, `AFTERMATH`)을 하나의 `ClaimToken`으로 묶어 표시한 상태다. `CLARIFY` 행동은 이 표시 묶음을 풀 뿐이며, 새 Claim을 생성하지 않는다.

```text
정의:   claim_companion_01, claim_action_01, claim_aftermath_01   (항상 3개)
표시:   token_compound_01 = [claim_companion_01, claim_action_01, claim_aftermath_01]
CLARIFY 후 표시: token_01, token_02, token_03                      (3개로 분리)
```

이 구조 덕분에 CLARIFY의 판정 로직은 사건과 무관하게 “토큰이 참조하는 claim_id가 2개 이상인가”만 검사하면 된다.

### 5.3 ClaimFacet — 확장 가능한 분류 [확정]

v1.0의 육하원칙(누가·언제·어디서·무엇을·어떻게·왜)은 **상위 분류로 유지**하되, 구현에서는 다음 16종의 확장 가능한 태그를 사용한다.

| Facet | 의미 | v1.0 상위 분류 |
|---|---|---|
| `ACTOR` | 행위자 | 누가 |
| `TIME` | 시간 | 언제 |
| `LOCATION` | 장소 | 어디서 |
| `ACTION` | 행동 | 무엇을 |
| `OBJECT` | 대상 물건 | 무엇을 |
| `COMPANION` | 동행자 | 누가 |
| `OWNERSHIP` | 소유 관계 | 누가 / 무엇을 |
| `ROUTE` | 이동 경로 | 어디서 / 어떻게 |
| `SEQUENCE` | 행동 순서 | 언제 / 어떻게 |
| `METHOD` | 방법 | 어떻게 |
| `MOTIVE` | 동기 | 왜 |
| `CAUSE` | 원인 | 왜 |
| `ORDER` | 지시 관계 | 누가 / 왜 |
| `WITNESS` | 목격 관계 | 누가 |
| `SOURCE` | 정보 출처 | 어떻게 |
| `AFTERMATH` | 이후 행동·결과 | 언제 / 어떻게 |

**설계 의도**

이 확장이 있어야 절도·횡령·살인·산업재해·운송·부실 공사 등 서로 다른 사건을 같은 시스템에서 처리할 수 있다. 알리바이 사건은 `TIME`·`LOCATION`·`COMPANION` 중심이고, 지시 책임 사건은 `ORDER`·`SEQUENCE`·`SOURCE` 중심이며, 횡령 사건은 `OWNERSHIP`·`OBJECT`·`ROUTE` 중심이다. 6칸 고정 구조로는 두 번째 유형을 표현할 수 없다.

**상위 분류의 존속 이유**

육하원칙 상위 분류는 폐기하지 않는다. 다음 두 시스템이 이 분류를 소비한다.

- 포스트잇(분류 특화 모듈): `[어디서]` 계열 Facet에 보너스
- 적 기믹 분류: v1.0의 6대 코어 기믹은 상위 분류 단위로 정의됨

따라서 `ClaimFacet`은 `facet_group` 필드를 함께 갖는다.

```json
{ "facet": "ROUTE", "facet_group": ["WHERE", "HOW"] }
```

### 5.4 Facet 확장 규칙

새 Facet을 추가하는 것은 허용된다. 단 다음 조건을 지켜야 한다.

- 기존 Facet으로 표현 가능한 것을 중복 추가하지 않는다.
- 추가 시 반드시 `facet_group` 매핑을 지정한다.
- Facet 추가는 엔진 코드 변경이 아니라 **열거형 데이터 추가**여야 한다. 즉 `switch(facet)` 형태의 분기가 엔진에 존재하면 안 된다.
- Facet은 판정 결과를 바꾸지 않는다. 판정은 `ProofScope`를 기준으로 하며, Facet은 대상 필터링과 표시 분류에만 쓰인다.

---

## 6. Claim 상태 3축 모델

### 6.1 왜 하나의 상태로는 부족한가

v1.0의 `확인 잠금`은 두 가지 의미를 동시에 담고 있다.

1. **용의자가 그 말을 명확히 했다** — 이후 말을 바꾸면 자체 모순이 된다.
2. **증거가 그 주장을 지지한다** — 사실로 확정되었다.

이 둘은 다른 사건이다. 다음 상황을 생각해 보자.

> 용의자: “그 카드는 밤새 제가 갖고 있었습니다.”
> (반복 확인으로 발언이 고정되었지만, 이를 뒷받침하는 증거는 아직 없다.)

v1.0 구조에서는 이 상태를 표현할 수 없다. 확인 잠금을 걸면 “사실로 확정됨”이 되고, 걸지 않으면 “말 바꾸기를 추적할 수 없음”이 된다.

더 심각한 문제는 다음이다. 용의자가 **거짓말을 명확하게 고정**하는 것이 플레이어에게 유리한 상황이 있는데, 하나의 상태 축으로는 “거짓을 확정적으로 말하게 만들었다”와 “진실을 증명했다”를 구분할 수 없다. 이는 심문 게임의 핵심 쾌감(상대를 말로 묶어 놓고 증거로 무너뜨리기)을 구현 불가능하게 만든다.

따라서 v2.0은 Claim 상태를 **세 개의 독립 축**으로 분리한다.

### 6.2 축 1 — CommitmentState (발언 상태)

용의자가 그 말을 실제로 했는지를 나타낸다. **진실값과 무관하다.**

| 값 | 의미 | 진입 조건 |
|---|---|---|
| `UNSTATED` | 아직 말하지 않음 | 초기값 (숨겨진 Claim) |
| `ASSERTED` | 말했지만 명확하지 않음 | Statement로 공개됨 |
| `COMMITTED` | 명확하게 발언을 고정함 | `COMMIT` 행동 성공 |
| `REVISED` | 말을 수정함 | 후속 Statement에서 내용 변경 |
| `RETRACTED` | 발언을 철회함 | 용의자가 명시적으로 취소 |
| `CONTRADICTED` | 자신의 이전 말과 충돌함 | `COMMITTED` 이후 상충 진술 등장 |

**전이 규칙**

```text
UNSTATED ──(공개)──> ASSERTED ──(COMMIT 성공)──> COMMITTED
                        │                            │
                        │                            ├──(상충 진술)──> CONTRADICTED
                        ├──(내용 변경)──> REVISED     │
                        └──(취소)──> RETRACTED       └──(취소)──> RETRACTED
```

**핵심 성질**: `COMMITTED`는 그 말이 사실이라는 뜻이 아니다. 오직 “용의자가 그렇게 말했음이 확정되었다”는 뜻이다.

### 6.3 축 2 — EpistemicState (사실 검증 상태)

증거가 해당 주장을 얼마나 지지하거나 반박하는지를 나타낸다. **플레이어의 지식이며, 진실 자체가 아니다.**

| 값 | 의미 | 진입 조건 |
|---|---|---|
| `UNKNOWN` | 미검토 | 초기값 |
| `SUSPECTED` | 의심 | 충돌 근거가 있으나 충분성 미달 |
| `PROVISIONAL` | 잠정적으로 지지됨 | 지지 근거가 있으나 대안 남음 |
| `SUPPORTED` | 충분히 지지됨 | ProofRule(`SUPPORT`) 충족 |
| `REFUTED` | 충분히 반박됨 | ProofRule(`CONTRADICT`) 충족 |
| `UNRESOLVED` | 증거 부족으로 미해결 | 필요한 증거가 소실되어 해결 경로 없음 |

**전이 규칙**

```text
UNKNOWN ──(충돌 근거, 불충분)──> SUSPECTED ──(충분)──> REFUTED
   │                                 │
   │                                 └──(지지 근거로 전환)──> PROVISIONAL
   ├──(지지 근거, 불충분)──> PROVISIONAL ──(충분)──> SUPPORTED
   └──(해결 경로 상실)──> UNRESOLVED
```

**되돌림 규칙 [확정]**

- `SUPPORTED`와 `REFUTED`는 원칙적으로 되돌리지 않는다. 이미 성립한 증거 관계를 적 기믹이 취소하면 플레이어의 추리가 무효화된다.
- 예외는 새로운 증거가 기존 판정을 뒤집는 경우이며, 이는 `OutcomeRule` 수준의 서사 장치로만 사용한다. [후속 확장]
- `UNRESOLVED`는 되돌릴 수 있다. 대체 증거를 획득하면 다시 평가한다.

### 6.4 두 축의 동시 존재

다음 조합이 정상적으로 존재한다.

```text
CommitmentState: COMMITTED
EpistemicState:  UNKNOWN
```

의미:

> “용의자가 그렇게 말했다는 것은 확정되었지만, 그 말이 사실인지는 아직 모른다.”

이 구조가 있어야 다음 플레이가 가능하다.

1. 플레이어가 `COMMIT`으로 용의자의 알리바이를 고정한다. → `COMMITTED / UNKNOWN`
2. 플레이어가 증거를 모아 `CONTRADICT`를 제출한다. → `COMMITTED / REFUTED`
3. 엔진이 “고정된 발언이 반박됨”을 감지해 강한 후속 반응과 페이즈 전환을 발생시킨다.

만약 1단계 없이 3단계로 가면(= `ASSERTED / REFUTED`) 용의자는 “대충 말한 것뿐”이라고 빠져나갈 수 있다. 이 차이가 심문 게임의 전략성을 만든다.

### 6.5 축 3 — PresentationState (표시 상태)

UI와 적 기믹에 의한 표현 상태다. **의미와 진실값을 바꾸지 않는다.**

| 값 | 의미 |
|---|---|
| `NORMAL` | 정상 표시 |
| `COMPOUND` | 여러 Claim이 한 토큰으로 묶여 표시됨 |
| `DISTORTED` | 적 기믹으로 표현이 교란됨 |
| `HIDDEN` | 화면에 표시되지 않음 |
| `LOCKED` | 일시적으로 선택 불가 |
| `DUPLICATED` | 유사 표현으로 복제 표시됨 |

**불변식 [확정]**

- 적 기믹은 이 축만 바꿀 수 있다.
- `DISTORTED` 상태에서도 내부 `claim_id`는 유지된다.
- `DISTORTED`·`DUPLICATED`는 원문 진술 이력을 삭제하지 않는다.
- `LOCKED`는 영구 잠금이 될 수 없다. 반드시 해제 수단(카운터플레이)이 존재해야 한다.
- `HIDDEN`은 필수 목표 Claim에 적용할 수 없다.

### 6.6 3축 조합 의미표

| Commitment | Epistemic | 의미 | 대표 플레이 상황 |
|---|---|---|---|
| `UNSTATED` | `UNKNOWN` | 아직 존재를 모름 | 미진술 슬롯 |
| `ASSERTED` | `UNKNOWN` | 말했지만 미검증 | 초기 진술 직후 |
| `ASSERTED` | `SUSPECTED` | 약한 근거로 의심 중 | 보강 단서 1장 제출 후 |
| `ASSERTED` | `REFUTED` | 흘리듯 한 말이 반박됨 | 약한 타격, 용의자가 회피 가능 |
| `COMMITTED` | `UNKNOWN` | 말을 묶었으나 아직 미검증 | **함정 설치 완료 상태** |
| `COMMITTED` | `SUPPORTED` | 고정된 말이 사실로 확인됨 | 후보군 축소, 다른 Claim에 집중 |
| `COMMITTED` | `REFUTED` | **고정된 말이 무너짐** | 최대 타격, 페이즈 전환 유발 |
| `REVISED` | `SUSPECTED` | 말을 바꿨고 의심스러움 | 자체 모순 추궁 경로 |
| `CONTRADICTED` | any | 자기 말끼리 충돌 | `CROSS_CHECK` 성립 |
| any | `UNRESOLVED` | 증명 경로 상실 | 부분 실패 위험 |

이 표는 서술이 아니라 **판정 규칙의 입력**이다. `ResolutionEffectApplier`는 이 조합을 참조해 평정심 피해 배율과 후속 Claim 공개 여부를 결정한다.

### 6.7 상태 간 불변식 검사

다음은 런타임 assert 대상이다. 위반 시 콘텐츠 오류다.

| 불변식 | 내용 |
|---|---|
| **I-1** | `CommitmentState == UNSTATED`인 Claim은 `PresentationState`가 `HIDDEN`이어야 한다. |
| **I-2** | `EpistemicState`는 `CommitmentState`와 독립적으로 변할 수 있다. 한쪽 변화가 다른 쪽을 자동 변경해서는 안 된다. |
| **I-3** | `PresentationState` 변경은 `CommitmentState`·`EpistemicState`를 절대 변경하지 않는다. |
| **I-4** | 필수 목표 Claim은 `UNRESOLVED`로 진입할 때 반드시 대체 해결 경로 존재 여부를 재검사한다. |
| **I-5** | `TruthGraph.truth_relation`은 어떤 상태 축에도 복사되지 않는다. |

---

## 7. 일반화 완료 판정 기준

### 7.1 잘못된 판정 기준

일반화가 제대로 되었는지는 **첫 번째 사건이 작동하는 것으로 판단하면 안 된다.** 하드코딩된 구현도 첫 번째 사건은 완벽하게 작동한다. 오히려 하드코딩이 더 빠르고 더 안정적이다.

### 7.2 올바른 판정 기준 [확정]

> **두 번째 사건을 추가할 때 공통 엔진 코드를 수정하지 않고, 사건 JSON·대사·이미지·밸런스 데이터만 추가해서 플레이할 수 있어야 한다.**

이 기준을 실제로 측정하기 위해, 제3부에서 **구조가 서로 다른 두 사건**을 정의한다.

```text
사건 A: 장소 알리바이 + 소유 관계     (TIME, LOCATION, COMPANION, OWNERSHIP)
사건 B: 행동 순서 + 지시 관계         (SEQUENCE, ORDER, SOURCE, AFTERMATH)
```

사건 B가 엔진 코드 변경 없이 돌아가면 일반화가 검증된 것이다.

### 7.3 완료 조건 체크리스트

| # | 조건 | 검증 방법 |
|---|---|---|
| 1 | 다른 용의자를 데이터만으로 추가 가능 | 사건 B의 `SpeakerProfile` 교체 |
| 2 | 다른 Claim 분류를 데이터만으로 추가 가능 | `SEQUENCE`·`ORDER` Facet 사용 |
| 3 | 다른 증거 조합을 데이터만으로 추가 가능 | 사건 B의 `ProofRule` 작성 |
| 4 | 다른 질문 경로를 데이터만으로 추가 가능 | 사건 B의 `InquiryGraph` 작성 |
| 5 | 다른 승리 조건을 데이터만으로 추가 가능 | `Objective` 유형 변경 |
| 6 | 다른 적 기믹을 기존 Trigger·Effect 조합으로 추가 가능 | 사건 B 기믹 정의 |
| 7 | AI를 끄고도 폴백 데이터로 사건 진행 가능 | AI 비활성 모드 플레이 |
| 8 | 같은 사건의 표현이 바뀌어도 판정 결과가 동일 | 동일 시드 재생성 후 판정 로그 비교 |

**허용되는 예외**

새 적 기믹을 위해 새 `EffectType`을 추가하는 것은 허용한다. 이는 엔진의 어휘를 늘리는 것이며 콘텐츠 결합이 아니다. 다만 다음은 허용하지 않는다.

- 특정 `case_id`·`suspect_id`·`evidence_id`를 검사하는 코드
- 특정 사건에서만 동작하는 판정 분기
- 특정 카드 이름에 반응하는 로직

### 7.4 지금 일반화해야 하는 것

| 대상 | 일반화 방식 |
|---|---|
| 사건 | `CaseDefinition` 데이터 |
| 주장 | `Claim` 객체 |
| 질문 | `QuestionRoute` |
| 증거 | 관찰·한계·출처 데이터 |
| 판정 | 공통 `ArgumentResolver` |
| 카드 | `ActionIntent` + `CardModifier` |
| 적 기믹 | `Trigger` + `Effect` |
| 승패 | `Objective` + `OutcomeRule` |
| 대사 | Claim 기반 AI 표현 |
| UI | Public DTO 기반 렌더링 |

### 7.5 해커톤 단계에서 일반화하지 말아야 하는 것 [확정]

일반화에도 비용이 있다. 다음 항목은 의도적으로 일반화하지 않는다.

| 대상 | 이유 |
|---|---|
| 완전 자유 질문 | 무한한 자연어 해석 필요 |
| AI가 Claim을 자유 생성 | 사건 논리 붕괴 위험 |
| AI가 ProofRule 생성 | 판정 비결정성 |
| 모든 자연어 증거 조합 자동 추론 | 구현 범위 폭발 |
| 사건 전체 자동 생성 | 검증 불가능한 퍼즐 생성 위험 |
| 완전한 동적 법률·수사 논리 | 해커톤 MVP 범위 초과 |
| 카드 효과 자유 스크립트 | 디버깅·보안·밸런스 문제 |

따라서 올바른 일반화의 정의는 다음과 같다.

> **사건 구조는 데이터화하되, 사건 논리는 기획자가 검증한다.
> AI는 자유롭게 사건을 만들지 않고 검증된 구조를 표현한다.**

### 7.6 일반화 수준의 판단 축

새로운 요소를 만들 때 다음 질문으로 일반화 수준을 결정한다.

```text
Q1. 이것이 두 번째 사건에서도 필요한가?
    아니오 → 데이터로 표현. 엔진에 넣지 않는다.
    예 → Q2

Q2. 이것을 기존 Intent·Trigger·Effect 조합으로 표현할 수 있는가?
    예 → 조합으로 표현. 새 개념을 만들지 않는다.
    아니오 → Q3

Q3. 이것이 다섯 개 이상의 사건에서 재사용될 것인가?
    예 → 엔진 어휘에 추가 (새 Effect, 새 Facet 등)
    아니오 → SPECIAL Intent 또는 사건 전용 Effect로 처리하고 재평가 시점을 기록
```
# 제2부 — 세분화

> 각 모듈을 어떻게 구현하는가

---

## 8. 모듈 구성과 책임

### 8.1 디렉터리 구조 [확정]

```text
Game/
├─ Domain/                    사건 논리 계층 (읽기 전용 데이터)
│  ├─ CaseDefinition
│  ├─ TruthGraph
│  ├─ Entity
│  ├─ Event
│  ├─ Claim
│  ├─ Evidence
│  ├─ Observation
│  ├─ Hypothesis
│  ├─ ProofRule
│  └─ QuestionRoute
│
├─ Knowledge/                 지식 상태 계층
│  ├─ KnowledgeState
│  ├─ ClaimState
│  ├─ EvidenceState
│  ├─ RouteState
│  └─ StatementHistory
│
├─ Resolution/                판정
│  ├─ ActionValidator
│  ├─ EvidenceRelationEvaluator
│  ├─ ProofEvaluator
│  ├─ IndependenceEvaluator
│  ├─ HypothesisEvaluator
│  ├─ ArgumentResolver
│  └─ ResolutionEffectApplier
│
├─ Encounter/                 전투 진행
│  ├─ EncounterStateMachine
│  ├─ EncounterFlowRunner
│  ├─ ObjectiveEvaluator
│  ├─ OutcomeEvaluator
│  ├─ ModifierSystem
│  └─ ResourceSystem
│
├─ Cards/                     행동과 카드
│  ├─ ActionDefinition
│  ├─ CardDefinition
│  ├─ CardInstance
│  ├─ CardModifier
│  └─ DeckState
│
├─ Content/                   데이터 로딩
│  ├─ CaseRepository
│  ├─ CardRepository
│  ├─ ModifierRepository
│  ├─ DialogueFallbackRepository
│  └─ SchemaValidator
│
├─ AI/                        표현 계층
│  ├─ DialogueRequestBuilder
│  ├─ DialogueRenderer
│  ├─ OutputValidator
│  ├─ ContentCache
│  └─ FallbackProvider
│
├─ Presentation/              UI 경계
│  ├─ PublicDTOMapper
│  ├─ StatementView
│  ├─ EvidenceView
│  ├─ ArgumentTrayView
│  └─ CaseBoardView
│
└─ Tests/
   ├─ SolvabilityTests
   ├─ ProofRuleTests
   ├─ LeakageTests
   ├─ CardReachabilityTests
   ├─ AIContractTests
   └─ GeneralizationTests
```

### 8.2 모듈별 입출력 계약

| 모듈 | 입력 | 출력 | 부작용 |
|---|---|---|---|
| `CaseRepository` | `case_id` | `CaseDefinition` | 없음 |
| `SchemaValidator` | `CaseDefinition` | `ValidationReport` | 없음 |
| `ActionValidator` | `ActionSubmission`, `KnowledgeState` | `Validity` | 없음 |
| `EvidenceRelationEvaluator` | `target Claim`, `Evidence[]` | `Relevance`, `Relation` | 없음 |
| `ProofEvaluator` | `Claim`, `Evidence[]`, `ProofRule[]` | `Sufficiency` | 없음 |
| `IndependenceEvaluator` | `Evidence[]` | `independent_source_count` | 없음 |
| `HypothesisEvaluator` | `Claim`, `Evidence[]` | `remaining_hypotheses[]` | 없음 |
| `ArgumentResolver` | 위 결과 전부 | `Resolution` | 없음 |
| `ResolutionEffectApplier` | `Resolution` | `StateDelta` | `KnowledgeState`·`EncounterState` 변경 |
| `ModifierSystem` | `Trigger`, `EncounterState` | `Effect[]` | `EncounterState` 변경 |
| `ObjectiveEvaluator` | `KnowledgeState`, `Objective[]` | `ObjectiveState[]` | 없음 |
| `OutcomeEvaluator` | `ObjectiveState[]`, `ResourceState` | `OutcomeCode` 또는 `null` | 없음 |
| `PublicDTOMapper` | 엔진 상태 | `PublicDTO` | 없음 |

**핵심 설계 규칙**: 평가(Evaluator)는 순수 함수이며 부작용이 없다. 상태 변경은 오직 `ResolutionEffectApplier`와 `ModifierSystem`만 수행한다. 이 규칙 덕분에 판정 로직 전체를 상태 없이 단위 테스트할 수 있다.

### 8.3 모듈 의존성 규칙

```text
Domain      ← 아무것도 참조하지 않음
Knowledge   ← Domain
Resolution  ← Domain, Knowledge
Encounter   ← Domain, Knowledge, Resolution, Cards
Cards       ← Domain
Content     ← Domain
AI          ← Domain(제한적: Claim의 canonical_meaning만), Knowledge(공개분만)
Presentation ← PublicDTO만
Tests       ← 전부
```

`AI` 모듈이 `Domain`을 참조하되 **`truth_relation`·`ProofRule`·`Hypothesis`에는 접근할 수 없다.** 이는 타입 수준에서 강제한다. `Claim`을 그대로 넘기지 않고 `RenderableClaim`이라는 축소 뷰를 사용한다.

```text
RenderableClaim {
  claim_id: string
  canonical_meaning: string
  facet: ClaimFacet
}
```

---

## 9. CaseDefinition 데이터 명세

### 9.1 최상위 구조

```json
{
  "schema_version": "2.0",
  "case_id": "case_example_001",
  "metadata": { },
  "entities": [ ],
  "events": [ ],
  "claims": [ ],
  "inquiry_routes": [ ],
  "evidence": [ ],
  "hypotheses": [ ],
  "proof_rules": [ ],
  "encounter": { },
  "flow_nodes": [ ],
  "modifiers": [ ],
  "objectives": { },
  "outcomes": [ ],
  "dialogue": { }
}
```

### 9.2 최상위 필드 정의

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `schema_version` | string | 예 | 스키마 버전. 로더가 호환성 검사에 사용 |
| `case_id` | string | 예 | 전역 고유 사건 ID |
| `metadata` | object | 예 | 제목·난이도·태그·작성자 |
| `entities` | Entity[] | 예 | 등장 인물·장소·물건·조직 |
| `events` | Event[] | 예 | 실제로 일어난 일 (TruthGraph의 뼈대) |
| `claims` | Claim[] | 예 | 검증 가능한 주장 목록 |
| `inquiry_routes` | QuestionRoute[] | 아니오 | 질문 경로. 없으면 초기 공개 Claim만 존재 |
| `evidence` | Evidence[] | 예 | 획득 가능한 증거 |
| `hypotheses` | Hypothesis[] | 아니오 | 대안 설명 목록 |
| `proof_rules` | ProofRule[] | 예 | 입증·반박 조건 |
| `encounter` | object | 예 | 초기 자원·초기 페이즈 |
| `flow_nodes` | FlowNode[] | 예 | 진행 그래프 |
| `modifiers` | EncounterModifier[] | 아니오 | 적 기믹 |
| `objectives` | object | 예 | 필수·선택 목표 |
| `outcomes` | OutcomeRule[] | 예 | 결말 조건 |
| `dialogue` | object | 예 | 화자 프로필과 폴백 문장 |

### 9.3 metadata

```json
{
  "metadata": {
    "title": "사건명",
    "act": 1,
    "difficulty": 1,
    "tags": ["ALIBI", "OWNERSHIP"],
    "estimated_turns": 8,
    "author": "planner_id",
    "content_version": "1.0.0",
    "validated_at": "2026-07-26"
  }
}
```

`tags`는 사건 구조 분류이며 무작위 사건 풀 구성과 밸런스 분석에 사용한다. 엔진 로직은 `tags`로 분기하지 않는다.

### 9.4 Entity

```json
{
  "entity_id": "entity_suspect_01",
  "type": "PERSON",
  "role": "SUSPECT",
  "display_name_key": "loc.case001.suspect_01.name",
  "attributes": {
    "race": "RACE_A",
    "job": "JOB_A"
  }
}
```

| 필드 | 타입 | 설명 |
|---|---|---|
| `entity_id` | string | 고유 ID |
| `type` | enum | `PERSON` / `LOCATION` / `OBJECT` / `ORGANIZATION` / `RECORD_SYSTEM` |
| `role` | enum | `SUSPECT` / `WITNESS` / `VICTIM` / `BYSTANDER` / `SCENE` / `INSTRUMENT` / `NONE` |
| `display_name_key` | string | 현지화 키. 엔진은 원문 문자열을 갖지 않음 |
| `attributes` | object | 자유 속성. AI 프로필 구성에만 사용 |

### 9.5 Event

`Event`는 TruthGraph의 원자 단위다. **실제로 일어난 일**만 기록한다. 용의자의 주장은 `Event`가 아니라 `Claim`이다.

```json
{
  "event_id": "event_01",
  "time": { "from": "22:00", "to": "22:10", "error_seconds": 60 },
  "location_id": "entity_location_02",
  "participants": ["entity_suspect_01"],
  "action": "ACTION_TYPE_A",
  "objects": ["entity_object_01"],
  "caused_by": "event_00",
  "instructed_by": "entity_person_02"
}
```

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `event_id` | string | 예 | 고유 ID |
| `time` | TimeRange | 예 | 발생 시각 구간과 허용 오차 |
| `location_id` | string | 아니오 | 발생 장소 |
| `participants` | string[] | 예 | 참여 Entity |
| `action` | string | 예 | 행동 유형 키 |
| `objects` | string[] | 아니오 | 관련 물건 |
| `caused_by` | string | 아니오 | 선행 사건 |
| `instructed_by` | string | 아니오 | 지시자 |

**TimeRange 구조**

```json
{ "from": "22:00", "to": "22:10", "error_seconds": 60 }
```

`error_seconds`는 시간 충돌 판정에서 결정적이다. 두 Event 또는 Event와 Observation의 시간 구간이 오차를 포함해도 겹치지 않을 때만 “동시 존재 불가”가 성립한다.

### 9.6 Claim

```json
{
  "claim_id": "claim_location_01",
  "subject_id": "entity_suspect_01",
  "predicate": "LOCATED_AT",
  "object_id": "entity_location_01",
  "time_ref": { "from": "22:00", "to": "22:10" },
  "facet": "LOCATION",
  "facet_group": ["WHERE"],
  "canonical_meaning_key": "loc.case001.claim_location_01",
  "truth_relation": "CONTRADICTED_BY_WORLD",
  "supporting_event_ids": [],
  "initial_commitment": "ASSERTED",
  "initial_visibility": "VISIBLE",
  "resistance": 2,
  "is_required": true,
  "presentation_group": "token_group_01"
}
```

| 필드 | 타입 | 필수 | 계층 | 설명 |
|---|---|---|---|---|
| `claim_id` | string | 예 | 1 | 고유 ID |
| `subject_id` | string | 예 | 1 | 주어 Entity |
| `predicate` | enum | 예 | 1 | 술어 유형 (9.7 참조) |
| `object_id` | string | 아니오 | 1 | 목적어 Entity |
| `time_ref` | TimeRange | 아니오 | 1 | 주장이 가리키는 시각 |
| `facet` | ClaimFacet | 예 | 1 | 분류 |
| `facet_group` | string[] | 예 | 1 | 육하원칙 상위 분류 |
| `canonical_meaning_key` | string | 예 | 1 | AI에 전달할 표준 의미 (현지화 키) |
| `truth_relation` | enum | 예 | **1 (엔진 전용)** | 진실 관계 |
| `supporting_event_ids` | string[] | 아니오 | 1 | 이 주장을 참으로 만드는 Event |
| `initial_commitment` | CommitmentState | 예 | 2 | 초기 발언 상태 |
| `initial_visibility` | enum | 예 | 2 | `VISIBLE` / `HIDDEN` |
| `resistance` | int | 예 | 2 | 저항도 (0~3) [밸런스 초안] |
| `is_required` | bool | 예 | 1 | 필수 목표 여부 |
| `presentation_group` | string | 아니오 | 2 | 같은 값이면 하나의 복합 토큰으로 표시 |

#### 9.6.1 truth_relation 열거형

| 값 | 의미 |
|---|---|
| `CONSISTENT_WITH_WORLD` | 실제 사건과 일치. 진실 |
| `CONTRADICTED_BY_WORLD` | 실제 사건과 불일치. 거짓 |
| `PARTIALLY_TRUE` | 일부만 일치 (예: 장소는 맞고 시간이 틀림) |
| `UNVERIFIABLE` | 사건 데이터로는 참·거짓을 결정할 수 없음 |
| `IRRELEVANT` | 사건과 무관한 사적 정보 |

`UNVERIFIABLE`과 `IRRELEVANT`는 반드시 존재해야 한다. 모든 Claim이 참 또는 거짓이면 플레이어는 “저항하는 것은 거짓”이라는 휴리스틱으로 게임을 풀 수 있다. v1.0이 “진실에도 사생활에도 방어막이 생길 수 있다”고 규정한 것을 데이터로 보장하는 장치다.

#### 9.6.2 저항도 배정 규칙 [확정]

```text
저항도는 truth_relation의 함수가 되어서는 안 된다.
```

콘텐츠 검증기는 사건 내 Claim 집합에 대해 `resistance`와 `truth_relation`의 상관계수를 계산하고, 임계값을 초과하면 경고를 발생시킨다(24.3 참조). 저항도는 **캐릭터의 심리**에서 도출한다.

| 저항도 | 심리적 근거 예시 |
|---|---|
| 0 | 숨길 이유가 없는 사실 |
| 1 | 약간 곤란하지만 인정 가능 |
| 2 | 사생활이거나 책임 문제가 걸림 |
| 3 | 핵심 이해관계가 걸림 (거짓일 수도, 진실일 수도) |

### 9.7 predicate 열거형

`predicate`는 Claim의 의미 구조를 나타내며, `ProofScope` 산출과 시간·장소 충돌 판정에 사용된다.

| predicate | 의미 | 파생 ProofScope |
|---|---|---|
| `LOCATED_AT` | 특정 시각에 특정 장소에 있었다 | `IDENTITY`, `TIME`, `LOCATION` |
| `PERFORMED_ACTION` | 어떤 행동을 했다 | `IDENTITY`, `TIME`, `ACTION` |
| `ACCOMPANIED_BY` | 누구와 함께 있었다 | `IDENTITY`, `TIME`, `PRESENCE` |
| `OWNED` | 무엇을 소유·소지했다 | `IDENTITY`, `OWNERSHIP` |
| `USED` | 무엇을 사용했다 | `IDENTITY`, `OWNERSHIP`, `ACTION` |
| `MOVED_FROM_TO` | 어디서 어디로 이동했다 | `IDENTITY`, `TIME`, `LOCATION`, `ROUTE` |
| `OCCURRED_BEFORE` | A가 B보다 먼저 일어났다 | `TIME`, `SEQUENCE` |
| `INSTRUCTED_BY` | 누구의 지시를 받았다 | `IDENTITY`, `INSTRUCTION` |
| `WITNESSED` | 무엇을 목격했다 | `IDENTITY`, `TIME`, `LOCATION` |
| `MOTIVATED_BY` | 어떤 동기로 행동했다 | `MOTIVE` |
| `HAS_PROPERTY` | 어떤 속성을 가진다 | `IDENTITY` |

`predicate` 목록은 확장 가능하다. 확장 시 반드시 파생 `ProofScope` 매핑을 함께 정의한다. 이 매핑이 있어야 `ProofRule`을 사건마다 손으로 쓰지 않고 기본값을 자동 생성할 수 있다.

### 9.8 스키마 버전 관리

| 규칙 | 내용 |
|---|---|
| 메이저 버전 상승 | 필드 삭제·의미 변경. 마이그레이션 스크립트 필수 |
| 마이너 버전 상승 | 필드 추가. 구버전 데이터는 기본값으로 로드 |
| 로더 동작 | `schema_version`이 지원 범위를 벗어나면 로드 거부 후 명시적 오류 |
| 세이브 호환 | 세이브에는 `case_id` + `content_version`을 기록. 콘텐츠가 바뀌면 진행 중 사건은 무효 처리 |

---

## 10. InquiryGraph 명세

### 10.1 미진술 슬롯의 일반화

미진술 슬롯은 단순한 빈칸이 아니라 **아직 공개되지 않은 Claim으로 이어지는 질문 경로**다.

```text
QuestionRoute
├─ 현재 알고 있는 정보          preconditions
├─ 질문 가능한 대상            target_slot
├─ 사용 가능한 질문 행동        allowed_intents
├─ 공개되는 Claim              reveals
├─ 새로 생성되는 후속 질문       unlocks_routes
└─ 부작용 또는 위험             side_effects
```

### 10.2 QuestionRoute 구조

```json
{
  "route_id": "route_ownership_01",
  "target_slot": "slot_item_owner",
  "slot_label_key": "loc.case001.slot_item_owner",
  "facet": "OWNERSHIP",
  "allowed_intents": ["QUERY", "COMMIT"],
  "preconditions": [
    { "type": "CLAIM_VISIBLE", "claim_id": "claim_item_used" }
  ],
  "reveals": ["claim_item_owner"],
  "unlocks_routes": ["route_ownership_02"],
  "creates_commitment": true,
  "commitment_level": "ASSERTED",
  "coercion_risk": 0,
  "composure_delta": 0,
  "single_use": true
}
```

### 10.3 필드 정의

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `route_id` | string | 예 | 고유 ID |
| `target_slot` | string | 예 | 화면에 표시할 슬롯 식별자 |
| `slot_label_key` | string | 예 | 슬롯 라벨 현지화 키 (예: “동기: 미진술”) |
| `facet` | ClaimFacet | 예 | 이 슬롯이 다루는 분류 |
| `allowed_intents` | ActionIntent[] | 예 | 이 경로를 열 수 있는 행동 |
| `preconditions` | Condition[] | 아니오 | 개방 선행 조건 |
| `reveals` | string[] | 예 | 공개되는 Claim ID |
| `unlocks_routes` | string[] | 아니오 | 후속으로 열리는 질문 경로 |
| `creates_commitment` | bool | 예 | 공개된 Claim이 발언 상태를 갖는지 |
| `commitment_level` | CommitmentState | 아니오 | 공개 시 초기 발언 상태 |
| `coercion_risk` | int | 예 | 사용 시 강압 수사 증가량 [밸런스 초안] |
| `composure_delta` | int | 예 | 사용 시 평정심 변화 [밸런스 초안] |
| `single_use` | bool | 예 | 재사용 가능 여부 |

### 10.4 Condition 표현식

`preconditions`, `enter_conditions`, `modifier.condition`은 모두 동일한 조건 표현식 문법을 사용한다. 자유 스크립트가 아니라 **구조화된 조건 객체**를 쓴다. 이는 검증 가능성과 보안 때문이다.

| type | 파라미터 | 의미 |
|---|---|---|
| `CLAIM_VISIBLE` | `claim_id` | 해당 Claim이 화면에 공개됨 |
| `CLAIM_COMMITMENT` | `claim_id`, `state` | 발언 상태가 지정 값 |
| `CLAIM_EPISTEMIC` | `claim_id`, `state` | 검증 상태가 지정 값 |
| `EVIDENCE_OWNED` | `evidence_id` | 해당 증거 보유 |
| `EVIDENCE_INTACT` | `evidence_id` | 증거가 손상되지 않음 |
| `ROUTE_USED` | `route_id` | 해당 질문 경로 사용 완료 |
| `RESOURCE_RANGE` | `resource`, `min`, `max` | 자원이 구간 내 |
| `PHASE_IS` | `node_id` | 현재 페이즈 |
| `TURN_RANGE` | `min`, `max` | 턴 수 구간 |
| `OBJECTIVE_DONE` | `objective_id` | 목표 달성 |
| `ALL_OF` | `conditions[]` | 논리곱 |
| `ANY_OF` | `conditions[]` | 논리합 |
| `NOT` | `condition` | 부정 |

```json
{
  "type": "ALL_OF",
  "conditions": [
    { "type": "CLAIM_EPISTEMIC", "claim_id": "claim_location_01", "state": "REFUTED" },
    { "type": "NOT", "condition": { "type": "ROUTE_USED", "route_id": "route_ownership_01" } }
  ]
}
```

### 10.5 질문 경로의 일반 원칙 [확정]

| 원칙 | 내용 |
|---|---|
| **Q-1** | 플레이어는 엔진에 등록된 질문 경로만 열 수 있다. |
| **Q-2** | AI가 자유롭게 핵심 사실을 추가하지 않는다. |
| **Q-3** | 질문 결과는 새 Claim을 공개하거나 기존 Claim을 구체화한다. |
| **Q-4** | 질문은 반드시 정보 상태를 변화시켜야 한다. |
| **Q-5** | 아무 변화도 없는 질문은 콘텐츠에서 제외한다. |

Q-4와 Q-5는 검증기가 강제한다. `reveals`와 `unlocks_routes`가 모두 비어 있고 `creates_commitment`가 `false`인 경로는 콘텐츠 오류로 처리한다.

### 10.6 등록되지 않은 질문의 처리

플레이어가 사건에 존재하지 않는 대상을 질문하려 할 때의 처리는 다음과 같다.

```text
1. 해당 대상에 열린 QuestionRoute가 없다
2. → ActionValidator가 INVALID 반환
3. → CP 소비 없음 (또는 부분 환급) [밸런스 초안]
4. → ReactionKey = "reaction_out_of_scope"
5. → AI가 “그건 이 사건과 관계없습니다” 계열 문장 생성
6. → 어떤 새 사실도 생성되지 않음
```

5단계에서 AI가 사건에 없는 정보를 즉흥 생성하는 것을 막는 것이 핵심이다. 프롬프트에는 `allowed_claims`가 빈 배열로 전달된다.

### 10.7 InquiryGraph 도달성 요구사항

콘텐츠 검증기는 다음을 검사한다.

- 모든 필수 Claim은 초기 공개되거나, 초기 공개 Claim에서 시작하는 경로로 도달 가능해야 한다.
- 순환 참조가 없어야 한다(`unlocks_routes` 그래프의 사이클 검사).
- `single_use: true`인 경로가 필수 Claim의 유일한 공개 수단이면서 선행 조건이 소실 가능한 경우 경고를 발생시킨다.

---

## 11. Evidence 명세

### 11.1 증거는 결론이 아니라 관찰 데이터 [확정]

증거 데이터에는 다음 두 가지를 반드시 구분해 기록한다.

```text
증거가 직접 관찰한 것
증거를 보고 플레이어가 추론하는 것
```

예시:

```text
직접 관찰:
- 22:03에 특정 카드가 특정 장소에서 사용됨

직접 관찰하지 않음:
- 카드 소유자가 직접 사용했는가
- 결제 전후 어디에 있었는가
- 누구와 함께 있었는가
```

이 구분이 v1.0의 핵심 교정 사항(결제 내역만으로는 알리바이를 깰 수 없다)을 **데이터 구조 자체로 보장**한다. 기획자가 실수로 “결제 기록이 위치를 증명한다”고 쓰려 해도, `observations`에 위치 관찰이 없으므로 `ProofEvaluator`가 자동으로 불충분 판정을 낸다.

### 11.2 Evidence 구조

```json
{
  "evidence_id": "evidence_001",
  "type": "TRANSACTION_RECORD",
  "title_key": "loc.case001.evidence_001.title",
  "source_id": "source_merchant_system",
  "independence_group": "payment_network_A",
  "derived_from": [],
  "acquisition": {
    "node_ids": ["node_investigate_02"],
    "guaranteed": true
  },
  "integrity": {
    "state": "INTACT",
    "tamper_probability": 0.02
  },
  "observations": [
    {
      "observation_id": "obs_001",
      "subject": "entity_object_card_01",
      "predicate": "USED_AT",
      "object": "entity_location_02",
      "time": { "from": "22:03", "to": "22:03", "error_seconds": 30 },
      "scopes": ["TIME", "LOCATION", "OWNERSHIP"],
      "confidence": 0.99
    }
  ],
  "not_proven": [
    "actual_user",
    "travel_before_use",
    "travel_after_use"
  ],
  "alternative_hypotheses": [
    "hypothesis_other_user",
    "hypothesis_later_travel"
  ],
  "forensics": {
    "available": true,
    "reveals": ["integrity.tamper_probability", "observations[0].confidence"]
  }
}
```

### 11.3 필드 정의

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `evidence_id` | string | 예 | 고유 ID |
| `type` | enum | 예 | 증거 유형 (11.4) |
| `title_key` | string | 예 | 표시명 현지화 키 |
| `source_id` | string | 예 | 정보 출처 식별자 |
| `independence_group` | string | 예 | 독립성 판정 그룹 |
| `derived_from` | string[] | 아니오 | 다른 증거에서 파생된 경우 원본 ID |
| `acquisition` | object | 예 | 획득 가능한 조사 노드 |
| `integrity` | object | 예 | 무결성 상태와 위조 가능성 |
| `observations` | Observation[] | 예 | 직접 관찰한 원자 사실 |
| `not_proven` | string[] | 예 | 이 증거가 입증하지 **못하는** 항목 (UI 공개) |
| `alternative_hypotheses` | string[] | 예 | 남아 있는 대안 가설 ID |
| `forensics` | object | 아니오 | 감식으로 추가 공개되는 정보 |

`strength`(약한 단서/보강/직접/복합) 필드가 없다는 점에 주목한다. **강도는 입력이 아니라 계산 결과다.** 11.7 참조.

### 11.4 증거 유형

| type | 예시 |
|---|---|
| `TRANSACTION_RECORD` | 결제·송금·거래 기록 |
| `SURVEILLANCE` | 영상·마력 감지·출입 기록 |
| `TESTIMONY` | 목격자·관계자 진술 |
| `DOCUMENT` | 서류·장부·계약서·일지 |
| `PHYSICAL_TRACE` | 물리적 흔적·잔류물 |
| `SYSTEM_LOG` | 기계·설비 자동 기록 |
| `COMMUNICATION` | 통신·전언 기록 |
| `SCHEDULE_RECORD` | 근무표·운행표·영업 시간 |

`type`은 표시 아이콘과 감식 가능 여부의 기본값을 결정할 뿐, 판정에는 직접 관여하지 않는다.

### 11.5 Observation — 원자 관찰

```json
{
  "observation_id": "obs_001",
  "subject": "entity_id",
  "predicate": "OBSERVED_PREDICATE",
  "object": "entity_id",
  "time": { "from": "22:01", "to": "22:07", "error_seconds": 15 },
  "scopes": ["IDENTITY", "TIME", "LOCATION"],
  "confidence": 0.95
}
```

| 필드 | 설명 |
|---|---|
| `subject` / `predicate` / `object` | 관찰의 삼항 구조 |
| `time` | 관찰 시각 구간과 오차 |
| `scopes` | 이 관찰이 실제로 다루는 입증 범위 |
| `confidence` | 관찰 신뢰도 0.0~1.0 |

**관찰 predicate 목록**

| predicate | 의미 |
|---|---|
| `PRESENT_AT` | 대상이 해당 장소에 존재했음 |
| `USED_AT` | 물건이 해당 장소에서 사용됨 |
| `IDENTIFIED_AS` | 인물이 특정 인물로 식별됨 |
| `PERFORMED` | 행동이 수행됨 |
| `OWNED_BY` | 소유 관계가 확인됨 |
| `RECORDED_AT_TIME` | 기록 시각이 확인됨 |
| `OPERATIONAL_STATE` | 시설·설비의 운영 상태 |
| `TRAVEL_TIME_MIN` | 두 장소 간 최소 이동 시간 |
| `COMMUNICATED_WITH` | 통신이 발생함 |
| `SIGNED_AT_TIME` | 서명·승인 시각 |

### 11.6 ProofScope 열거형

`scopes`는 판정의 핵심 어휘다. `ProofRule.required_scopes`와 대조된다.

| Scope | 의미 |
|---|---|
| `IDENTITY` | 누구인지 특정됨 |
| `TIME` | 언제인지 특정됨 |
| `LOCATION` | 어디인지 특정됨 |
| `PRESENCE` | 동석·부재 여부 |
| `OWNERSHIP` | 소유·소지 관계 |
| `ACTION` | 행동의 발생 |
| `SEQUENCE` | 사건 간 선후 관계 |
| `ROUTE` | 이동 경로·가능성 |
| `INSTRUCTION` | 지시·명령 관계 |
| `MOTIVE` | 동기 |
| `INTEGRITY` | 기록의 무결성 |

### 11.7 증거 강도의 파생 [확정]

v1.0의 4등급(약한 단서·보강 단서·직접 증거·복합 증거)은 **저장하지 않고 계산한다.** 저장하면 기획자가 실수로 관찰 내용과 불일치하는 등급을 매길 수 있고, 그 순간 UI가 정답을 누설한다.

```text
strength(evidence, target_claim) =
    covered = evidence.scopes ∩ claim.required_scopes
    conf    = min(observation.confidence for relevant observations)

    if covered == ∅                             → NONE
    if covered ⊊ required and conf < 0.8        → LEAD
    if covered ⊊ required                       → CORROBORATING
    if covered ⊇ required and conf ≥ 0.9        → DIRECT
    else                                        → CORROBORATING

COMPOSITE는 단일 증거의 등급이 아니라 증거 집합의 성질이다.
    독립 출처 2개 이상 && 결합 scopes ⊇ required → COMPOSITE
```

중요한 점: **강도는 대상 주장에 따라 달라진다.** 같은 CCTV라도 위치 주장에는 `DIRECT`, 동기 주장에는 `NONE`이다. 이 상대성을 저장 필드로는 표현할 수 없다.

### 11.8 증거 독립성 [확정]

증거 두 장을 제출했다고 무조건 복합 증거가 되는 것은 아니다. 다음은 독립 증거가 아니다.

- 같은 보고서를 복사한 두 문서
- 같은 목격자가 두 번 한 진술
- 감시 기록과 그 기록만 보고 말한 경비원 진술
- 동일 시스템에서 생성한 중복 로그

따라서 세 필드로 독립성을 판정한다.

```text
source_id            정보의 원천. 같으면 완전 종속
independence_group   물리적·조직적 출처 그룹. 같으면 부분 종속
derived_from         파생 관계. 조상이 겹치면 종속
```

**독립 출처 수 산출 알고리즘**

```text
function countIndependentSources(evidence_set):
    groups = {}
    for e in evidence_set:
        root = resolveRoot(e)          // derived_from 체인의 최상위
        key  = root.independence_group
        groups[key] = groups[key] ∪ { root.source_id }

    // 같은 independence_group 안의 서로 다른 source는 부분 독립
    count = 0
    for key, sources in groups:
        if |sources| == 1: count += 1
        else:              count += 1 + (|sources| - 1) * PARTIAL_WEIGHT
    return floor(count)

PARTIAL_WEIGHT = 0.5   [밸런스 초안]
```

### 11.9 무결성과 손상

```json
{
  "integrity": {
    "state": "INTACT",
    "tamper_probability": 0.02
  }
}
```

| state | 의미 | 판정 영향 |
|---|---|---|
| `INTACT` | 정상 | 없음 |
| `DEGRADED` | 일부 손상 | 관련 관찰의 `confidence`에 감쇠 계수 적용 |
| `DISPUTED` | 진위 다툼 중 | `INTEGRITY` scope 요구 시 불충분 |
| `DESTROYED` | 소실 | 제출 불가 |

적 기믹의 `DAMAGE_EVIDENCE`는 `INTACT → DEGRADED`, `REMOVE_EVIDENCE`는 `→ DESTROYED`로 전이시킨다. 15.6의 불변 규칙에 따라, 필수 Claim의 **모든** 해결 경로를 동시에 파괴하는 것은 금지된다.

### 11.10 not_proven의 UI 노출

`not_proven`은 UI에 공개한다. 이는 정답 누설이 아니라 **공정성 장치**다. 플레이어가 “이 증거는 사용자를 특정하지 못한다”를 알 수 있어야 논리적으로 다음 행동을 선택할 수 있다.

반대로 `alternative_hypotheses`의 **제거 여부**는 공개하지 않는다. 어떤 가설이 남아 있는지는 개수만 표시하고, 어떤 증거가 그것을 제거하는지는 표시하지 않는다.

```text
UI 표시:   “이 기록이 입증하지 못하는 것: 실제 사용자, 결제 전후 이동”
UI 표시:   “남은 대안 설명: 2개”
UI 비표시: “hypothesis_other_user는 evidence_005로 제거됨”
```

---

## 12. ProofRule 명세

### 12.1 증거 ID 열거 방식의 한계

다음 방식은 MVP에서 구현이 쉽지만 확장성이 낮다.

```text
E1 + E2를 제출하면 정답
```

합리적인 다른 증거 조합이 생기면 일일이 추가해야 하기 때문이다. 플레이어가 기획자가 예상하지 못한 타당한 조합을 냈을 때 “게임 정답과 다르다”는 이유로 실패 판정을 내리게 되며, 이는 v1.0의 공정성 원칙 위반이다.

### 12.2 혼합형 Proof Rule [확정]

```text
Proof Rule
= 의미 기반 조건        (플레이어의 창의적 조합을 허용)
+ 보장된 증거 조합       (기획자가 검증한 확실한 경로)
```

의미 기반 조건이 1차 판정을 담당하고, 보장 조합은 “이 조합은 무슨 일이 있어도 성공한다”는 안전망 역할을 한다. 두 경로 중 하나만 통과해도 충족이다.

### 12.3 ProofRule 구조

```json
{
  "rule_id": "proof_claim_001_contradict",
  "target_claim_id": "claim_001",
  "direction": "CONTRADICT",
  "requirements": {
    "required_scopes": ["IDENTITY", "TIME", "LOCATION"],
    "minimum_confidence": 0.9,
    "minimum_independent_sources": 1,
    "require_integrity": true,
    "eliminate_hypotheses": [
      "hypothesis_identity_mismatch",
      "hypothesis_time_margin"
    ]
  },
  "guaranteed_evidence_sets": [
    ["evidence_direct_01"],
    ["evidence_02", "evidence_03"]
  ],
  "known_insufficient_sets": [
    ["evidence_02"]
  ],
  "partial_credit": {
    "scopes_covered_ratio": 0.5,
    "result": "SUSPECTED"
  }
}
```

### 12.4 필드 정의

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `rule_id` | string | 예 | 고유 ID |
| `target_claim_id` | string | 예 | 대상 주장 |
| `direction` | enum | 예 | `SUPPORT` / `CONTRADICT` |
| `requirements.required_scopes` | ProofScope[] | 예 | 반드시 다뤄야 하는 입증 범위 |
| `requirements.minimum_confidence` | float | 예 | 관련 관찰의 최소 신뢰도 |
| `requirements.minimum_independent_sources` | int | 예 | 필요한 독립 출처 수 |
| `requirements.require_integrity` | bool | 아니오 | 무결성 손상 증거 배제 여부 |
| `requirements.eliminate_hypotheses` | string[] | 아니오 | 제거해야 하는 대안 가설 |
| `guaranteed_evidence_sets` | string[][] | 아니오 | 검증된 성공 조합 |
| `known_insufficient_sets` | string[][] | 아니오 | 명시적으로 불충분한 조합 (튜토리얼 유도용) |
| `partial_credit` | object | 아니오 | 부분 충족 시 결과 |

### 12.5 required_scopes의 기본값 생성

기획자가 매 Claim마다 `required_scopes`를 직접 쓰는 것은 실수를 유발한다. 로더는 `predicate`에서 기본값을 자동 생성하고, 명시된 값이 있으면 그것을 우선한다(9.7의 매핑표 사용).

```text
claim.predicate = LOCATED_AT
→ 기본 required_scopes = [IDENTITY, TIME, LOCATION]

기획자가 "이 사건에서는 동석 여부까지 필요"라고 판단하면
→ required_scopes = [IDENTITY, TIME, LOCATION, PRESENCE] 로 덮어씀
```

### 12.6 Proof 평가 알고리즘

```text
function evaluateProof(claim, evidence_set, direction, state):

    rules = proofGraph.getRules(claim.id, direction)
    if rules is empty:
        return Sufficiency.INSUFFICIENT      // 해결 경로 미정의 = 콘텐츠 오류

    best = Sufficiency.INSUFFICIENT

    for rule in rules:

        // ── 경로 1: 보장된 조합
        for set in rule.guaranteed_evidence_sets:
            if evidence_set ⊇ set and allIntact(set, rule):
                return Sufficiency.SUFFICIENT

        // ── 경로 1b: 명시적 불충분 조합
        if evidence_set ∈ rule.known_insufficient_sets:
            best = max(best, Sufficiency.PROVISIONAL_FLOOR)
            continue

        // ── 경로 2: 의미 기반 조건
        relevant   = filterRelevant(evidence_set, claim)
        covered    = union(o.scopes for o in relevant.observations)
        confidence = min(o.confidence for o in relevant.observations)
        sources    = countIndependentSources(relevant)
        remaining  = claim.hypotheses − eliminatedBy(relevant)

        scopeOK   = covered ⊇ rule.requirements.required_scopes
        confOK    = confidence ≥ rule.requirements.minimum_confidence
        sourceOK  = sources   ≥ rule.requirements.minimum_independent_sources
        hypoOK    = remaining ∩ rule.requirements.eliminate_hypotheses == ∅
        integrOK  = not rule.requirements.require_integrity
                    or allIntact(relevant)

        if scopeOK and confOK and sourceOK and hypoOK and integrOK:
            return Sufficiency.SUFFICIENT

        // ── 경로 3: 부분 충족
        ratio = |covered ∩ required| / |required|
        if ratio ≥ rule.partial_credit.scopes_covered_ratio:
            best = max(best, Sufficiency.PROVISIONAL)

    return best
```

### 12.7 MVP와 확장 버전의 구현 차이

| 구분 | [MVP 축소] | [후속 확장] |
|---|---|---|
| 관찰 비교 | scope 집합 비교만 | 원자 관찰값의 의미 비교 |
| 시간 판정 | 구간 겹침 검사 | 이동 시간 고려한 물리적 가능성 검사 |
| 장소 판정 | 동일 ID 비교 | 장소 계층 구조(건물·층·구역) 포함 검사 |
| 소유·사용 | 명시 관찰만 | 소유→사용 추론 체인 |
| 대안 가설 | 명시 목록 대조 | 자동 가설 생성·제거 |
| 조합 탐색 | 제출된 집합만 평가 | 가능한 모든 조합의 최적 경로 탐색 |

**해커톤 단계에서 자연어 논리 추론 엔진까지 구현할 필요는 없다.** MVP 열에 적힌 수준으로도 v1.0의 모든 판정 사례를 재현할 수 있다.

### 12.8 시간 충돌 판정 [MVP 축소]

`LOCATED_AT` 계열 Claim의 반박에서 가장 자주 쓰이는 판정이다.

```text
function timeConflicts(claim_time, observation_time, travel_min_seconds = 0):
    a_start = claim_time.from  − claim_time.error_seconds
    a_end   = claim_time.to    + claim_time.error_seconds
    b_start = observation_time.from − observation_time.error_seconds
    b_end   = observation_time.to   + observation_time.error_seconds

    if overlaps(a_start..a_end, b_start..b_end):
        return true        // 동시에 두 곳에 있을 수 없음

    gap = distanceBetween(a, b)
    if gap < travel_min_seconds:
        return true        // 이동이 물리적으로 불가능

    return false
```

`travel_min_seconds`는 `TRAVEL_TIME_MIN` 관찰을 가진 증거를 함께 제출했을 때만 0이 아닌 값이 된다. 이것이 v1.0에서 “두 장소 사이 이동 시간이 최소 40분”이라는 기록이 별도 증거로 필요한 이유의 구현 근거다.

---

## 13. Action 시스템 명세

### 13.1 카드별 클래스 금지 원칙 [확정]

**잘못된 구현**

```text
유도신문카드 클래스
재질문카드 클래스
모순지적카드 클래스
동기추궁카드 클래스
과거행적카드 클래스
...
```

이렇게 구현하면 카드가 늘어날 때마다 코드가 늘어난다.

**올바른 구현**

```text
카드
= 공통 Intent
+ 대상 조건
+ 비용
+ 효과 보정
+ 성공·실패 후속 효과
```

### 13.2 ActionIntent 열거형 [확정]

| Intent | 목적 | 근거 필요 | 주 대상 |
|---|---|---|---|
| `QUERY` | 미공개 정보를 질문 | 불필요 | `QuestionRoute` |
| `CLARIFY` | 복합·모호·교란 주장을 분해 | 불필요 | 복합·교란 `ClaimToken` |
| `COMMIT` | 발언을 명확하게 고정 | 불필요 | `ASSERTED` Claim |
| `CONFIRM` | 근거로 사실을 지지 | 1개 이상 권장 | 공개된 Claim |
| `CONTRADICT` | 근거로 주장을 반박 | **1개 이상 필수** | 공개된 Claim |
| `CROSS_CHECK` | 이전 진술·다른 용의자 진술 대조 | 진술 참조 필수 | Claim + 진술 이력 |
| `FORENSIC` | 증거의 품질·범위 감식 | 불필요 | `Evidence` |
| `PRESSURE` | 평정심 조절 | 불필요 | Claim 또는 전체 |
| `RECOVER` | 스트레스·강압·CP 관리 | 불필요 | 자기 자신 |
| `SPECIAL` | 사건별 특수 행동 | 정의에 따름 | 정의에 따름 |

### 13.3 Intent별 상세 계약

#### QUERY

```text
입력:   target = route_id
검사:   1. route가 존재하는가
        2. preconditions 충족인가
        3. intent가 route.allowed_intents에 포함되는가
        4. single_use이고 이미 사용됐는가
결과:   성공 → reveals의 Claim을 VISIBLE로, unlocks_routes 개방
        실패 → R_QUERY_BLOCKED
상태:   CommitmentState: UNSTATED → route.commitment_level
```

#### CLARIFY

```text
입력:   target = token_id
검사:   1. 토큰이 참조하는 claim_id가 2개 이상인가 (COMPOUND)
        2. 또는 PresentationState가 DISTORTED/DUPLICATED인가
결과:   COMPOUND  → 참조 claim 수만큼 토큰 분리
        DISTORTED → PresentationState를 NORMAL로 복구
        해당 없음 → R_CLARIFY_NOOP (CP 일부 환급)
상태:   PresentationState만 변경. 다른 두 축은 불변
```

#### COMMIT

```text
입력:   target = claim_id
검사:   1. CommitmentState가 ASSERTED인가
        2. 저항도에 따른 추가 비용 충족인가
결과:   성공 → CommitmentState = COMMITTED
        실패 → R_COMMIT_REFUSED, 저항도 1 감소
상태:   EpistemicState는 변경하지 않는다 (6.4의 핵심)
```

#### CONFIRM

```text
입력:   target = claim_id, supports = evidence[] | statement[]
검사:   ProofEvaluator(direction = SUPPORT)
결과:   SUFFICIENT   → EpistemicState = SUPPORTED  (확인 잠금)
        PROVISIONAL  → EpistemicState = PROVISIONAL
        충돌 감지     → R_CONFIRM_CONFLICT (모순 경로 힌트만 제공)
        INSUFFICIENT → 후속 질문 경로 개방
```

#### CONTRADICT

```text
입력:   target = claim_id, supports = evidence[] (1개 이상 필수)
검사:   Relevance → Relation → Sufficiency → Independence → Hypothesis
결과:   14.4의 조합표를 따름
```

#### CROSS_CHECK

```text
입력:   target = claim_id, cited_statements = statement_id[]
검사:   1. 인용 진술이 이력에 존재하는가
        2. 인용 진술의 claim과 대상 claim이 의미적으로 충돌하는가
결과:   같은 화자 + 충돌       → R_SELF_CONTRADICTION
        다른 화자 + 충돌       → R_CROSS_CONTRADICTION
        충돌 없음              → R_INSUFFICIENT_GROUNDS
전제:   자체 모순은 인용 진술이 COMMITTED일 때만 최대 효과
```

#### FORENSIC

```text
입력:   target = evidence_id
결과:   evidence.forensics.reveals에 나열된 필드를 UI에 공개
        (신뢰도, 위조 가능성, 관찰 범위 상세)
제약:   truth_relation이나 정답 조합은 절대 공개하지 않음
```

#### PRESSURE

```text
입력:   target = claim_id 또는 null
결과:   평정심 감소, 강압 수사 증가
제약:   어떤 Objective도 달성시키지 않는다 [확정]
        EpistemicState를 변경하지 않는다
```

#### RECOVER

```text
입력:   target = self
결과:   스트레스 회복 / 강압 감소 / CP 회복 / 드로우
```

#### SPECIAL

```text
사건 전용 행동. CardDefinition에 special_effect_id로 지정하고
ModifierRepository에 등록된 Effect 조합을 실행한다.
자유 스크립트는 허용하지 않는다.
```

### 13.4 CardDefinition 구조

```json
{
  "card_id": "card_query_motive",
  "intent": "QUERY",
  "name_key": "loc.card.query_motive.name",
  "cost": { "cp": 1 },
  "target_filter": {
    "allowed_facets": ["MOTIVE"],
    "allowed_commitment": ["UNSTATED"],
    "allowed_epistemic": ["UNKNOWN"],
    "allowed_presentation": ["NORMAL", "HIDDEN"],
    "requires_route": true
  },
  "support_slots": 0,
  "modifiers": {
    "reveal_bonus": 1,
    "coercion_delta": 0,
    "on_success": [{ "type": "DRAW_CARD", "amount": 1 }],
    "on_failure": [{ "type": "REFUND_CP", "amount": 1 }]
  },
  "tags": ["QUESTION", "WHY"],
  "rarity": "COMMON",
  "upgrade_slots": { "stamp": null, "postit": null }
}
```

### 13.5 필드 정의

| 필드 | 타입 | 설명 |
|---|---|---|
| `card_id` | string | 고유 ID |
| `intent` | ActionIntent | 이 카드가 수행하는 공통 행동 |
| `cost` | object | CP 및 기타 자원 비용 |
| `target_filter` | object | 사용 가능한 대상 조건 |
| `support_slots` | int | 함께 제출 가능한 근거 수 |
| `modifiers` | object | 판정 보정과 후속 효과 |
| `tags` | string[] | 분류·검색·시너지용 |
| `rarity` | enum | 보상 풀 구성용 |
| `upgrade_slots` | object | 도장·포스트잇 장착 슬롯 |

### 13.6 카드 이름이 달라도 Intent는 같다

카드 이름과 연출은 달라도 같은 Intent를 공유할 수 있다.

```text
“다시 정확히 말해봐”
“처음부터 천천히”
“그 부분만 다시”
```

세 카드 모두 내부적으로는 `CLARIFY`다. 차이는 다음 Modifier로 만든다.

- 비용 감소
- 드로우
- 추가 Claim 분리 수
- 교란 제거 범위
- 강압 위험
- 특정 Facet 보너스

### 13.7 v1.0 카드의 Intent 매핑

| v1.0 카드 | Intent | 주요 Modifier |
|---|---|---|
| 유도 신문 | `QUERY` | `reveal_bonus` |
| 유도 신문(강제) | `QUERY` | `force_reveal`, `coercion_delta: +6` |
| 재질문 | `CLARIFY` | `on_success: DRAW_CARD` |
| 확인 질문 | `CONFIRM` | `support_slots: 2` |
| 모순 지적 | `CONTRADICT` | `support_slots: 2` |
| 서류철 툭 던지기 | `PRESSURE` | `target_filter: resistance == 0` |
| 책상 쾅! 내리치기 | `PRESSURE` | `composure_delta: -20, coercion_delta: +10` |
| 알루미늄 배트 위협 | `PRESSURE` | `composure_delta: -30, coercion_delta: +15` |
| 동기 추궁 | `QUERY` | `target_filter.allowed_facets: [MOTIVE]`, `force_reveal` |
| 진술 대조표 | `CROSS_CHECK` | `cross_check_bonus` |
| 결정적 물증 제시 | — | `CardModifier` (다음 `CONTRADICT`에 `support_slots +1`) |
| 고블린의 목격담 | `CONTRADICT` 보조 | 독립 출처로 등록되는 `Evidence` 지급 |
| 압수수색 영장 | `SPECIAL` | `Effect: UNLOCK_EVIDENCE_SLOT` |
| 믹스커피 타임 | `RECOVER` | `stress +5, next_turn_cp +1` |
| 바람 쐬고 오기 | `RECOVER` | `coercion -15` |
| 과거 행적 들추기 | `CROSS_CHECK` | 이력에서 동일 Facet 2개 소환 |

**주목할 점**: v1.0의 16종 카드가 6종 Intent로 수렴한다. 카드 100장을 추가해도 `ArgumentResolver`는 변하지 않는다.

### 13.8 CardModifier — 성장 시스템의 일반화

v1.0의 3단계 성장(결재 도장 → 포스트잇 → 카드 병합)은 모두 `CardModifier`의 적용으로 통일한다.

```json
{
  "modifier_id": "stamp_blue",
  "layer": "STAMP",
  "applies_to_intents": ["QUERY", "CLARIFY", "CONFIRM", "CONTRADICT"],
  "effects": {
    "on_success": [{ "type": "DRAW_CARD", "amount": 1 }],
    "coercion_delta": -2,
    "composure_multiplier": 1.0
  }
}
```

| layer | v1.0 대응 | 적용 규칙 |
|---|---|---|
| `STAMP` | 결재 도장 | 카드당 최대 1개. 교체 가능 |
| `POSTIT` | 형광 포스트잇 | 카드당 최대 1개. 탈착 자유 |
| `MERGE` | 서류 클립 병합 | 병합 시 재료 카드의 STAMP·POSTIT 상속 |
| `RELIC` | 유물 | 전역 적용 |
| `PARTNER` | 파트너 스킬 | 일시 적용 |
| `STATUS` | 상태 효과 | 일시 적용 |

**Modifier 적용 순서 [확정]**

```text
1. 기본 CardDefinition.modifiers
2. STAMP
3. POSTIT
4. MERGE 상속분
5. RELIC
6. PARTNER
7. STATUS (적 기믹 포함)
```

동일 필드에 대해 가산 값은 합산하고, 배율은 곱산하며, 불리언은 논리합으로 처리한다.

**불변 규칙 [확정]**

어떤 Modifier도 다음을 우회할 수 없다.

- `ProofRule`의 `required_scopes`
- `minimum_independent_sources`
- `truth_relation`
- `Relation` 판정(지지/충돌)

Modifier가 바꿀 수 있는 것은 비용, 슬롯 수, 드로우, 자원 변화량, 대상 필터의 완화, 부가 효과뿐이다. 즉 **논리를 우회하는 성장은 존재하지 않는다.**

### 13.9 덱 RNG 대책 [확정]

논리 퍼즐을 해결하는 데 반드시 필요한 행동이 카드 드로우 운에 따라 장시간 나오지 않으면 불공정해진다. 다음 세 방식 중 하나를 적용한다.

| 방식 | 내용 | 장점 | 단점 |
|---|---|---|---|
| **A. 필수 Intent 보장** | 질문·명확화·확인·반박 카드가 덱에 최소 수량 존재. 모두 사용되면 빠르게 재순환. 영구 소각 불가 | 덱빌딩 정체성 유지 | 덱 압축 전략과 충돌 |
| **B. 기본 행동 + 카드 보정** | `QUERY`·`CONFIRM`·`CONTRADICT`는 항상 기본 행동으로 사용 가능. 카드는 할인·추가 슬롯·추가 효과만 부여 | 완전한 공정성 | 카드의 존재감 약화 |
| **C. 사건별 필수 카드 임시 지급** | 사건 시작 시 필요한 기본 카드를 임시 덱에 추가. 종료 후 제거 | 사건별 튜닝 용이 | 임시 카드 관리 복잡 |

**해커톤 MVP 채택: 방식 A + 부분 B**

- 초기 덱에 4대 Intent 카드를 각 2장씩 보장 (v1.0의 P0 초기 덱 구조 유지)
- 손패가 비고 덱에 해당 Intent가 없으면 기본 행동(비용 +1, 보정 없음)을 항상 허용
- 4대 Intent 카드는 소각 대상에서 제외

이 조합이면 “카드 운으로 사건을 풀 수 없는 상황”이 원천 차단되면서도 덱빌딩의 의미가 남는다.
## 14. Argument Resolver 명세

### 14.1 ResolutionInput

```text
ResolutionInput
├─ intent                   ActionIntent
├─ target                   claim_id | route_id | token_id | evidence_id | null
├─ evidence_set             evidence_id[]
├─ cited_statements         statement_id[]
├─ card_modifiers           병합된 Modifier 집합
├─ current_knowledge_state  KnowledgeState 스냅샷
├─ encounter_state          페이즈·턴·활성 Modifier
└─ resource_state           CP·평정심·강압·스트레스
```

`ArgumentResolver`는 이 입력만 받는다. `CaseDefinition` 전체나 사건 ID는 받지 않는다. 필요한 사건 데이터는 `KnowledgeState`가 보유한 참조를 통해서만 조회한다.

### 14.2 판정 순서 [확정]

```text
1. 행동과 대상이 호환되는가?               → Validity
2. 대상이 현재 공개되어 있는가?             → Validity
3. 제출 근거가 대상 주장과 관련 있는가?      → Relevance
4. 근거가 주장을 지지하는가, 충돌하는가?     → Relation
5. 필요한 입증 범위를 충족하는가?           → Sufficiency
6. 증거 신뢰도가 기준 이상인가?             → Sufficiency
7. 복합 증거가 서로 독립적인가?             → Independence
8. 남아 있는 대안 가설을 제거했는가?         → AlternativeElimination
9. 행동이 절차적으로 적절한가?              → Procedure
10. 판정 결과와 후속 상태를 생성한다.        → ResolutionCode + Effects
```

**단축 평가 규칙**: 1~2단계에서 `INVALID`가 나오면 즉시 종료하고 CP를 소비하지 않는다. 3단계에서 `NONE`이 나오면 4~8단계를 건너뛰고 `R_IRRELEVANT_EVIDENCE`로 확정한다.

### 14.3 판정 축

판정은 결과명을 직접 검사하지 않고 축을 조합해 도출한다.

```text
Validity                    행동 자체가 성립하는가
├─ VALID
└─ INVALID

Relevance                   근거가 대상과 관계있는가
├─ NONE                     대상 Claim의 required_scopes와 교집합 없음
├─ PARTIAL                  일부 scope만 겹침
└─ FULL                     required_scopes를 모두 다룸

Relation                    근거와 주장의 방향
├─ SUPPORTS                 관찰이 주장과 정합
├─ CONTRADICTS              관찰이 주장과 충돌
├─ NEUTRAL                  방향 없음
└─ AMBIGUOUS                해석에 따라 양쪽 가능

Sufficiency                 증명 수준
├─ INSUFFICIENT
├─ PROVISIONAL
└─ SUFFICIENT

Independence                독립 출처
├─ MET                      minimum_independent_sources 충족
└─ UNMET

AlternativeElimination      대안 가설
├─ CLEARED                  요구된 가설이 모두 제거됨
└─ REMAINING                남아 있음

Procedure                   수사 절차
├─ FAIR
├─ COERCIVE                 강압 위험 있음
└─ FORBIDDEN                금지된 절차 (자동 실패 + 큰 페널티)
```

### 14.4 축 조합 → ResolutionCode 결정표 [확정]

`—`는 “해당 없음 또는 무관”을 뜻한다.

| Intent | 관련성 | 관계 | 충분성 | 독립 | 가설 | 판정 코드 | v1.0 대응 |
|---|---|---|---|---|---|---|---|
| `CONTRADICT` | FULL | CONTRADICTS | SUFFICIENT | MET | CLEARED | `R_DIRECT_CONTRADICTION` | 직접 모순 |
| `CONTRADICT` | FULL | CONTRADICTS | SUFFICIENT | UNMET | — | `R_INDIRECT_SUSPICION` | 간접 의심 |
| `CONTRADICT` | FULL | CONTRADICTS | SUFFICIENT | MET | REMAINING | `R_INDIRECT_SUSPICION` | 간접 의심 |
| `CONTRADICT` | FULL | CONTRADICTS | PROVISIONAL | — | — | `R_INDIRECT_SUSPICION` | 간접 의심 |
| `CONTRADICT` | PARTIAL | CONTRADICTS | INSUFFICIENT | — | — | `R_INSUFFICIENT_GROUNDS` | 합리적 증거 불충분 |
| `CONTRADICT` | PARTIAL | AMBIGUOUS | INSUFFICIENT | — | — | `R_INSUFFICIENT_GROUNDS` | 합리적 증거 불충분 |
| `CONTRADICT` | FULL | SUPPORTS | SUFFICIENT | — | — | `R_TRUTH_ATTACKED` | 진실에 모순 지적 |
| `CONTRADICT` | FULL | SUPPORTS | INSUFFICIENT | — | — | `R_INSUFFICIENT_GROUNDS` | 근거 부족, 피해 없음 |
| `CONTRADICT` | NONE | NEUTRAL | — | — | — | `R_IRRELEVANT_EVIDENCE` | 무관한 증거 |
| `CONFIRM` | FULL | SUPPORTS | SUFFICIENT | MET | — | `R_CONFIRM_LOCKED` | 확인 성공 |
| `CONFIRM` | FULL | SUPPORTS | PROVISIONAL | — | — | `R_CONFIRM_PROVISIONAL` | 임시 확인 |
| `CONFIRM` | FULL | CONTRADICTS | SUFFICIENT | — | — | `R_CONFIRM_CONFLICT` | 확인 실패, 모순 가능성 힌트 |
| `CONFIRM` | PARTIAL | — | INSUFFICIENT | — | — | `R_CONFIRM_INSUFFICIENT` | 확인 불충분 |
| `CONFIRM` | NONE | — | — | — | — | `R_IRRELEVANT_EVIDENCE` | 무관 |
| `QUERY` | 경로 유효 | — | — | — | — | `R_QUERY_SUCCESS` | 미진술 질문 성공 |
| `QUERY` | 경로 없음 | — | — | — | — | `R_QUERY_BLOCKED` | 사건에 없는 질문 |
| `CLARIFY` | 복합/교란 | — | — | — | — | `R_CLARIFY_SUCCESS` | 복합 토큰 분해 |
| `CLARIFY` | 해당 없음 | — | — | — | — | `R_CLARIFY_NOOP` | — |
| `COMMIT` | 대상 ASSERTED | — | — | — | — | `R_COMMIT_SUCCESS` | 발언 약속 생성 |
| `COMMIT` | 저항 초과 | — | — | — | — | `R_COMMIT_REFUSED` | — |
| `CROSS_CHECK` | 동일 화자 | CONTRADICTS | SUFFICIENT | — | — | `R_SELF_CONTRADICTION` | 자체 모순 |
| `CROSS_CHECK` | 타 화자 | CONTRADICTS | SUFFICIENT | — | — | `R_CROSS_CONTRADICTION` | 교차 모순 |
| `CROSS_CHECK` | — | NEUTRAL | — | — | — | `R_INSUFFICIENT_GROUNDS` | — |
| `FORENSIC` | 대상 유효 | — | — | — | — | `R_FORENSIC_REVEALED` | 감식 결과 공개 |
| `PRESSURE` | 대상 유효 | — | — | — | — | `R_PRESSURE_APPLIED` | 근거 없는 압박 |
| `PRESSURE` | 대상이 고저항 진실 | — | — | — | — | `R_PRESSURE_BACKFIRE` | 강압 위험 증가 |
| `RECOVER` | — | — | — | — | — | `R_RECOVER_APPLIED` | 정비·회복 |
| 모든 Intent | Validity = INVALID | — | — | — | — | `R_ACTION_INVALID` | — |
| 모든 Intent | Procedure = FORBIDDEN | — | — | — | — | `R_PROCEDURE_VIOLATION` | — |

**이 표의 성질**: 새 판정 결과를 추가해도 전체 코드를 다시 만들 필요가 없다. 축의 조합에 행을 추가할 뿐이다. 표 자체를 데이터 파일로 외부화하면 밸런스 조정도 코드 변경 없이 가능하다. [후속 확장]

### 14.5 ResolutionCode별 효과

수치는 [밸런스 초안]이며, 관계는 [확정]이다.

| 판정 코드 | 평정심 | 강압 | 발언 상태 | 검증 상태 | 추가 결과 |
|---|---:|---:|---|---|---|
| `R_DIRECT_CONTRADICTION` | -20~-30 | 0~+2% | 유지 | → `REFUTED` | 저항도 파괴, 후속 Claim 공개, 페이즈 전이 검사 |
| `R_INDIRECT_SUSPICION` | -3~-8 | 0~+2% | 유지 | → `SUSPECTED` | 후속 질문 경로 개방 |
| `R_INSUFFICIENT_GROUNDS` | 0 | 0~+3% | 유지 | 유지 | 카드 일부 환급, 부족한 scope 표시 |
| `R_TRUTH_ATTACKED` | 0 | +10~+15% | 유지 | → `PROVISIONAL` | 저항도 +1, 반발 대사 |
| `R_IRRELEVANT_EVIDENCE` | 0 | +5~+10% | 유지 | 유지 | 신뢰 상실 1스택 |
| `R_CONFIRM_LOCKED` | 0~-3 | 0 | 유지 | → `SUPPORTED` | 후보군 축소 |
| `R_CONFIRM_PROVISIONAL` | 0 | 0 | 유지 | → `PROVISIONAL` | 추가 질문 경로 |
| `R_CONFIRM_CONFLICT` | 0~-3 | 0 | 유지 | → `SUSPECTED` | 모순 경로 존재 힌트만 표시 |
| `R_CONFIRM_INSUFFICIENT` | 0 | 0 | 유지 | 유지 | 부족한 scope 표시 |
| `R_QUERY_SUCCESS` | 0~-5 | 0~+2% | → route 지정값 | `UNKNOWN` | 새 Claim 공개 |
| `R_QUERY_BLOCKED` | 0 | 0 | 유지 | 유지 | CP 환급, 범위 밖 반응 |
| `R_CLARIFY_SUCCESS` | 0 | 0 | 유지 | 유지 | 토큰 분리 또는 교란 복구, 드로우 |
| `R_CLARIFY_NOOP` | 0 | 0 | 유지 | 유지 | CP 일부 환급 |
| `R_COMMIT_SUCCESS` | 0~-3 | 0~+2% | → `COMMITTED` | 유지 | 이후 말 바꾸기 추적 활성 |
| `R_COMMIT_REFUSED` | 0 | +2~+5% | 유지 | 유지 | 저항도 -1 |
| `R_SELF_CONTRADICTION` | -15~-25 | 0 | → `CONTRADICTED` | → `SUSPECTED` | 강한 후속 반응 |
| `R_CROSS_CONTRADICTION` | -15~-25 | 0 | → `CONTRADICTED` | → `SUSPECTED` | 다른 용의자 진술 연동 |
| `R_FORENSIC_REVEALED` | 0 | 0 | 유지 | 유지 | 증거 상세 정보 공개 |
| `R_PRESSURE_APPLIED` | 카드 수치 | 카드 수치 | 유지 | **유지** | Objective 미해결 |
| `R_PRESSURE_BACKFIRE` | 0~-3 | +10~+20% | 유지 | 유지 | 저항도 +1 |
| `R_RECOVER_APPLIED` | 0 | 카드 수치 | 유지 | 유지 | 자원 회복 |
| `R_ACTION_INVALID` | 0 | 0 | 유지 | 유지 | CP 미소비 |
| `R_PROCEDURE_VIOLATION` | 0 | +20% 이상 | 유지 | 유지 | 즉시 턴 종료 가능 |

### 14.6 COMMITTED 배율 규칙 [확정]

6.4에서 설명한 “함정 설치” 플레이를 보상하기 위해, 다음 배율을 적용한다.

```text
if target.CommitmentState == COMMITTED and result == R_DIRECT_CONTRADICTION:
    composure_delta *= COMMITTED_MULTIPLIER      // 1.4 [밸런스 초안]
    phase_transition_weight += 1

if target.CommitmentState == ASSERTED and result == R_DIRECT_CONTRADICTION:
    // 용의자가 "그건 대충 말한 것"이라며 REVISED로 회피할 수 있음
    allow_evasion = true
```

이 규칙 하나로 `COMMIT` 행동이 전략적 의미를 갖는다. `COMMIT` 없이 바로 `CONTRADICT`를 쓰면 효율이 낮다.

### 14.7 공정성 원칙 [확정]

| 원칙 | 내용 |
|---|---|
| **F-1** | 합리적인 추리를 했지만 증거가 약한 경우(`R_INSUFFICIENT_GROUNDS`)와, 완전히 무관한 증거를 낸 경우(`R_IRRELEVANT_EVIDENCE`)를 구분한다. |
| **F-2** | “게임 정답과 다르다”는 이유만으로 강압 페널티를 주지 않는다. 강압은 절차 위반과 진실 공격에서만 발생한다. |
| **F-3** | 실패 결과는 **어떤 입증 범위가 부족했는지** 설명하되 정답 증거 자체는 알려주지 않는다. |
| **F-4** | 무작위 빗나감을 사용하지 않는다. 같은 입력은 항상 같은 결과를 낸다. |
| **F-5** | 플레이어가 기획자가 예상하지 못한 타당한 조합을 제출하면 의미 기반 조건으로 인정한다. |

**F-3의 구현**

```json
{
  "resolution_code": "R_INSUFFICIENT_GROUNDS",
  "feedback": {
    "covered_scopes": ["TIME", "LOCATION"],
    "missing_scopes": ["IDENTITY"],
    "remaining_hypothesis_count": 2
  }
}
```

`missing_scopes`는 공개한다. 어떤 증거가 `IDENTITY`를 채우는지는 공개하지 않는다.

### 14.8 Argument Resolver 의사코드

```text
function resolve(input) -> Resolution:

    // ── 1~2. Validity
    validity = ActionValidator.check(input.intent, input.target, input.state)
    if validity == INVALID:
        return Resolution(R_ACTION_INVALID, refund_cp = true)

    procedure = ProcedureEvaluator.check(input)
    if procedure == FORBIDDEN:
        return Resolution(R_PROCEDURE_VIOLATION)

    // ── Intent별 분기 (Intent 수만큼만 존재. 사건별 분기 없음)
    switch input.intent:

        case QUERY:
            route = InquiryGraph.findRoute(input.target)
            if route == null or not route.preconditionsMet(input.state):
                return Resolution(R_QUERY_BLOCKED, refund_cp = true)
            return Resolution(R_QUERY_SUCCESS, reveals = route.reveals)

        case CLARIFY:
            token = input.state.getToken(input.target)
            if token.claimRefs.count >= 2:
                return Resolution(R_CLARIFY_SUCCESS, split = token.claimRefs)
            if token.presentation in [DISTORTED, DUPLICATED]:
                return Resolution(R_CLARIFY_SUCCESS, restore = token.id)
            return Resolution(R_CLARIFY_NOOP, refund_cp = partial)

        case COMMIT:
            claim = input.state.getClaim(input.target)
            if claim.commitment != ASSERTED:
                return Resolution(R_ACTION_INVALID)
            if claim.resistance > input.modifiers.commitPower:
                return Resolution(R_COMMIT_REFUSED, resistance_delta = -1)
            return Resolution(R_COMMIT_SUCCESS)

        case FORENSIC:
            return Resolution(R_FORENSIC_REVEALED,
                              reveal = evidence.forensics.reveals)

        case PRESSURE:
            claim = input.state.getClaim(input.target)
            if claim.resistance >= HIGH_RESISTANCE
               and TruthGraph.isConsistent(claim):
                return Resolution(R_PRESSURE_BACKFIRE)
            return Resolution(R_PRESSURE_APPLIED, from_card = true)

        case RECOVER:
            return Resolution(R_RECOVER_APPLIED, from_card = true)

        case CROSS_CHECK:
            return resolveCrossCheck(input)

        case CONFIRM:
        case CONTRADICT:
            return resolveEvidenceAction(input)

        case SPECIAL:
            return resolveSpecial(input)


function resolveEvidenceAction(input) -> Resolution:

    claim     = input.state.getClaim(input.target)
    evidence  = input.state.getEvidence(input.evidence_set)
    direction = (input.intent == CONFIRM) ? SUPPORT : CONTRADICT

    // ── 3. Relevance
    relevance = EvidenceRelationEvaluator.relevance(claim, evidence)
    if relevance == NONE:
        return Resolution(R_IRRELEVANT_EVIDENCE)

    // ── 4. Relation
    relation = EvidenceRelationEvaluator.relation(claim, evidence)

    // ── 5~6. Sufficiency
    sufficiency = ProofEvaluator.evaluate(claim, evidence, direction, input.state)

    // ── 7. Independence
    required  = ProofEvaluator.requiredSources(claim, direction)
    actual    = IndependenceEvaluator.count(evidence)
    independence = (actual >= required) ? MET : UNMET

    // ── 8. Alternative elimination
    remaining = HypothesisEvaluator.remaining(claim, evidence)
    elimination = (remaining is empty) ? CLEARED : REMAINING

    // ── 9~10. 조합표 조회
    code = ResolutionTable.lookup(
        input.intent, relevance, relation,
        sufficiency, independence, elimination)

    return Resolution(code,
        feedback = buildFeedback(claim, evidence, relevance, sufficiency),
        modifiers = input.card_modifiers)
```

**주목할 점**: 이 함수 전체에 사건 ID·용의자 ID·증거 ID 상수가 하나도 등장하지 않는다. 이것이 일반화의 실질적 증거다.

### 14.9 ResolutionEffectApplier

```text
function apply(resolution, state) -> StateDelta:

    delta = new StateDelta()

    // 1. 자원 변화 (Modifier 적용 후)
    delta.composure = base(resolution).composure
                      * modifiers.composure_multiplier
                      * commitmentMultiplier(resolution, state)
    delta.coercion  = base(resolution).coercion
                      + modifiers.coercion_delta
    delta.cp        = -cost + modifiers.refund

    // 2. Claim 상태 변화
    delta.claimStates = stateTransition(resolution, state)

    // 3. 저항도 변화
    delta.resistance = resistanceDelta(resolution)

    // 4. 후속 공개
    delta.reveals = resolution.reveals ∪ followUpRoutes(resolution, state)

    // 5. 카드 효과
    delta.cards = resolution.success
                  ? modifiers.on_success
                  : modifiers.on_failure

    return delta
```

적용 순서는 **자원 → 상태 → 공개 → 카드 → 기믹 → 목표 검사**로 고정한다. 순서를 바꾸면 같은 입력에 다른 결과가 나올 수 있다.

---

## 15. EncounterModifier 명세

### 15.1 트리거-조건-효과 시스템 [확정]

적마다 전용 코드를 작성하지 않고 다음 구조로 만든다.

```text
EncounterModifier
├─ trigger              언제 검사하는가
├─ condition            어떤 상태에서 발동하는가
├─ effect               무엇을 하는가
├─ counterplay          플레이어가 어떻게 대응하는가
├─ activation_limit     최대 발동 횟수
└─ priority             동시 발동 시 순서
```

### 15.2 Trigger 카탈로그

| Trigger | 발생 시점 |
|---|---|
| `ON_ENCOUNTER_START` | 심문 시작 |
| `ON_TURN_START` | 턴 시작 |
| `ON_ACTION_SELECTED` | 플레이어가 행동을 고름 (제출 전) |
| `ON_ACTION_SUBMITTED` | 행동 제출 직후, 판정 전 |
| `ON_RESOLUTION` | 판정 완료 직후 |
| `ON_CLAIM_REVEALED` | 새 Claim 공개 |
| `ON_CLAIM_CONFIRMED` | Claim이 `SUPPORTED`가 됨 |
| `ON_DIRECT_CONTRADICTION` | `R_DIRECT_CONTRADICTION` 발생 |
| `ON_COMPOSURE_THRESHOLD` | 평정심이 임계값 통과 |
| `ON_EVIDENCE_USED` | 특정 증거가 제출됨 |
| `ON_TURN_END` | 턴 종료 |

### 15.3 Effect 카탈로그

| Effect | 대상 | 설명 |
|---|---|---|
| `DISTORT_CLAIM_VIEW` | ClaimToken | 표현을 교란 (`PresentationState = DISTORTED`) |
| `DUPLICATE_CLAIM_VIEW` | ClaimToken | 유사 표현으로 복제 표시 |
| `RESTORE_CLAIM_VIEW` | ClaimToken | 표시 상태 복구 |
| `LOCK_CLAIM` | ClaimToken | 일시적으로 선택 불가 |
| `LOCK_ACTION` | ActionIntent | 특정 Intent 사용 금지 |
| `ALTER_ACTION_COST` | ActionIntent | 비용 변경 |
| `DAMAGE_EVIDENCE` | Evidence | `INTACT → DEGRADED` |
| `REMOVE_EVIDENCE` | Evidence | `→ DESTROYED` |
| `REDUCE_CP` | 자원 | CP 감소 |
| `ADD_TIMER` | 전투 | 제한 턴 부여 |
| `INCREASE_RESISTANCE` | Claim | 저항도 증가 |
| `DRAW_CARD` | 덱 | 드로우 |
| `DISCARD_CARD` | 손패 | 버리기 |
| `APPLY_STRESS` | 자원 | 스트레스 감소 |
| `APPLY_COERCION` | 자원 | 강압 수사 증가 |
| `CHANGE_PHASE` | 진행 | 페이즈 강제 전환 |
| `SPAWN_STATEMENT` | 진행 | 추가 진술 라운드 삽입 |
| `UNLOCK_EVIDENCE_SLOT` | 증거 | 숨겨진 증거 공개 |

### 15.4 EncounterModifier 구조

```json
{
  "modifier_id": "modifier_statement_distortion",
  "trigger": "ON_DIRECT_CONTRADICTION",
  "condition": {
    "type": "PHASE_IS",
    "node_id": "phase_01"
  },
  "effect": {
    "type": "DISTORT_CLAIM_VIEW",
    "target_selector": {
      "scope": "VISIBLE_CLAIMS",
      "exclude_states": { "epistemic": ["REFUTED", "SUPPORTED"] },
      "count": 2,
      "selection": "DETERMINISTIC_BY_INDEX"
    }
  },
  "counterplay": {
    "allowed_intents": ["CLARIFY", "FORENSIC"],
    "partner_skills": ["skill_record_cleanup"],
    "always_available": true
  },
  "activation_limit": 2,
  "priority": 100
}
```

### 15.5 target_selector

무작위 대상 선택은 재현성을 해치므로 기본적으로 사용하지 않는다.

| selection | 동작 |
|---|---|
| `DETERMINISTIC_BY_INDEX` | 공개 순서 기준 앞에서부터 선택 |
| `HIGHEST_RESISTANCE` | 저항도가 높은 순 |
| `MOST_RECENT` | 가장 최근 공개 순 |
| `SEEDED_RANDOM` | 런 시드 기반 결정론적 무작위 |

`exclude_states`는 반드시 지정한다. 이미 `REFUTED`·`SUPPORTED`가 된 Claim을 교란 대상에 넣으면 확정된 추리를 되돌리는 셈이므로 15.6의 불변 규칙 위반이다.

### 15.6 적 기믹의 불변 규칙 [확정]

적 기믹은 다음을 **바꿀 수 있다.**

- 표현 (`PresentationState`)
- 접근 비용
- 행동 비용
- 제한 시간
- 증거 무결성 상태
- 저항도
- 자원 (CP·스트레스·강압)
- UI 정리 난이도

적 기믹은 다음을 **바꿀 수 없다.**

- 실제 사건의 진실 (`TruthGraph`)
- Claim의 canonical 의미
- 이미 성립한 증거 관계 (`SUPPORTED`·`REFUTED`)
- 플레이어가 기록한 원문 진술 이력
- 판정 결과의 논리
- 증명 가능한 경로 **전체**

즉, 적 기믹은 **추리를 삭제하는 것이 아니라 추리 비용을 변화시켜야 한다.**

**해결 경로 보존 검사**

`REMOVE_EVIDENCE`·`LOCK_CLAIM`을 사용할 때 엔진은 다음을 실행한다.

```text
before = solvablePaths(required_claims, state)
apply(effect)
after  = solvablePaths(required_claims, state)

if after is empty and before is not empty:
    → 효과를 취소하고 대체 효과 적용 (또는 콘텐츠 오류 보고)
```

이 검사는 런타임 안전망이며, 24.2의 진행 검증이 콘텐츠 단계에서 미리 걸러야 한다.

### 15.7 v1.0 6대 기믹의 재작성

v1.0의 상위 분류별 코어 기믹을 Trigger·Effect 조합으로 표현한다. **새 코드는 필요하지 않다.**

| v1.0 기믹 | 상위 분류 | Trigger | Effect | Counterplay |
|---|---|---|---|---|
| 정체 은닉·신원 락 | 누가 | `ON_ENCOUNTER_START` | `LOCK_CLAIM`(facet=ACTOR/COMPANION) | `FORENSIC`, 신분 증거 획득 |
| 타임어택·증거 인멸 | 언제 | `ON_TURN_START` | `ADD_TIMER` + `DAMAGE_EVIDENCE` | 우선순위 판단, 조기 제출 |
| 동선 교란·주장 변형 | 어디서 | `ON_DIRECT_CONTRADICTION` | `DISTORT_CLAIM_VIEW` ×2 | `CLARIFY`, 진술 이력 대조 |
| 실시간 개입·증거 손실 | 무엇을 | `ON_ACTION_SELECTED` | `ALTER_ACTION_COST` + `REMOVE_EVIDENCE` | 사전 감식, 파트너 회수 |
| 강압 반사·방법 저항 | 어떻게 | `ON_RESOLUTION` | `APPLY_COERCION` + `INCREASE_RESISTANCE` | `PRESSURE` 사용 자제 |
| 리소스 압박·감정 소모 | 왜 | `ON_TURN_END` | `APPLY_STRESS` + `REDUCE_CP` | `RECOVER`, 턴 수 단축 |

### 15.8 전면 블라인드 셔플의 위치 [검증안]

전면 가림 후 전체 위치 셔플은 시각적 임팩트는 크지만 다음 위험이 있다.

- 증거 대조보다 슬롯 위치 암기가 중요해짐
- UI 애니메이션 추적 가능 여부가 난이도를 결정
- 접근성 저하
- 심문 기록을 읽는 핵심 재미와 충돌

v2.0 구조에서는 이를 다음 조합으로 표현할 수 있으나, **해커톤 MVP에는 넣지 않는다.**

```json
{
  "effect": {
    "type": "DISTORT_CLAIM_VIEW",
    "target_selector": { "scope": "ALL_VISIBLE", "selection": "SEEDED_RANDOM" }
  },
  "activation_limit": 1
}
```

구조적으로 표현 가능하다는 점이 중요하다. 채택 결정이 나면 데이터만 추가하면 되고 엔진 수정은 필요 없다.

---

## 16. EncounterFlow 명세

### 16.1 고정 대사 순서에서 상태 전이로

한 사건은 고정된 대사 순서가 아니라 상태 전이에 따라 진행되어야 한다.

```text
초기 진술
├─ 장소 질문 → 이동 경로 공개
├─ 소유 질문 → 소유 Claim 공개
├─ 약한 증거 제출 → 의심 단계
└─ 직접 증거 제출 → 첫 모순
                         ↓
                    방어 진술 2단계
                    ├─ 이전 발언 수정
                    ├─ 새 지시자 언급
                    └─ 적 기믹 발동
                              ↓
                         최종 주장 공개
```

### 16.2 FlowNode 구조

```json
{
  "node_id": "phase_02",
  "enter_conditions": [
    {
      "type": "CLAIM_EPISTEMIC",
      "claim_id": "claim_location_01",
      "state": "REFUTED"
    }
  ],
  "reveal_claim_ids": ["claim_route_01", "claim_orderer_01"],
  "revise_claim_ids": ["claim_sequence_01"],
  "open_route_ids": ["route_orderer_01"],
  "activate_modifiers": ["modifier_statement_distortion"],
  "deactivate_modifiers": [],
  "reaction_key": "reaction_phase_02",
  "resource_delta": { "resistance_global": 1 },
  "is_terminal": false
}
```

### 16.3 필드 정의

| 필드 | 타입 | 설명 |
|---|---|---|
| `node_id` | string | 페이즈 ID |
| `enter_conditions` | Condition[] | 진입 조건 (전부 충족해야 진입) |
| `reveal_claim_ids` | string[] | 진입 시 공개되는 Claim |
| `revise_claim_ids` | string[] | 진입 시 `REVISED`로 전이하는 Claim |
| `open_route_ids` | string[] | 진입 시 열리는 질문 경로 |
| `activate_modifiers` | string[] | 활성화되는 적 기믹 |
| `deactivate_modifiers` | string[] | 비활성화되는 적 기믹 |
| `reaction_key` | string | AI 반응 생성용 키 |
| `resource_delta` | object | 진입 시 자원 변화 |
| `is_terminal` | bool | 종료 노드 여부 |

### 16.4 전이 규칙 [확정]

| 규칙 | 내용 |
|---|---|
| **T-1** | 전이 검사는 매 `ResolutionEffectApplier` 실행 후 1회 수행한다. |
| **T-2** | 여러 노드의 조건이 동시에 충족되면 정의 순서상 앞선 노드로 전이한다. |
| **T-3** | 같은 노드로 두 번 전이하지 않는다. |
| **T-4** | 전이 시 이미 공개된 Claim을 다시 숨기지 않는다. |
| **T-5** | 모든 노드는 종료 노드에 도달 가능해야 한다 (24.2 검증 대상). |
| **T-6** | `revise_claim_ids`는 `COMMITTED` 상태의 Claim에 적용하면 `CONTRADICTED`를 발생시킨다. |

T-6이 v1.0의 “변명으로 새로운 시간 순서 등장 → 이전 진술과 교차 검증” 플레이를 자동으로 만들어 낸다. 용의자가 스스로 말을 바꾸면, 그것이 플레이어가 이미 `COMMIT`으로 묶어 둔 발언이었을 때 즉시 자체 모순이 성립한다.

### 16.5 진행 예시

```text
phase_01 (초기)
  공개: claim_actor, claim_time, claim_location, claim_action,
        claim_companion, claim_aftermath
  경로: route_motive, route_ownership

    │ enter_condition: claim_location.epistemic == REFUTED
    ▼
phase_02 (1차 방어)
  수정: claim_sequence  (순서를 바꿔 다시 진술)
  공개: claim_route
  기믹: modifier_statement_distortion 활성화
  경로: route_route_detail

    │ enter_condition: ALL_OF(
    │     claim_route.epistemic == REFUTED,
    │     route_motive.used == true)
    ▼
phase_03 (지시자 공개)
  공개: claim_orderer
  기믹: modifier_statement_distortion 비활성화
  경로: route_orderer_detail

    │ enter_condition: OBJECTIVE_DONE(all required)
    ▼
phase_end (종료)
  is_terminal: true
```

---

## 17. Objective·Outcome 명세

### 17.1 승리 조건의 데이터화

v1.0의 `필수 주장 해결 + 평정심 1~30%`는 기본 규칙으로 유지하되, 엔진에서는 고정 수치로 작성하지 않는다.

### 17.2 ObjectiveType 카탈로그

| Type | 파라미터 | 달성 조건 |
|---|---|---|
| `RESOLVE_CLAIM` | `claim_id` | `EpistemicState ∈ {SUPPORTED, REFUTED}` |
| `CONFIRM_CLAIM` | `claim_id` | `EpistemicState == SUPPORTED` |
| `REFUTE_CLAIM` | `claim_id` | `EpistemicState == REFUTED` |
| `REVEAL_CLAIM` | `claim_id` | `PresentationState != HIDDEN` |
| `CREATE_COMMITMENT` | `claim_id` | `CommitmentState == COMMITTED` |
| `PRESERVE_EVIDENCE` | `evidence_id` | `integrity.state == INTACT` |
| `REACH_COMPOSURE_RANGE` | `min`, `max` | 평정심이 구간 내 |
| `KEEP_COERCION_BELOW` | `max` | 강압 수사가 한계 미만 |
| `SURVIVE_UNTIL_TURN` | `turn` | 지정 턴까지 실패하지 않음 |
| `FINISH_BEFORE_TURN` | `turn` | 지정 턴 이전 종료 |
| `PROTECT_ENTITY` | `entity_id` | 해당 인물이 피해를 입지 않음 |
| `OPTIONAL_DISCOVERY` | `claim_id` | 선택 목표. 보상만 영향 |

### 17.3 Objective 정의

```json
{
  "objectives": {
    "required": [
      { "objective_id": "obj_01", "type": "REFUTE_CLAIM", "claim_id": "claim_location_01" },
      { "objective_id": "obj_02", "type": "REVEAL_CLAIM", "claim_id": "claim_orderer_01" }
    ],
    "optional": [
      { "objective_id": "obj_opt_01", "type": "OPTIONAL_DISCOVERY", "claim_id": "claim_motive_01" }
    ],
    "state_conditions": {
      "composure_min": 1,
      "composure_max": 30,
      "coercion_max": 60
    }
  }
}
```

### 17.4 OutcomeRule

```json
{
  "outcomes": [
    {
      "outcome_id": "best",
      "grade": "BEST_RESOLUTION",
      "priority": 10,
      "conditions": [
        { "type": "ALL_REQUIRED_OBJECTIVES" },
        { "type": "RESOURCE_RANGE", "resource": "composure", "min": 1, "max": 30 },
        { "type": "RESOURCE_RANGE", "resource": "coercion", "min": 0, "max": 60 },
        { "type": "EVIDENCE_INTACT", "evidence_id": "evidence_key_01" }
      ],
      "rewards": {
        "decisive_evidence": ["relic_case_ledger"],
        "dp": 300,
        "performance": 100,
        "unlock_causal": "causal_ep002"
      }
    },
    {
      "outcome_id": "partial",
      "grade": "PARTIAL_RESOLUTION",
      "priority": 20,
      "conditions": [{ "type": "SOME_REQUIRED_OBJECTIVES", "min_ratio": 0.5 }]
    },
    {
      "outcome_id": "coerced",
      "grade": "COERCED_CONFESSION",
      "priority": 5,
      "conditions": [{ "type": "RESOURCE_RANGE", "resource": "composure", "min": 0, "max": 0 }]
    },
    {
      "outcome_id": "failed",
      "grade": "FAILED",
      "priority": 1,
      "conditions": [
        { "type": "ANY_OF", "conditions": [
          { "type": "RESOURCE_RANGE", "resource": "stress", "min": 0, "max": 0 },
          { "type": "RESOURCE_EXCEEDS", "resource": "coercion", "value": 100 },
          { "type": "TURN_EXCEEDED" },
          { "type": "NO_SOLVABLE_PATH" }
        ]}
      ]
    }
  ]
}
```

### 17.5 결과 등급

| Grade | 조건 요약 | v1.0 대응 |
|---|---|---|
| `BEST_RESOLUTION` | 필수 목표 전부 + 적정 평정심 + 강압 한도 이하 + 핵심 증거 보존 | 완전 진술 확보 |
| `PARTIAL_RESOLUTION` | 일부 목표 미해결, 사건은 진행 가능 | 부분 실패 |
| `COERCED_CONFESSION` | 평정심 0, 진술 신뢰도 낮음 | 강제·허위 자백 |
| `FAILED` | 스트레스 0 / 강압 초과 / 시간 초과 / 해결 경로 전무 | 전투 실패 |

### 17.6 평가 순서 [확정]

```text
1. FAILED 조건 검사 (priority 1)  — 즉시 종료
2. COERCED_CONFESSION 검사 (priority 5) — 즉시 종료
3. 매 턴 종료 시 BEST 조건 검사 → 충족 시 [진술 확보] 버튼 활성화
4. 플레이어가 [진술 확보]를 선택하면 BEST_RESOLUTION 확정
5. 제한 턴 도달 시 PARTIAL_RESOLUTION 판정
```

3~4단계의 분리가 중요하다. 조건이 충족되어도 **플레이어의 명시적 선택**으로만 최적 결과가 확정된다. 이는 v1.0의 스위트 스팟 설계를 그대로 유지한 것이며, 플레이어가 추가 정보를 더 캘지 여기서 끝낼지 판단하는 순간을 만든다.

### 17.7 NO_SOLVABLE_PATH 조건

```text
function hasSolvablePath(state) -> bool:
    for objective in required_objectives:
        if objective.isComplete(state): continue
        rules = proofGraph.getRules(objective.claim_id, objective.direction)
        reachable = false
        for rule in rules:
            for set in rule.guaranteed_evidence_sets:
                if all(e in state.availableOrObtainable and intact(e) for e in set):
                    reachable = true
        if not reachable: return false
    return true
```

이 검사가 있어야 “핵심 증거가 모두 소각되어 더 이상 필수 주장을 증명할 수 없는” 상태를 즉시 실패로 처리할 수 있다. 플레이어를 해결 불가능한 상태에서 계속 플레이하게 두는 것은 불공정하다.

---

## 18. AI 표현 계층 계약

### 18.1 AI 입력 스키마

AI에는 실제 사건 전체를 주는 것이 아니라, 현재 표현해야 할 Claim만 제공한다.

```json
{
  "request_id": "req_0001",
  "prompt_version": "2.0.3",
  "speaker_profile": {
    "race": "RACE_TYPE",
    "job": "JOB_TYPE",
    "personality": ["DEFENSIVE", "FORMAL"],
    "speech_register": "POLITE_ARCHAIC",
    "forbidden_expressions": ["거짓말", "수상", "모순"]
  },
  "allowed_claims": [
    {
      "claim_id": "claim_time_01",
      "canonical_meaning": "용의자는 22시경 해당 장소에 있었다",
      "facet": "TIME"
    },
    {
      "claim_id": "claim_companion_01",
      "canonical_meaning": "용의자는 혼자 있었다",
      "facet": "COMPANION"
    }
  ],
  "presentation_groups": [
    { "group_id": "g1", "claim_ids": ["claim_companion_01", "claim_action_01"] }
  ],
  "forbidden_information": [
    "truth_value",
    "correct_evidence",
    "hidden_actor",
    "proof_rules"
  ],
  "output_format": "STATEMENT_WITH_SPANS",
  "seed": 481516
}
```

`presentation_groups`는 “이 Claim들을 한 문장 안에 붙여 말하라”는 지시다. 이것이 복합 토큰을 만든다. 복합 토큰이 필요하지 않으면 그룹을 비운다.

### 18.2 AI 출력 스키마

```json
{
  "request_id": "req_0001",
  "full_text": "형사님, 어젯밤 열 시에는 그 자리에 혼자 있었을 뿐이오.",
  "tokens": [
    {
      "token_id": "token_01",
      "claim_ids": ["claim_time_01"],
      "text": "어젯밤 열 시에는",
      "span_start": 5,
      "span_end": 14
    },
    {
      "token_id": "token_02",
      "claim_ids": ["claim_location_01", "claim_companion_01"],
      "text": "그 자리에 혼자 있었을 뿐이오",
      "span_start": 15,
      "span_end": 30
    }
  ],
  "model_id": "model_name",
  "seed": 481516
}
```

`claim_ids`가 배열이라는 점에 주의한다. 2개 이상이면 복합 토큰이며 `CLARIFY` 대상이 된다.

### 18.3 AI가 결정하면 안 되는 것 [확정]

```text
- 어떤 Claim이 거짓인가
- 어떤 질문이 해금되는가
- 어떤 증거가 충분한가
- 판정이 직접 모순인가
- 평정심이 얼마나 감소하는가
- 다음 페이즈로 넘어가는가
- 사건 결과가 무엇인가
```

### 18.4 반응 생성 방식

먼저 엔진이 결과를 결정한다.

```text
ResolutionCode = R_INDIRECT_SUSPICION
ReactionKey    = reaction_defensive_insufficient
```

그 후 AI가 해당 결과를 캐릭터 말투로 표현한다.

```text
엔진이 전달:
  "제출된 근거가 관련은 있지만 행위자를 특정하지 못했다"

AI가 표현:
  "그 카드가 내 거라는 것만으로 내가 썼다는 증거가 됩니까?"
```

**반응 요청 스키마**

```json
{
  "reaction_key": "reaction_defensive_insufficient",
  "speaker_profile": { },
  "context": {
    "target_claim_id": "claim_location_01",
    "missing_scopes": ["IDENTITY"],
    "composure_band": "HIGH"
  },
  "forbidden_information": ["truth_value", "correct_evidence"]
}
```

`composure_band`는 정확한 수치가 아니라 구간(`HIGH`/`MID`/`LOW`/`CRITICAL`)만 전달한다. 정확한 수치를 주면 AI가 “이제 거의 무너졌다” 같은 표현으로 게임 상태를 누설할 수 있다.

### 18.5 출력 검증 파이프라인 [확정]

```text
1. JSON Schema 검증
2. claim_id 매핑 검사        모든 allowed_claims가 정확히 1회 이상 매핑되었는가
3. 원자성 검사               한 토큰이 서로 다른 facet의 독립 사실을 섞지 않았는가
                            (presentation_groups로 허용된 조합은 예외)
4. span 정합성 검사          span이 full_text와 일치하는가, 겹치지 않는가
5. 허용 정보 검사            allowed_claims 외의 핵심 사실이 등장하지 않았는가
6. 금지 표현 검사            forbidden_expressions 및 정답 암시 표현 부재
7. 문체 균질성 검사          진실/거짓 Claim의 문장 길이·확신도 분포 차이 [MVP 축소: 샘플링]
8. 실패 시 재시도 1회
9. 재실패 시 검증된 폴백 사용
```

### 18.6 5단계 — 허용 정보 검사의 구현

AI가 사건에 없는 핵심 사실을 추가하는 것은 가장 흔한 실패 모드다. 완전한 자연어 사실 추출은 [후속 확장]이며, MVP에서는 다음 축소 검사를 사용한다.

```text
[MVP 축소]
- 사건에 등장하지 않는 Entity 이름이 포함되었는지 사전 대조
- 시각 표현(숫자+시)이 allowed_claims의 time_ref 범위를 벗어나는지 검사
- 토큰 밖 자연어 길이가 임계값을 넘으면 경고 (수식어 과다 = 사실 추가 위험)
```

### 18.7 폴백 계약 [확정]

```text
- 폴백 문장은 동일한 claim_id 집합을 사용한다
- 폴백 문장은 동일한 token 구조를 갖는다
- 폴백 사용 여부는 판정에 영향을 주지 않는다
- 폴백은 사전 검증된 데이터이며 런타임 검증을 다시 통과할 필요가 없다
- 모든 Claim은 최소 1개의 폴백 표현을 가져야 한다 (24.4 검증 대상)
```

```json
{
  "dialogue": {
    "fallbacks": {
      "claim_time_01": ["어젯밤 열 시쯤이었소."],
      "claim_location_01": ["그 자리에 있었소."],
      "reaction_defensive_insufficient": ["그게 무슨 증거가 됩니까?"]
    }
  }
}
```

### 18.8 재현성

모든 생성에 다음을 기록한다.

```text
run_id, encounter_id, statement_round_id, request_id,
seed, truth_graph_id, claim_ids,
prompt_version, model_id, request_time,
schema_validation, atomicity_validation, mapping_validation,
leakage_validation, fallback_used,
content_hash
```

`content_hash`가 같으면 캐시에서 재사용한다. 같은 시드·같은 Claim 집합·같은 프로필이면 같은 문장이 나와야 디버깅과 재현이 가능하다.

---

## 19. Presentation DTO 경계

### 19.1 PublicDTOMapper 규칙

엔진 상태에서 UI로 나가는 데이터는 반드시 `PublicDTOMapper`를 통과한다. 매퍼는 **화이트리스트 방식**으로 동작한다. 즉 “보내면 안 되는 것을 지우는” 것이 아니라 “보내도 되는 것만 담는다.” 블랙리스트 방식은 새 필드가 추가될 때 누설이 발생한다.

### 19.2 전달 금지 필드 [확정]

```text
truth_relation
truth_value
supporting_event_ids
proof_rules
required_scopes
guaranteed_evidence_sets
known_insufficient_sets
eliminate_hypotheses
hypothesis 제거 상세
is_required            (필수 목표 여부는 별도 UI로 표시하되 Claim 필드로는 전달 금지)
initial_visibility 원본
correct_evidence
is_decoy / is_lie
```

`is_required`에 대한 주석: 필수 목표는 UI에 “사건 목표” 패널로 표시하는 것이 정상이다. 다만 `Claim` 객체의 필드로 전달하면 아직 공개되지 않은 Claim의 중요도가 노출된다. 목표는 `Objective` DTO로만 전달한다.

### 19.3 UI DTO 스키마

```json
{
  "encounter_id": "enc_001",
  "statement_round_id": "round_01",
  "full_text": "형사님, 어젯밤 열 시에는 그 자리에 혼자 있었을 뿐이오.",
  "tokens": [
    {
      "token_id": "token_02",
      "text": "그 자리에 혼자 있었을 뿐이오",
      "span_start": 15,
      "span_end": 30,
      "facet_group": ["WHERE", "WHO"],
      "resistance": 2,
      "commitment": "ASSERTED",
      "epistemic": "UNKNOWN",
      "presentation": "COMPOUND",
      "is_compound": true
    }
  ],
  "open_slots": [
    { "slot_id": "slot_motive", "label_key": "loc.slot.motive", "facet": "MOTIVE" }
  ],
  "evidence": [
    {
      "evidence_id": "evidence_001",
      "title_key": "loc.evidence_001.title",
      "type": "TRANSACTION_RECORD",
      "observed_summary_keys": ["loc.evidence_001.obs_1"],
      "not_proven_keys": ["loc.evidence_001.np_1", "loc.evidence_001.np_2"],
      "remaining_hypothesis_count": 2,
      "integrity_state": "INTACT",
      "forensics_available": true
    }
  ],
  "resources": { "cp": 3, "composure": 100, "coercion": 0, "stress": 82 },
  "enemy_intent": { "type": "prepare_distortion", "turns_remaining": 1 },
  "objectives": [
    { "objective_id": "obj_01", "label_key": "loc.obj_01", "complete": false }
  ]
}
```

### 19.4 정답 비노출 검사 항목 [확정]

다음 항목 중 하나라도 실제 진실값에 따라 달라지면 실패다.

```text
토큰 테두리 색
방어막 흔들림 속도
카드 흡착 반응
커서 모양
효과음
피해 미리보기 수치
파트너 표정
툴팁 문구
DTO 필드 존재 여부
DTO 필드 순서
응답 지연 시간
```

마지막 두 항목은 놓치기 쉽다. 정답 Claim에만 추가 필드가 붙거나, 정답 경로에서만 서버 응답이 느려지면 그것도 누설이다. 자동 검사는 “동일 구조의 진실 Claim과 거짓 Claim의 DTO를 직렬화해 바이트 구조를 비교”하는 방식으로 구현한다.

### 19.5 공격 전 표시 가능·불가 정보

| 표시 가능 | 표시 불가 |
|---|---|
| 카드 비용 | 대상 Claim의 진실값 |
| 증거가 다루는 scope 목록 | 해당 증거가 정답 세트인지 여부 |
| 기록 오차와 식별 신뢰도 | 모순 확정 여부 사전 예측 |
| 증거가 입증하지 못하는 항목 | 필요한 나머지 증거의 정체 |
| 남은 대안 가설의 **개수** | 대안 가설의 **내용과 제거 수단** |
| 성공 시 효과 범위, 실패 시 위험 범위 | 정답일 때만 나타나는 연출 |

---

## 20. 런타임 처리 흐름

### 20.1 공통 처리 순서 [확정]

```text
1.  CaseDefinition 로드
2.  TruthGraph 생성
3.  플레이어 KnowledgeState 초기화
4.  초기 Claim 목록 선택
5.  AI 또는 폴백으로 진술문 렌더링
6.  UI용 DTO 생성
7.  플레이어가 행동 제출
8.  ArgumentResolver가 판정
9.  KnowledgeState 갱신
10. 평정심·강압·CP·저항도 갱신
11. 후속 Claim·QuestionRoute 공개
12. 엔진이 ReactionKey 선택
13. AI 또는 폴백으로 반응 표현
14. EncounterModifier 실행
15. Objective 및 Outcome 검사
16. 다음 턴·다음 페이즈·사건 종료
```

### 20.2 submitAction 의사코드

```text
function submitAction(submission):

    // ── 사전 검증
    validateAction(submission)

    // ── 판정 (부작용 없음)
    relation = evaluateEvidenceRelation(
        submission.target,
        submission.supports)

    sufficiency = evaluateProofSufficiency(
        submission.target,
        submission.supports)

    resolution = deriveResolution(
        submission.intent,
        relation,
        sufficiency)

    // ── 상태 변경
    applyResolutionEffects(resolution)
    updateKnowledgeState(resolution)
    revealFollowUpClaims(resolution)
    applyEncounterModifiers(resolution)
    evaluateObjectives()

    // ── 출력
    return createPublicResultDTO(resolution)
```

### 20.3 전투 상태 머신

```text
ENCOUNTER_INIT
→ LOAD_CASE_DEFINITION
→ VALIDATE_CASE            (개발 빌드에서만. 릴리스는 사전 검증본 사용)
→ BUILD_TRUTH_GRAPH
→ INIT_KNOWLEDGE_STATE
→ ENTER_FLOW_NODE
→ RENDER_STATEMENT
   ↳ AI_REQUEST → VALIDATE_OUTPUT → (실패) RETRY → (실패) FALLBACK
→ EMIT_PUBLIC_DTO
→ PLAYER_TURN_START
→ FREE_REVIEW              (CP 소비 없음)
→ BUILD_ARGUMENT
→ SUBMIT_ACTION
→ RESOLVE_ARGUMENT
→ APPLY_EFFECTS
→ RENDER_REACTION
→ RUN_MODIFIERS
→ CHECK_FLOW_TRANSITION
→ CHECK_OBJECTIVES
→ CHECK_OUTCOME
→ NEXT_TURN | ENTER_FLOW_NODE | ENCOUNTER_COMPLETE
```

**예외 처리**

```text
AI_TIMEOUT
→ RETRY_ONCE
→ FALLBACK_CONTENT
→ CONTINUE_WITH_SAME_CLAIM_IDS

CASE_VALIDATION_FAILED
→ 개발 빌드: 상세 리포트 후 중단
→ 릴리스 빌드: 해당 사건 스킵 + 텔레메트리 보고

NO_SOLVABLE_PATH
→ OUTCOME = FAILED (즉시)
```

### 20.4 무료 검토 단계의 보장

`FREE_REVIEW` 단계에서는 다음 행동이 CP를 소비하지 않는다.

- 사건 기록·증거·이전 진술 이력 열람
- 증거 간 비교
- 진술 토큰과 증거의 가설 연결(트레이에 올려 보기)
- 판정 미리보기 조회 (19.5의 표시 가능 정보만)

이 단계는 엔진 상태를 변경하지 않는다. 즉 `FREE_REVIEW`에서 발생하는 모든 호출은 순수 조회이며, 이 성질이 “미리보기가 정답을 누설하지 않는다”를 구조적으로 보장한다.

### 20.5 결정론 보장

같은 입력이 항상 같은 결과를 내야 한다. 이를 위해 다음을 지킨다.

| 항목 | 규칙 |
|---|---|
| 무작위 | 모든 무작위는 `run_seed`에서 파생된 결정론적 스트림 사용 |
| 순회 순서 | 딕셔너리·해시셋 순회 금지. 정렬된 리스트 또는 정의 순서 사용 |
| 부동소수점 | 신뢰도 비교는 명시적 epsilon 사용 |
| AI 출력 | 판정에 영향을 주지 않음. 실패해도 폴백으로 동일 진행 |
| 시각 | 실시간 시각을 게임 로직에 사용하지 않음 |

이 규칙이 지켜지면 판정 로그를 재생해 버그를 정확히 재현할 수 있으며, 24장의 자동 검증도 신뢰할 수 있다.
# 제3부 — 검증

> 일반화가 실제로 되었는가

---

## 21. 검증 사건 A — 알리바이·소유 구조

### 21.1 목적

v1.0의 드워프 감리사 시뮬레이션을 **엔진 코드 없이 데이터만으로** 재현한다. 이 장의 JSON에는 게임 로직이 한 줄도 없다. v1.0 6.11절의 플레이 시퀀스가 그대로 재현되면 제2부의 명세가 실제로 작동한다는 뜻이다.

이 사건이 사용하는 구조:

```text
ClaimFacet:  ACTOR, TIME, LOCATION, ACTION, COMPANION, AFTERMATH,
             OWNERSHIP, MOTIVE, SEQUENCE, ROUTE
ProofScope:  IDENTITY, TIME, LOCATION, OWNERSHIP, ROUTE
Intent:      QUERY, CLARIFY, COMMIT, CONFIRM, CONTRADICT, FORENSIC
Effect:      DISTORT_CLAIM_VIEW
Objective:   REFUTE_CLAIM, REVEAL_CLAIM, REACH_COMPOSURE_RANGE
```

### 21.2 사건 진실 (TruthGraph)

| 시각 | 실제로 일어난 일 |
|---|---|
| 21:30 | 국밥집 영업 종료 및 봉인 |
| 22:01~22:07 | 용의자가 포장마차에 있었음 |
| 22:03 | 용의자 본인이 법인카드로 포장마차에서 결제 |
| 22:20 | 용의자가 숙소로 이동 |

용의자의 주장 중 거짓은 **장소**(국밥집에 있었다)와, 2차 방어에서 등장하는 **순서**(국밥집에 먼저 갔다)다. **소유**(카드를 본인이 소지했다)는 진실이며, 이것이 플레이어에게 유리하게 작용하는 것이 이 사건의 설계 포인트다.

### 21.3 Entity

```json
{
  "entities": [
    { "entity_id": "ent_suspect_a", "type": "PERSON", "role": "SUSPECT",
      "display_name_key": "loc.caseA.suspect.name",
      "attributes": { "race": "DWARF", "job": "SITE_INSPECTOR" } },

    { "entity_id": "ent_loc_soup", "type": "LOCATION", "role": "SCENE",
      "display_name_key": "loc.caseA.loc_soup.name", "attributes": {} },

    { "entity_id": "ent_loc_stall", "type": "LOCATION", "role": "SCENE",
      "display_name_key": "loc.caseA.loc_stall.name", "attributes": {} },

    { "entity_id": "ent_loc_lodging", "type": "LOCATION", "role": "SCENE",
      "display_name_key": "loc.caseA.loc_lodging.name", "attributes": {} },

    { "entity_id": "ent_obj_card", "type": "OBJECT", "role": "INSTRUMENT",
      "display_name_key": "loc.caseA.obj_card.name", "attributes": {} },

    { "entity_id": "ent_merchant", "type": "PERSON", "role": "WITNESS",
      "display_name_key": "loc.caseA.merchant.name", "attributes": {} },

    { "entity_id": "ent_sys_payment", "type": "RECORD_SYSTEM", "role": "NONE",
      "display_name_key": "loc.caseA.sys_payment.name", "attributes": {} }
  ]
}
```

### 21.4 Event

```json
{
  "events": [
    { "event_id": "ev_a_shop_closed",
      "time": { "from": "21:30", "to": "21:30", "error_seconds": 300 },
      "location_id": "ent_loc_soup",
      "participants": [],
      "action": "FACILITY_CLOSED",
      "objects": [] },

    { "event_id": "ev_a_at_stall",
      "time": { "from": "22:01", "to": "22:07", "error_seconds": 60 },
      "location_id": "ent_loc_stall",
      "participants": ["ent_suspect_a"],
      "action": "PRESENT_AT_PLACE",
      "objects": [] },

    { "event_id": "ev_a_payment",
      "time": { "from": "22:03", "to": "22:03", "error_seconds": 30 },
      "location_id": "ent_loc_stall",
      "participants": ["ent_suspect_a", "ent_merchant"],
      "action": "PAYMENT_MADE",
      "objects": ["ent_obj_card"],
      "caused_by": "ev_a_at_stall" },

    { "event_id": "ev_a_return",
      "time": { "from": "22:20", "to": "22:40", "error_seconds": 300 },
      "location_id": "ent_loc_lodging",
      "participants": ["ent_suspect_a"],
      "action": "RETURNED_TO_LODGING",
      "objects": [] }
  ]
}
```

### 21.5 Claim

```json
{
  "claims": [
    { "claim_id": "cl_a_actor", "subject_id": "ent_suspect_a",
      "predicate": "HAS_PROPERTY", "facet": "ACTOR", "facet_group": ["WHO"],
      "canonical_meaning_key": "loc.caseA.cl_actor",
      "truth_relation": "CONSISTENT_WITH_WORLD",
      "initial_commitment": "ASSERTED", "initial_visibility": "VISIBLE",
      "resistance": 0, "is_required": false },

    { "claim_id": "cl_a_time", "subject_id": "ent_suspect_a",
      "predicate": "LOCATED_AT", "object_id": "ent_loc_soup",
      "time_ref": { "from": "22:00", "to": "22:10", "error_seconds": 600 },
      "facet": "TIME", "facet_group": ["WHEN"],
      "canonical_meaning_key": "loc.caseA.cl_time",
      "truth_relation": "CONSISTENT_WITH_WORLD",
      "initial_commitment": "ASSERTED", "initial_visibility": "VISIBLE",
      "resistance": 1, "is_required": false },

    { "claim_id": "cl_a_location", "subject_id": "ent_suspect_a",
      "predicate": "LOCATED_AT", "object_id": "ent_loc_soup",
      "time_ref": { "from": "22:00", "to": "22:10", "error_seconds": 600 },
      "facet": "LOCATION", "facet_group": ["WHERE"],
      "canonical_meaning_key": "loc.caseA.cl_location",
      "truth_relation": "CONTRADICTED_BY_WORLD",
      "initial_commitment": "ASSERTED", "initial_visibility": "VISIBLE",
      "resistance": 2, "is_required": true },

    { "claim_id": "cl_a_action", "subject_id": "ent_suspect_a",
      "predicate": "PERFORMED_ACTION", "facet": "ACTION", "facet_group": ["WHAT"],
      "canonical_meaning_key": "loc.caseA.cl_action",
      "truth_relation": "UNVERIFIABLE",
      "initial_commitment": "ASSERTED", "initial_visibility": "VISIBLE",
      "resistance": 0, "is_required": false,
      "presentation_group": "grp_a_01" },

    { "claim_id": "cl_a_companion", "subject_id": "ent_suspect_a",
      "predicate": "ACCOMPANIED_BY", "facet": "COMPANION", "facet_group": ["WHO"],
      "canonical_meaning_key": "loc.caseA.cl_companion",
      "truth_relation": "IRRELEVANT",
      "initial_commitment": "ASSERTED", "initial_visibility": "VISIBLE",
      "resistance": 2, "is_required": false,
      "presentation_group": "grp_a_01" },

    { "claim_id": "cl_a_aftermath", "subject_id": "ent_suspect_a",
      "predicate": "MOVED_FROM_TO", "object_id": "ent_loc_lodging",
      "facet": "AFTERMATH", "facet_group": ["WHEN", "HOW"],
      "canonical_meaning_key": "loc.caseA.cl_aftermath",
      "truth_relation": "PARTIALLY_TRUE",
      "initial_commitment": "ASSERTED", "initial_visibility": "VISIBLE",
      "resistance": 1, "is_required": false,
      "presentation_group": "grp_a_01" },

    { "claim_id": "cl_a_ownership", "subject_id": "ent_suspect_a",
      "predicate": "OWNED", "object_id": "ent_obj_card",
      "facet": "OWNERSHIP", "facet_group": ["WHO", "WHAT"],
      "canonical_meaning_key": "loc.caseA.cl_ownership",
      "truth_relation": "CONSISTENT_WITH_WORLD",
      "initial_commitment": "UNSTATED", "initial_visibility": "HIDDEN",
      "resistance": 1, "is_required": false },

    { "claim_id": "cl_a_motive", "subject_id": "ent_suspect_a",
      "predicate": "MOTIVATED_BY", "facet": "MOTIVE", "facet_group": ["WHY"],
      "canonical_meaning_key": "loc.caseA.cl_motive",
      "truth_relation": "UNVERIFIABLE",
      "initial_commitment": "UNSTATED", "initial_visibility": "HIDDEN",
      "resistance": 2, "is_required": false },

    { "claim_id": "cl_a_sequence", "subject_id": "ent_suspect_a",
      "predicate": "OCCURRED_BEFORE", "facet": "SEQUENCE", "facet_group": ["WHEN", "HOW"],
      "canonical_meaning_key": "loc.caseA.cl_sequence",
      "truth_relation": "CONTRADICTED_BY_WORLD",
      "initial_commitment": "UNSTATED", "initial_visibility": "HIDDEN",
      "resistance": 2, "is_required": true },

    { "claim_id": "cl_a_route", "subject_id": "ent_suspect_a",
      "predicate": "MOVED_FROM_TO", "object_id": "ent_loc_stall",
      "facet": "ROUTE", "facet_group": ["WHERE", "HOW"],
      "canonical_meaning_key": "loc.caseA.cl_route",
      "truth_relation": "PARTIALLY_TRUE",
      "initial_commitment": "UNSTATED", "initial_visibility": "HIDDEN",
      "resistance": 1, "is_required": false }
  ]
}
```

**설계 주석**

- `cl_a_companion`의 `truth_relation`은 `IRRELEVANT`이면서 저항도가 2다. “범죄와 무관한 사생활을 숨겨 방어한다”는 v1.0 원칙의 데이터 구현이다. 플레이어가 여기를 파면 시간과 강압만 소모한다.
- `cl_a_time`은 진실이며 저항도 1이다. 저항도가 진실값과 무관함을 보이는 사례다.
- `cl_a_action`·`cl_a_companion`·`cl_a_aftermath`가 같은 `presentation_group`을 갖는다. AI는 이 셋을 한 문장으로 붙여 말하고, 화면에는 복합 토큰으로 표시된다. `CLARIFY`가 여기서 필요해진다.

### 21.6 Evidence

```json
{
  "evidence": [
    {
      "evidence_id": "evd_a_payment",
      "type": "TRANSACTION_RECORD",
      "title_key": "loc.caseA.evd_payment.title",
      "source_id": "src_payment_network",
      "independence_group": "grp_payment_system",
      "derived_from": [],
      "acquisition": { "node_ids": ["node_a_docs"], "guaranteed": true },
      "integrity": { "state": "INTACT", "tamper_probability": 0.02 },
      "observations": [
        { "observation_id": "obs_a_p1",
          "subject": "ent_obj_card", "predicate": "USED_AT", "object": "ent_loc_stall",
          "time": { "from": "22:03", "to": "22:03", "error_seconds": 30 },
          "scopes": ["TIME", "LOCATION", "OWNERSHIP"], "confidence": 0.99 }
      ],
      "not_proven": ["np_actual_user", "np_travel_before", "np_travel_after"],
      "alternative_hypotheses": ["hyp_a_other_user", "hyp_a_later_travel"],
      "forensics": { "available": true, "reveals": ["integrity.tamper_probability"] }
    },
    {
      "evidence_id": "evd_a_cctv",
      "type": "SURVEILLANCE",
      "title_key": "loc.caseA.evd_cctv.title",
      "source_id": "src_orb_network",
      "independence_group": "grp_surveillance",
      "derived_from": [],
      "acquisition": { "node_ids": ["node_a_scene"], "guaranteed": false },
      "integrity": { "state": "INTACT", "tamper_probability": 0.01 },
      "observations": [
        { "observation_id": "obs_a_c1",
          "subject": "ent_suspect_a", "predicate": "IDENTIFIED_AS", "object": "ent_suspect_a",
          "time": { "from": "22:01", "to": "22:07", "error_seconds": 15 },
          "scopes": ["IDENTITY"], "confidence": 0.95 },
        { "observation_id": "obs_a_c2",
          "subject": "ent_suspect_a", "predicate": "PRESENT_AT", "object": "ent_loc_stall",
          "time": { "from": "22:01", "to": "22:07", "error_seconds": 15 },
          "scopes": ["TIME", "LOCATION"], "confidence": 0.95 }
      ],
      "not_proven": ["np_travel_after", "np_companion"],
      "alternative_hypotheses": ["hyp_a_later_travel"],
      "forensics": { "available": true, "reveals": ["observations[0].confidence"] }
    },
    {
      "evidence_id": "evd_a_closing",
      "type": "SCHEDULE_RECORD",
      "title_key": "loc.caseA.evd_closing.title",
      "source_id": "src_shop_seal",
      "independence_group": "grp_facility_record",
      "derived_from": [],
      "acquisition": { "node_ids": ["node_a_docs", "node_a_canvass"], "guaranteed": false },
      "integrity": { "state": "INTACT", "tamper_probability": 0.03 },
      "observations": [
        { "observation_id": "obs_a_s1",
          "subject": "ent_loc_soup", "predicate": "OPERATIONAL_STATE", "object": "STATE_CLOSED",
          "time": { "from": "21:30", "to": "23:59", "error_seconds": 300 },
          "scopes": ["TIME", "LOCATION"], "confidence": 0.97 }
      ],
      "not_proven": ["np_unofficial_entry"],
      "alternative_hypotheses": ["hyp_a_unofficial_entry"],
      "forensics": { "available": false, "reveals": [] }
    },
    {
      "evidence_id": "evd_a_merchant",
      "type": "TESTIMONY",
      "title_key": "loc.caseA.evd_merchant.title",
      "source_id": "src_merchant_witness",
      "independence_group": "grp_witness_stall",
      "derived_from": [],
      "acquisition": { "node_ids": ["node_a_canvass"], "guaranteed": false },
      "integrity": { "state": "INTACT", "tamper_probability": 0.10 },
      "observations": [
        { "observation_id": "obs_a_m1",
          "subject": "ent_obj_card", "predicate": "OWNED_BY", "object": "ent_suspect_a",
          "time": { "from": "22:03", "to": "22:03", "error_seconds": 120 },
          "scopes": ["IDENTITY", "OWNERSHIP"], "confidence": 0.85 }
      ],
      "not_proven": ["np_travel_after"],
      "alternative_hypotheses": [],
      "forensics": { "available": true, "reveals": ["integrity.tamper_probability"] }
    },
    {
      "evidence_id": "evd_a_carriage",
      "type": "SYSTEM_LOG",
      "title_key": "loc.caseA.evd_carriage.title",
      "source_id": "src_carriage_operator",
      "independence_group": "grp_transport",
      "derived_from": [],
      "acquisition": { "node_ids": ["node_a_docs"], "guaranteed": false },
      "integrity": { "state": "INTACT", "tamper_probability": 0.02 },
      "observations": [
        { "observation_id": "obs_a_t1",
          "subject": "ent_loc_stall", "predicate": "TRAVEL_TIME_MIN", "object": "ent_loc_soup",
          "time": { "from": "00:00", "to": "23:59", "error_seconds": 0 },
          "scopes": ["ROUTE"], "confidence": 0.9 }
      ],
      "not_proven": [],
      "alternative_hypotheses": [],
      "forensics": { "available": false, "reveals": [] }
    }
  ]
}
```

### 21.7 Hypothesis

```json
{
  "hypotheses": [
    { "hypothesis_id": "hyp_a_other_user",
      "label_key": "loc.caseA.hyp_other_user",
      "eliminated_by_scopes": ["IDENTITY"],
      "eliminated_by_evidence": ["evd_a_cctv", "evd_a_merchant"] },

    { "hypothesis_id": "hyp_a_later_travel",
      "label_key": "loc.caseA.hyp_later_travel",
      "eliminated_by_scopes": ["ROUTE"],
      "eliminated_by_evidence": ["evd_a_closing", "evd_a_carriage"] },

    { "hypothesis_id": "hyp_a_unofficial_entry",
      "label_key": "loc.caseA.hyp_unofficial_entry",
      "eliminated_by_scopes": [],
      "eliminated_by_evidence": ["evd_a_cctv"] }
  ]
}
```

### 21.8 ProofRule

```json
{
  "proof_rules": [
    {
      "rule_id": "prf_a_location_contradict",
      "target_claim_id": "cl_a_location",
      "direction": "CONTRADICT",
      "requirements": {
        "required_scopes": ["IDENTITY", "TIME", "LOCATION"],
        "minimum_confidence": 0.9,
        "minimum_independent_sources": 1,
        "require_integrity": true,
        "eliminate_hypotheses": ["hyp_a_other_user", "hyp_a_later_travel"]
      },
      "guaranteed_evidence_sets": [
        ["evd_a_cctv", "evd_a_closing"],
        ["evd_a_cctv", "evd_a_carriage"],
        ["evd_a_payment", "evd_a_merchant", "evd_a_closing"]
      ],
      "known_insufficient_sets": [
        ["evd_a_payment"],
        ["evd_a_cctv"]
      ],
      "partial_credit": { "scopes_covered_ratio": 0.5, "result": "SUSPECTED" }
    },
    {
      "rule_id": "prf_a_ownership_support",
      "target_claim_id": "cl_a_ownership",
      "direction": "SUPPORT",
      "requirements": {
        "required_scopes": ["IDENTITY", "OWNERSHIP"],
        "minimum_confidence": 0.8,
        "minimum_independent_sources": 1,
        "require_integrity": false,
        "eliminate_hypotheses": []
      },
      "guaranteed_evidence_sets": [["evd_a_merchant"]],
      "known_insufficient_sets": [],
      "partial_credit": { "scopes_covered_ratio": 0.5, "result": "PROVISIONAL" }
    },
    {
      "rule_id": "prf_a_sequence_contradict",
      "target_claim_id": "cl_a_sequence",
      "direction": "CONTRADICT",
      "requirements": {
        "required_scopes": ["TIME", "SEQUENCE"],
        "minimum_confidence": 0.9,
        "minimum_independent_sources": 2,
        "require_integrity": true,
        "eliminate_hypotheses": ["hyp_a_unofficial_entry"]
      },
      "guaranteed_evidence_sets": [
        ["evd_a_closing", "evd_a_cctv"],
        ["evd_a_closing", "evd_a_carriage"]
      ],
      "known_insufficient_sets": [["evd_a_closing"]],
      "partial_credit": { "scopes_covered_ratio": 0.5, "result": "SUSPECTED" }
    }
  ]
}
```

### 21.9 InquiryGraph

```json
{
  "inquiry_routes": [
    {
      "route_id": "rt_a_ownership",
      "target_slot": "slot_a_card_owner",
      "slot_label_key": "loc.caseA.slot_card_owner",
      "facet": "OWNERSHIP",
      "allowed_intents": ["QUERY", "COMMIT"],
      "preconditions": [
        { "type": "EVIDENCE_OWNED", "evidence_id": "evd_a_payment" }
      ],
      "reveals": ["cl_a_ownership"],
      "unlocks_routes": [],
      "creates_commitment": true,
      "commitment_level": "ASSERTED",
      "coercion_risk": 0,
      "composure_delta": 0,
      "single_use": true
    },
    {
      "route_id": "rt_a_motive",
      "target_slot": "slot_a_motive",
      "slot_label_key": "loc.caseA.slot_motive",
      "facet": "MOTIVE",
      "allowed_intents": ["QUERY"],
      "preconditions": [],
      "reveals": ["cl_a_motive"],
      "unlocks_routes": [],
      "creates_commitment": true,
      "commitment_level": "ASSERTED",
      "coercion_risk": 2,
      "composure_delta": -3,
      "single_use": true
    },
    {
      "route_id": "rt_a_route_detail",
      "target_slot": "slot_a_route",
      "slot_label_key": "loc.caseA.slot_route",
      "facet": "ROUTE",
      "allowed_intents": ["QUERY", "COMMIT"],
      "preconditions": [
        { "type": "CLAIM_VISIBLE", "claim_id": "cl_a_sequence" }
      ],
      "reveals": ["cl_a_route"],
      "unlocks_routes": [],
      "creates_commitment": true,
      "commitment_level": "ASSERTED",
      "coercion_risk": 0,
      "composure_delta": 0,
      "single_use": true
    }
  ]
}
```

### 21.10 Flow·Modifier·Objective·Outcome

```json
{
  "encounter": {
    "initial_composure": 100,
    "initial_cp": 3,
    "cp_per_turn": 3,
    "initial_phase": "ph_a_01",
    "turn_limit": null
  },
  "flow_nodes": [
    {
      "node_id": "ph_a_01",
      "enter_conditions": [],
      "reveal_claim_ids": ["cl_a_actor", "cl_a_time", "cl_a_location",
                           "cl_a_action", "cl_a_companion", "cl_a_aftermath"],
      "revise_claim_ids": [],
      "open_route_ids": ["rt_a_motive"],
      "activate_modifiers": [],
      "deactivate_modifiers": [],
      "reaction_key": "rk_a_opening",
      "resource_delta": {},
      "is_terminal": false
    },
    {
      "node_id": "ph_a_02",
      "enter_conditions": [
        { "type": "CLAIM_EPISTEMIC", "claim_id": "cl_a_location", "state": "REFUTED" }
      ],
      "reveal_claim_ids": ["cl_a_sequence"],
      "revise_claim_ids": ["cl_a_location"],
      "open_route_ids": ["rt_a_route_detail"],
      "activate_modifiers": ["mod_a_distortion"],
      "deactivate_modifiers": [],
      "reaction_key": "rk_a_defense_02",
      "resource_delta": {},
      "is_terminal": false
    },
    {
      "node_id": "ph_a_end",
      "enter_conditions": [
        { "type": "OBJECTIVE_DONE", "objective_id": "obj_a_02" }
      ],
      "reveal_claim_ids": [],
      "revise_claim_ids": [],
      "open_route_ids": [],
      "activate_modifiers": [],
      "deactivate_modifiers": ["mod_a_distortion"],
      "reaction_key": "rk_a_collapse",
      "resource_delta": {},
      "is_terminal": true
    }
  ],
  "modifiers": [
    {
      "modifier_id": "mod_a_distortion",
      "trigger": "ON_DIRECT_CONTRADICTION",
      "condition": { "type": "PHASE_IS", "node_id": "ph_a_02" },
      "effect": {
        "type": "DISTORT_CLAIM_VIEW",
        "target_selector": {
          "scope": "VISIBLE_CLAIMS",
          "exclude_states": { "epistemic": ["REFUTED", "SUPPORTED"] },
          "count": 2,
          "selection": "DETERMINISTIC_BY_INDEX"
        }
      },
      "counterplay": {
        "allowed_intents": ["CLARIFY", "FORENSIC"],
        "partner_skills": ["skill_record_cleanup"],
        "always_available": true
      },
      "activation_limit": 2,
      "priority": 100
    }
  ],
  "objectives": {
    "required": [
      { "objective_id": "obj_a_01", "type": "REFUTE_CLAIM", "claim_id": "cl_a_location" },
      { "objective_id": "obj_a_02", "type": "REFUTE_CLAIM", "claim_id": "cl_a_sequence" }
    ],
    "optional": [
      { "objective_id": "obj_a_opt", "type": "OPTIONAL_DISCOVERY", "claim_id": "cl_a_motive" }
    ],
    "state_conditions": { "composure_min": 1, "composure_max": 30, "coercion_max": 60 }
  },
  "outcomes": [
    { "outcome_id": "out_a_best", "grade": "BEST_RESOLUTION", "priority": 10,
      "conditions": [
        { "type": "ALL_REQUIRED_OBJECTIVES" },
        { "type": "RESOURCE_RANGE", "resource": "composure", "min": 1, "max": 30 },
        { "type": "RESOURCE_RANGE", "resource": "coercion", "min": 0, "max": 60 }
      ],
      "rewards": { "dp": 300, "performance": 100 } },

    { "outcome_id": "out_a_partial", "grade": "PARTIAL_RESOLUTION", "priority": 20,
      "conditions": [{ "type": "SOME_REQUIRED_OBJECTIVES", "min_ratio": 0.5 }],
      "rewards": { "dp": 120, "performance": 40 } },

    { "outcome_id": "out_a_coerced", "grade": "COERCED_CONFESSION", "priority": 5,
      "conditions": [{ "type": "RESOURCE_RANGE", "resource": "composure", "min": 0, "max": 0 }],
      "rewards": { "dp": 60, "performance": 10 } },

    { "outcome_id": "out_a_failed", "grade": "FAILED", "priority": 1,
      "conditions": [
        { "type": "ANY_OF", "conditions": [
          { "type": "RESOURCE_RANGE", "resource": "stress", "min": 0, "max": 0 },
          { "type": "RESOURCE_EXCEEDS", "resource": "coercion", "value": 100 },
          { "type": "NO_SOLVABLE_PATH" }
        ] }
      ],
      "rewards": {} }
  ]
}
```

### 21.11 플레이 시퀀스 재현

v1.0 6.11절의 시뮬레이션을 엔진 판정 축으로 추적한다. 모든 결과는 데이터에서 계산된 것이며 특별 분기가 없다.

#### 턴 1 — 결제 내역 단독 제출 (플레이어의 흔한 실수)

```text
ActionSubmission {
  intent: CONTRADICT
  target: cl_a_location
  supports: [evd_a_payment]
}
```

| 축 | 값 | 근거 |
|---|---|---|
| Validity | `VALID` | `cl_a_location`은 공개 상태, `CONTRADICT`는 근거 1개 이상 충족 |
| Relevance | `FULL` | 증거 scope `{TIME, LOCATION, OWNERSHIP}` ⊇ 필요 scope 중 `TIME, LOCATION` 포함 |
| Relation | `CONTRADICTS` | 22:03 다른 장소 사용 기록 vs 국밥집 주장 |
| Sufficiency | `PROVISIONAL` | `IDENTITY` scope 미충족 (`known_insufficient_sets`에도 등재) |
| Independence | `MET` | 1개 ≥ 1 |
| Hypothesis | `REMAINING` | `hyp_a_other_user`, `hyp_a_later_travel` 잔존 |

**판정: `R_INDIRECT_SUSPICION`** (v1.0의 “간접 의심”)

```text
평정심 -5
강압 수사 +0%
cl_a_location.epistemic: UNKNOWN → SUSPECTED
피드백: covered_scopes=[TIME, LOCATION], missing_scopes=[IDENTITY],
        remaining_hypothesis_count=2
후속: rt_a_ownership 개방 (precondition = evd_a_payment 보유 충족)
```

이 결과는 플레이어의 추리 방향을 부정하지 않으면서도, 결제 내역 하나로 거짓말을 확정하는 논리 오류를 막는다. **엔진에 “결제 내역은 약하다”는 규칙이 없다는 점이 중요하다.** 증거의 `observations`에 `IDENTITY` scope가 없다는 사실만으로 자동 도출된다.

#### 턴 2 — 소유 관계 질문

```text
ActionSubmission { intent: QUERY, target: rt_a_ownership }
```

```text
판정: R_QUERY_SUCCESS
cl_a_ownership: HIDDEN → VISIBLE, UNSTATED → ASSERTED
평정심 -0, 강압 +0
```

용의자: “당연하지! 내 법인카드를 남에게 왜 줘!”

#### 턴 3 — 소유 발언 고정

```text
ActionSubmission { intent: COMMIT, target: cl_a_ownership }
```

```text
판정: R_COMMIT_SUCCESS
cl_a_ownership.commitment: ASSERTED → COMMITTED
cl_a_ownership.epistemic:  UNKNOWN  → UNKNOWN   (변하지 않음)
```

**여기가 3축 모델의 핵심 시연 지점이다.** 발언은 고정되었지만 사실 여부는 미검증이다. 그리고 이 주장은 실제로는 **진실**이다. 플레이어는 “카드를 남이 썼을 수 있다”는 대안 가설을 용의자 본인의 입으로 봉쇄한 것이다.

#### 턴 4 — 복합 토큰 분해

```text
ActionSubmission { intent: CLARIFY, target: token_grp_a_01 }
```

```text
token_grp_a_01.claim_ids = [cl_a_action, cl_a_companion, cl_a_aftermath]  (3개)
판정: R_CLARIFY_SUCCESS
→ 3개 토큰으로 분리, PresentationState: COMPOUND → NORMAL
→ 드로우 1 (재질문 카드 Modifier)
```

#### 턴 5 — 직접 모순 제출

```text
ActionSubmission {
  intent: CONTRADICT
  target: cl_a_location
  supports: [evd_a_cctv, evd_a_closing]
}
```

| 축 | 값 | 근거 |
|---|---|---|
| Validity | `VALID` | — |
| Relevance | `FULL` | 결합 scope `{IDENTITY, TIME, LOCATION}` = 필요 scope |
| Relation | `CONTRADICTS` | 22:01~22:07 타 장소 존재 + 21:30 이후 대상 장소 폐쇄 |
| Sufficiency | `SUFFICIENT` | `guaranteed_evidence_sets`에 등재 + 의미 조건도 충족 |
| Independence | `MET` | `grp_surveillance`, `grp_facility_record` = 2개 독립 |
| Hypothesis | `CLEARED` | `hyp_a_other_user` ← CCTV(IDENTITY), `hyp_a_later_travel` ← 폐쇄 기록 |

**판정: `R_DIRECT_CONTRADICTION`**

```text
평정심 -25
강압 수사 +0%
cl_a_location.epistemic: SUSPECTED → REFUTED
저항도 파괴
obj_a_01 달성
```

**FlowNode 전이**: `ph_a_02`의 `enter_conditions` 충족 → 진입

```text
공개: cl_a_sequence  ("국밥집에 먼저 갔소")
수정: cl_a_location → REVISED
개방: rt_a_route_detail
활성: mod_a_distortion
반응: rk_a_defense_02
```

**Modifier 발동**: `ON_DIRECT_CONTRADICTION` + `PHASE_IS(ph_a_02)`

```text
DISTORT_CLAIM_VIEW ×2
대상 선택: VISIBLE_CLAIMS 중 epistemic ∈ {REFUTED, SUPPORTED} 제외
        → cl_a_location 제외 (이미 REFUTED)
        → cl_a_ownership 제외 (COMMITTED이지만 epistemic은 UNKNOWN이므로 대상)
실제 선택: cl_a_time, cl_a_ownership  (DETERMINISTIC_BY_INDEX)
```

플레이어는 `CLARIFY`로 복구하거나, 진술 이력을 열람해 원문을 확인한다. **추리는 삭제되지 않았고 비용만 증가했다.**

#### 턴 6~7 — 순서 주장 반박

```text
ActionSubmission {
  intent: CONTRADICT
  target: cl_a_sequence
  supports: [evd_a_closing, evd_a_carriage]
}
```

| 축 | 값 |
|---|---|
| Relevance | `FULL` (`{TIME, LOCATION}` ∪ `{ROUTE}` ⊇ `{TIME, SEQUENCE}` 파생 충족) |
| Relation | `CONTRADICTS` |
| Sufficiency | `SUFFICIENT` (`guaranteed_evidence_sets` 등재) |
| Independence | `MET` (`grp_facility_record`, `grp_transport` = 2 ≥ 2) |
| Hypothesis | `CLEARED` |

**판정: `R_DIRECT_CONTRADICTION`** → `obj_a_02` 달성 → `ph_a_end` 진입 → `[진술 확보]` 활성화 조건 검사

### 21.12 CCTV 단독 제출의 처리 — 콘텐츠 튜닝 사례

v1.0 부록 E-6은 “CCTV 단독 또는 허용된 복합 증거는 직접 모순이 된다”고 기술했다. v2.0의 데이터에서는 `evd_a_cctv` 단독이 `known_insufficient_sets`에 등재되어 `R_INDIRECT_SUSPICION`이 된다. 이유는 다음과 같다.

```text
주장 시간 범위: 22:00~22:10, 오차 600초  → 실질 21:50~22:20
CCTV 관찰 범위: 22:01~22:07, 오차 15초
→ 22:08~22:20 구간이 비어 있음
→ hyp_a_later_travel(결제 후 대상 장소로 이동) 미제거
```

**어느 쪽이 맞는가는 엔진이 아니라 콘텐츠가 결정한다.** CCTV 단독을 충분하게 만들고 싶으면 다음 중 하나를 데이터에서 바꾸면 된다.

| 방법 | 변경 대상 | 효과 |
|---|---|---|
| 주장 시간 오차 축소 | `cl_a_time.time_ref.error_seconds: 600 → 120` | 빈 구간이 사라져 가설 자동 제거 |
| 가설 요구 완화 | `eliminate_hypotheses`에서 `hyp_a_later_travel` 제거 | 즉시 충분 판정 |
| 보장 조합 추가 | `guaranteed_evidence_sets`에 `["evd_a_cctv"]` 추가 | 의미 조건과 무관하게 충분 |
| CCTV 관찰 확대 | `obs_a_c2.time`을 22:00~22:20으로 확장 | 물리적으로 알리바이 전체 커버 |

**엔진 코드는 어느 경우에도 변경되지 않는다.** 이것이 데이터 주도 설계의 실질적 이득이다. 이 항목은 27장의 의사결정 목록에 등록한다.

---

## 22. 검증 사건 B — 순서·지시 구조

### 22.1 설계 의도

사건 A와 **구조가 다른** 사건을 만들어 일반화를 검증한다.

```text
사건 A: 장소 알리바이 + 소유 관계
        핵심 Facet: LOCATION, TIME, COMPANION, OWNERSHIP
        핵심 Scope: IDENTITY, TIME, LOCATION, OWNERSHIP
        핵심 질문: "그때 어디 있었나"

사건 B: 행동 순서 + 지시 관계
        핵심 Facet: SEQUENCE, ORDER, SOURCE, AFTERMATH
        핵심 Scope: TIME, SEQUENCE, INSTRUCTION, INTEGRITY
        핵심 질문: "무엇을 먼저 했나, 누가 시켰나"
```

사건 B가 엔진 코드 변경 없이 돌아가면 7.2절의 완료 기준을 충족한 것이다.

### 22.2 사건 개요

지하 공단에서 야간 폐자재 반출 작업이 있었다. 규정상 **안전 점검 → 반출** 순서를 지켜야 하지만, 실제로는 **반출 → 사후 서명**이 이루어졌다. 반장은 규정을 지켰다고 주장하며, 그것이 자신의 단독 판단이었다고 말한다.

실제 진실:

| 시각 | 실제로 일어난 일 |
|---|---|
| 21:55 | 공무팀장이 반장에게 통신으로 선반출을 지시 |
| 22:10~22:35 | 크레인 운행 및 반출 실행 |
| 23:05 | 안전 점검 서명부에 사후 서명 |

거짓인 주장: **순서**(점검을 먼저 했다), **지시 관계**(아무도 시키지 않았다).

### 22.3 Claim

```json
{
  "claims": [
    { "claim_id": "cl_b_actor", "subject_id": "ent_b_suspect",
      "predicate": "HAS_PROPERTY", "facet": "ACTOR", "facet_group": ["WHO"],
      "canonical_meaning_key": "loc.caseB.cl_actor",
      "truth_relation": "CONSISTENT_WITH_WORLD",
      "initial_commitment": "ASSERTED", "initial_visibility": "VISIBLE",
      "resistance": 0, "is_required": false },

    { "claim_id": "cl_b_action", "subject_id": "ent_b_suspect",
      "predicate": "PERFORMED_ACTION", "facet": "ACTION", "facet_group": ["WHAT"],
      "canonical_meaning_key": "loc.caseB.cl_action",
      "truth_relation": "CONSISTENT_WITH_WORLD",
      "initial_commitment": "ASSERTED", "initial_visibility": "VISIBLE",
      "resistance": 0, "is_required": false },

    { "claim_id": "cl_b_sequence", "subject_id": "ent_b_suspect",
      "predicate": "OCCURRED_BEFORE", "object_id": "ent_b_haul_event",
      "facet": "SEQUENCE", "facet_group": ["WHEN", "HOW"],
      "canonical_meaning_key": "loc.caseB.cl_sequence",
      "truth_relation": "CONTRADICTED_BY_WORLD",
      "initial_commitment": "ASSERTED", "initial_visibility": "VISIBLE",
      "resistance": 2, "is_required": true },

    { "claim_id": "cl_b_order", "subject_id": "ent_b_suspect",
      "predicate": "INSTRUCTED_BY", "object_id": "ent_b_none",
      "facet": "ORDER", "facet_group": ["WHO", "WHY"],
      "canonical_meaning_key": "loc.caseB.cl_order",
      "truth_relation": "CONTRADICTED_BY_WORLD",
      "initial_commitment": "UNSTATED", "initial_visibility": "HIDDEN",
      "resistance": 3, "is_required": true },

    { "claim_id": "cl_b_source", "subject_id": "ent_b_suspect",
      "predicate": "PERFORMED_ACTION", "object_id": "ent_b_logbook",
      "facet": "SOURCE", "facet_group": ["HOW"],
      "canonical_meaning_key": "loc.caseB.cl_source",
      "truth_relation": "PARTIALLY_TRUE",
      "initial_commitment": "UNSTATED", "initial_visibility": "HIDDEN",
      "resistance": 2, "is_required": false },

    { "claim_id": "cl_b_aftermath", "subject_id": "ent_b_suspect",
      "predicate": "MOVED_FROM_TO", "facet": "AFTERMATH", "facet_group": ["WHEN", "HOW"],
      "canonical_meaning_key": "loc.caseB.cl_aftermath",
      "truth_relation": "UNVERIFIABLE",
      "initial_commitment": "ASSERTED", "initial_visibility": "VISIBLE",
      "resistance": 1, "is_required": false }
  ]
}
```

### 22.4 Evidence

```json
{
  "evidence": [
    {
      "evidence_id": "evd_b_crane_log",
      "type": "SYSTEM_LOG",
      "title_key": "loc.caseB.evd_crane.title",
      "source_id": "src_crane_controller",
      "independence_group": "grp_machine_log",
      "derived_from": [],
      "acquisition": { "node_ids": ["node_b_docs"], "guaranteed": true },
      "integrity": { "state": "INTACT", "tamper_probability": 0.01 },
      "observations": [
        { "observation_id": "obs_b_cr1",
          "subject": "ent_b_crane", "predicate": "RECORDED_AT_TIME", "object": "ACT_HAULING",
          "time": { "from": "22:10", "to": "22:35", "error_seconds": 30 },
          "scopes": ["TIME", "ACTION"], "confidence": 0.98 }
      ],
      "not_proven": ["np_operator_identity", "np_instruction"],
      "alternative_hypotheses": ["hyp_b_other_operator"],
      "forensics": { "available": true, "reveals": ["integrity.tamper_probability"] }
    },
    {
      "evidence_id": "evd_b_inspection",
      "type": "DOCUMENT",
      "title_key": "loc.caseB.evd_inspection.title",
      "source_id": "src_safety_office",
      "independence_group": "grp_paper_record",
      "derived_from": [],
      "acquisition": { "node_ids": ["node_b_docs"], "guaranteed": true },
      "integrity": { "state": "INTACT", "tamper_probability": 0.15 },
      "observations": [
        { "observation_id": "obs_b_in1",
          "subject": "ent_b_suspect", "predicate": "SIGNED_AT_TIME", "object": "ent_b_logbook",
          "time": { "from": "23:05", "to": "23:05", "error_seconds": 60 },
          "scopes": ["TIME", "IDENTITY", "SEQUENCE"], "confidence": 0.92 }
      ],
      "not_proven": ["np_actual_inspection_time"],
      "alternative_hypotheses": ["hyp_b_late_signature"],
      "forensics": { "available": true, "reveals": ["integrity.tamper_probability"] }
    },
    {
      "evidence_id": "evd_b_comm",
      "type": "COMMUNICATION",
      "title_key": "loc.caseB.evd_comm.title",
      "source_id": "src_comm_relay",
      "independence_group": "grp_comm_network",
      "derived_from": [],
      "acquisition": { "node_ids": ["node_b_warrant"], "guaranteed": false },
      "integrity": { "state": "INTACT", "tamper_probability": 0.02 },
      "observations": [
        { "observation_id": "obs_b_cm1",
          "subject": "ent_b_manager", "predicate": "COMMUNICATED_WITH", "object": "ent_b_suspect",
          "time": { "from": "21:55", "to": "21:58", "error_seconds": 15 },
          "scopes": ["TIME", "IDENTITY", "INSTRUCTION"], "confidence": 0.9 }
      ],
      "not_proven": ["np_conversation_content"],
      "alternative_hypotheses": ["hyp_b_unrelated_call"],
      "forensics": { "available": false, "reveals": [] }
    },
    {
      "evidence_id": "evd_b_guard",
      "type": "TESTIMONY",
      "title_key": "loc.caseB.evd_guard.title",
      "source_id": "src_night_guard",
      "independence_group": "grp_witness_site",
      "derived_from": [],
      "acquisition": { "node_ids": ["node_b_canvass"], "guaranteed": false },
      "integrity": { "state": "INTACT", "tamper_probability": 0.12 },
      "observations": [
        { "observation_id": "obs_b_g1",
          "subject": "ent_b_suspect", "predicate": "PERFORMED", "object": "ACT_HAULING",
          "time": { "from": "22:10", "to": "22:35", "error_seconds": 300 },
          "scopes": ["IDENTITY", "ACTION", "TIME"], "confidence": 0.85 },
        { "observation_id": "obs_b_g2",
          "subject": "ent_b_suspect", "predicate": "COMMUNICATED_WITH", "object": "ent_b_manager",
          "time": { "from": "21:55", "to": "21:58", "error_seconds": 300 },
          "scopes": ["INSTRUCTION"], "confidence": 0.7 }
      ],
      "not_proven": ["np_conversation_content"],
      "alternative_hypotheses": [],
      "forensics": { "available": false, "reveals": [] }
    },
    {
      "evidence_id": "evd_b_handwriting",
      "type": "PHYSICAL_TRACE",
      "title_key": "loc.caseB.evd_handwriting.title",
      "source_id": "src_forensic_lab",
      "independence_group": "grp_forensic",
      "derived_from": ["evd_b_inspection"],
      "acquisition": { "node_ids": ["node_b_lab"], "guaranteed": false },
      "integrity": { "state": "INTACT", "tamper_probability": 0.01 },
      "observations": [
        { "observation_id": "obs_b_hw1",
          "subject": "ent_b_logbook", "predicate": "RECORDED_AT_TIME", "object": "ACT_BACKDATED",
          "time": { "from": "23:05", "to": "23:05", "error_seconds": 60 },
          "scopes": ["INTEGRITY", "SEQUENCE"], "confidence": 0.93 }
      ],
      "not_proven": [],
      "alternative_hypotheses": [],
      "forensics": { "available": false, "reveals": [] }
    }
  ]
}
```

**주목**: `evd_b_handwriting`은 `derived_from: ["evd_b_inspection"]`이다. 이 둘을 함께 제출해도 독립 출처는 1개로 계산된다. 11.8절의 독립성 알고리즘이 사건 B에서도 그대로 작동한다.

### 22.5 ProofRule

```json
{
  "proof_rules": [
    {
      "rule_id": "prf_b_sequence_contradict",
      "target_claim_id": "cl_b_sequence",
      "direction": "CONTRADICT",
      "requirements": {
        "required_scopes": ["TIME", "SEQUENCE", "ACTION"],
        "minimum_confidence": 0.9,
        "minimum_independent_sources": 2,
        "require_integrity": true,
        "eliminate_hypotheses": ["hyp_b_late_signature"]
      },
      "guaranteed_evidence_sets": [
        ["evd_b_crane_log", "evd_b_inspection"],
        ["evd_b_crane_log", "evd_b_handwriting"]
      ],
      "known_insufficient_sets": [
        ["evd_b_inspection"],
        ["evd_b_inspection", "evd_b_handwriting"]
      ],
      "partial_credit": { "scopes_covered_ratio": 0.5, "result": "SUSPECTED" }
    },
    {
      "rule_id": "prf_b_order_contradict",
      "target_claim_id": "cl_b_order",
      "direction": "CONTRADICT",
      "requirements": {
        "required_scopes": ["INSTRUCTION", "TIME", "IDENTITY"],
        "minimum_confidence": 0.85,
        "minimum_independent_sources": 2,
        "require_integrity": false,
        "eliminate_hypotheses": ["hyp_b_unrelated_call"]
      },
      "guaranteed_evidence_sets": [
        ["evd_b_comm", "evd_b_guard"]
      ],
      "known_insufficient_sets": [
        ["evd_b_comm"]
      ],
      "partial_credit": { "scopes_covered_ratio": 0.5, "result": "SUSPECTED" }
    }
  ]
}
```

**핵심 설계**: `cl_b_order`는 `CROSS_CHECK`와 결합될 때 진가를 발휘한다. 플레이어가 먼저 `COMMIT`으로 “아무도 시키지 않았다”를 고정한 뒤 통신 기록을 제출하면, 6.6절의 조합표에서 `COMMITTED / REFUTED`가 되어 최대 배율이 적용된다.

### 22.6 사건 B의 InquiryGraph

```json
{
  "inquiry_routes": [
    {
      "route_id": "rt_b_order",
      "target_slot": "slot_b_orderer",
      "slot_label_key": "loc.caseB.slot_orderer",
      "facet": "ORDER",
      "allowed_intents": ["QUERY", "COMMIT"],
      "preconditions": [
        { "type": "CLAIM_EPISTEMIC", "claim_id": "cl_b_sequence", "state": "SUSPECTED" }
      ],
      "reveals": ["cl_b_order"],
      "unlocks_routes": ["rt_b_source"],
      "creates_commitment": true,
      "commitment_level": "ASSERTED",
      "coercion_risk": 0,
      "composure_delta": -3,
      "single_use": true
    },
    {
      "route_id": "rt_b_source",
      "target_slot": "slot_b_logbook_author",
      "slot_label_key": "loc.caseB.slot_logbook_author",
      "facet": "SOURCE",
      "allowed_intents": ["QUERY"],
      "preconditions": [
        { "type": "EVIDENCE_OWNED", "evidence_id": "evd_b_inspection" }
      ],
      "reveals": ["cl_b_source"],
      "unlocks_routes": [],
      "creates_commitment": true,
      "commitment_level": "ASSERTED",
      "coercion_risk": 0,
      "composure_delta": 0,
      "single_use": true
    }
  ]
}
```

### 22.7 사건 B의 적 기믹

```json
{
  "modifiers": [
    {
      "modifier_id": "mod_b_shredding",
      "trigger": "ON_TURN_START",
      "condition": { "type": "TURN_RANGE", "min": 4, "max": 99 },
      "effect": {
        "type": "DAMAGE_EVIDENCE",
        "target_selector": {
          "scope": "OWNED_EVIDENCE",
          "exclude_states": { "used_in_completed_objective": true },
          "count": 1,
          "selection": "HIGHEST_RESISTANCE"
        }
      },
      "counterplay": {
        "allowed_intents": ["FORENSIC"],
        "partner_skills": ["skill_evidence_recovery"],
        "always_available": true
      },
      "activation_limit": 3,
      "priority": 50
    },
    {
      "modifier_id": "mod_b_timer",
      "trigger": "ON_ENCOUNTER_START",
      "condition": null,
      "effect": { "type": "ADD_TIMER", "turns": 10 },
      "counterplay": { "allowed_intents": [], "always_available": false },
      "activation_limit": 1,
      "priority": 10
    }
  ]
}
```

사건 A는 `DISTORT_CLAIM_VIEW`를, 사건 B는 `DAMAGE_EVIDENCE` + `ADD_TIMER`를 쓴다. **두 기믹 모두 15.3절의 기존 Effect 카탈로그에 이미 존재한다.** 새 Effect 타입을 만들 필요가 없었다.

`DAMAGE_EVIDENCE`는 15.6의 해결 경로 보존 검사를 받는다. `evd_b_crane_log`가 손상되어 두 필수 목표의 모든 경로가 사라지면, 엔진이 효과를 취소한다.

### 22.8 일반화 검증 결과표

| # | 완료 조건 | 사건 B에서 필요했던 작업 | 엔진 코드 변경 | 판정 |
|---|---|---|---|---|
| 1 | 다른 용의자를 데이터만으로 추가 | `SpeakerProfile` JSON 작성 | 없음 | **O** |
| 2 | 다른 Claim 분류를 데이터만으로 추가 | `SEQUENCE`·`ORDER`·`SOURCE` Facet 사용 (5.3에 사전 정의됨) | 없음 | **O** |
| 3 | 다른 증거 조합을 데이터만으로 추가 | `INSTRUCTION`·`INTEGRITY` scope 사용 (11.6에 사전 정의됨) | 없음 | **O** |
| 4 | 다른 질문 경로를 데이터만으로 추가 | `rt_b_order` → `rt_b_source` 연쇄 정의 | 없음 | **O** |
| 5 | 다른 승리 조건을 데이터만으로 추가 | `REFUTE_CLAIM` ×2 + `FINISH_BEFORE_TURN` | 없음 | **O** |
| 6 | 다른 적 기믹을 기존 조합으로 추가 | `DAMAGE_EVIDENCE` + `ADD_TIMER` | 없음 | **O** |
| 7 | AI 없이 폴백으로 진행 가능 | `dialogue.fallbacks`에 Claim별 문장 작성 | 없음 | **O** |
| 8 | 표현이 바뀌어도 판정 동일 | 동일 시드 3회 재생성 후 판정 로그 비교 | 없음 | **O** |

**결론**: 사건 B는 다음 파일만 추가하면 플레이 가능하다.

```text
content/cases/case_b.json          사건 데이터 패키지
content/locale/ko/case_b.json      대사·라벨 현지화
content/art/case_b/*.png           초상화·배경
content/balance/case_b.json        저항도·비용 조정값
```

### 22.9 사건 A와 B의 구조 대비

| 항목 | 사건 A | 사건 B |
|---|---|---|
| 핵심 거짓 | 장소 | 순서 + 지시 |
| 핵심 Facet | `LOCATION`, `SEQUENCE` | `SEQUENCE`, `ORDER` |
| 핵심 Scope | `IDENTITY`, `LOCATION` | `SEQUENCE`, `INSTRUCTION`, `INTEGRITY` |
| 결정적 행동 | 두 증거의 시각 대조 | 두 기록의 **선후** 대조 + 통신 기록 |
| 진실인 방어 주장 | 소유 (플레이어에게 유리) | 행동 자체 (반출한 것은 사실) |
| 무관한 저항 | 동행자 (사생활) | 이후 행동 |
| 적 압박 유형 | 정보 정리 비용 | 시간 + 증거 소실 |
| 필수 독립 출처 | 1~2 | 2 |
| 필요한 파생 관계 | 없음 | `derived_from` (필적 감정 ← 서명부) |

두 사건이 이만큼 다른데도 같은 `ArgumentResolver`, 같은 `ProofEvaluator`, 같은 `ModifierSystem`을 사용한다.
## 23. 콘텐츠 제작 파이프라인

### 23.1 제작 순서 [확정]

새 사건은 반드시 다음 순서로 만든다. 순서를 바꾸면 대사가 먼저 나오고 논리가 뒤따라가는 구조가 되어, 논리적으로 풀 수 없는 사건이 만들어진다.

```text
1단계  실제 사건 작성          자연어 대사를 쓰지 않는다
2단계  원자 Claim 생성
3단계  초기 공개와 숨김 설정
4단계  증거 작성
5단계  ProofRule 작성
6단계  질문 경로 작성
7단계  심문 페이즈 작성
8단계  승패·보상 작성
9단계  AI 표현 데이터 작성      여기서 처음 대사를 쓴다
```

### 23.2 각 단계 상세

#### 1단계 — 실제 사건 작성

먼저 자연어 대사를 쓰지 않고 실제 사건을 작성한다.

```text
누가
언제
어디에서
무엇을 했는가
누구의 지시였는가
어떤 결과가 발생했는가
```

**산출물**: `entities[]`, `events[]`
**검수 기준**: 모든 Event가 시간·장소·참여자를 갖는가. 사건 전체가 모순 없이 하나의 타임라인으로 성립하는가.

#### 2단계 — 원자 Claim 생성

사건에서 검증 가능한 최소 주장들을 분리한다.

**산출물**: `claims[]` (단, `initial_*` 필드는 아직 미정)
**검수 기준**: 5.2절의 원자성 원칙 A-1~A-5 준수. 각 Claim이 정확히 하나의 Facet을 갖는가.

**흔한 실수**: 거짓 Claim만 만들고 진실 Claim을 충분히 만들지 않는 것. 진실·미검증·무관 Claim이 최소 절반 이상이어야 한다. 그렇지 않으면 “아무거나 공격하면 맞는” 게임이 된다.

#### 3단계 — 초기 공개와 숨김 설정

- 처음부터 말하는 Claim (`VISIBLE` + `ASSERTED`)
- 질문하면 나오는 Claim (`HIDDEN` + `UNSTATED`)
- 첫 모순 이후 나오는 Claim (`HIDDEN`, FlowNode에서 공개)
- 다른 용의자와 대조해야 나오는 Claim (`HIDDEN`, 교차 검증 조건)

**검수 기준**: 초기 공개 Claim이 4~8개인가. 각 진술 라운드가 이 범위를 유지하는가.

#### 4단계 — 증거 작성

각 증거에 다음을 기록한다.

- 직접 관찰한 사실 (`observations`)
- 관찰하지 못한 사실 (`not_proven`)
- 신뢰도 (`confidence`)
- 오차 (`error_seconds`)
- 출처 (`source_id`)
- 독립성 (`independence_group`, `derived_from`)
- 대안 가설 (`alternative_hypotheses`)

**검수 기준**: 각 증거가 “무엇을 입증하지 못하는가”를 명시했는가. 이 항목이 비어 있는 증거는 대부분 과대평가된 증거다.

#### 5단계 — ProofRule 작성

필수 Claim마다 **최소 한 개 이상의 해결 경로**를 만든다.

**검수 기준**:
- 모든 `is_required: true` Claim에 ProofRule이 존재하는가
- `guaranteed_evidence_sets`의 모든 증거가 실제로 획득 가능한가
- 두 개 이상의 서로 다른 해결 경로가 있는가 (증거 소실 대비)

#### 6단계 — 질문 경로 작성

어떤 질문이 어떤 Claim을 공개하는지 연결한다.

**검수 기준**: 10.5절의 Q-1~Q-5 준수. 아무 변화도 없는 경로가 없는가.

#### 7단계 — 심문 페이즈 작성

- 첫 모순 후 새 진술
- 말 바꾸기
- 지시자 공개
- 적 기믹 발동
- 최종 주장 공개

**검수 기준**: 모든 FlowNode가 종료 노드에 도달 가능한가. 진입 조건이 서로 배타적인가.

#### 8단계 — 승패·보상 작성

- 완전 진술
- 부분 해결
- 강제 자백
- 실패
- 다음 사건 연결

#### 9단계 — AI 표현 데이터 작성

- 종족·직업·성격
- 말투
- 금지 표현
- 폴백 문장

**검수 기준**: 모든 Claim과 모든 ReactionKey에 폴백이 존재하는가.

### 23.3 기획자용 체크리스트

사건 제출 전 다음을 스스로 확인한다.

```text
□ 이 사건의 거짓 주장을 증명하는 방법을 두 가지 이상 적을 수 있다
□ 각 증거가 "무엇을 입증하지 못하는지" 한 문장으로 말할 수 있다
□ 저항도가 높은 진실 주장이 최소 1개 있다
□ 사건과 무관한데 용의자가 숨기려는 주장이 최소 1개 있다
□ 플레이어가 처음 시도할 법한 "그럴듯하지만 불충분한" 조합을 등록했다
□ 필수 목표 Claim이 2~3개다 (1개면 단조롭고 4개 이상이면 길다)
□ 증거가 하나 소실돼도 해결 경로가 남는다
□ 적 기믹이 어떤 추리도 삭제하지 않는다
□ 대사를 쓰기 전에 위 항목을 전부 마쳤다
```

### 23.4 작성 금지 사항

| 금지 | 이유 |
|---|---|
| 증거의 `strength`를 직접 지정 | 11.7절. 강도는 계산 결과다 |
| 진실 Claim에만 높은 저항도 배정 | 9.6.2절. 정답 누설 |
| `guaranteed_evidence_sets`만 쓰고 `requirements` 생략 | 12.2절. 플레이어의 타당한 조합을 거부하게 됨 |
| 필수 Claim을 `HIDDEN` + 경로 없음으로 설정 | 도달 불가 |
| 대사를 먼저 쓰고 Claim을 역산 | 원자성 붕괴 |
| 적 기믹으로 필수 증거 전부 파괴 | 15.6절 위반 |
| 특정 카드가 있어야만 풀리는 구간 설계 | 13.9절 위반 |

---

## 24. 자동 검증 규칙

일반화된 시스템에는 콘텐츠 검증기가 반드시 필요하다. 사람이 JSON 수백 줄의 논리 정합성을 눈으로 확인할 수는 없다.

검증은 두 시점에 실행한다.

```text
빌드 시   전체 사건 데이터에 대해 실행. ERROR가 하나라도 있으면 빌드 실패
런타임    개발 빌드에서만 사건 로드 시 실행. 릴리스는 사전 검증본 신뢰
```

### 24.1 논리 검증

| ID | 검사 | 등급 |
|---|---|---|
| L-1 | 모든 필수 Claim에 최소 하나의 ProofRule이 존재하는가 | ERROR |
| L-2 | ProofRule이 참조하는 증거가 실제 조사 경로에서 획득 가능한가 | ERROR |
| L-3 | 필수 질문 경로를 실제로 사용할 수 있는가 (선행 조건 도달 가능) | ERROR |
| L-4 | 증거가 입증 범위를 넘어 결론을 내리지 않는가 (`observations`의 scope ⊇ ProofRule 요구를 실제로 만족) | ERROR |
| L-5 | 대안 가설이 남아 있는데 `guaranteed_evidence_sets`에 등재되었는가 | WARNING |
| L-6 | 복합 증거의 출처가 실제로 독립적인가 (`minimum_independent_sources`를 만족하는 조합이 존재하는가) | ERROR |
| L-7 | 모든 Claim ID·Evidence ID·Route ID 참조가 유효한가 | ERROR |
| L-8 | Event 타임라인에 시간 모순이 없는가 | WARNING |

**L-4의 구현**

```text
for rule in proof_rules:
    for set in rule.guaranteed_evidence_sets:
        covered = union(obs.scopes for e in set for obs in e.observations)
        if not covered ⊇ rule.requirements.required_scopes:
            ERROR("보장 조합이 의미 조건을 만족하지 않음: " + rule.rule_id)
```

이 검사가 있으면 기획자가 “이 두 장이면 될 것 같다”고 감으로 등록한 조합을 자동으로 걸러낸다.

### 24.2 진행 검증

| ID | 검사 | 등급 |
|---|---|---|
| P-1 | 모든 FlowNode가 종료 상태에 도달 가능한가 | ERROR |
| P-2 | 특정 카드가 없으면 진행 불가한 구간이 있는가 | ERROR |
| P-3 | 증거 소각 후에도 최소 한 개의 해결 경로가 남는가 | ERROR |
| P-4 | 필수 Intent가 덱 RNG 때문에 영구적으로 사라질 수 있는가 | ERROR |
| P-5 | 제한 턴 안에 이론상 해결 가능한가 | WARNING |
| P-6 | `unlocks_routes` 그래프에 순환이 없는가 | ERROR |

**P-3의 구현**

```text
for modifier in modifiers where effect.type in [REMOVE_EVIDENCE, DAMAGE_EVIDENCE]:
    worst_case = simulateMaxDamage(modifier, activation_limit)
    for objective in required_objectives:
        if not hasSolvablePath(objective, state_after(worst_case)):
            ERROR("최악의 경우 해결 불가: " + objective.objective_id)
```

**P-5의 구현**

```text
minimum_actions =
      필수 Claim 공개에 필요한 QUERY 수
    + 복합 토큰 분해에 필요한 CLARIFY 수
    + 필수 Claim 반박에 필요한 CONTRADICT 수
    + 평정심 조절에 필요한 최소 행동 수

if minimum_actions > turn_limit * cp_per_turn / average_cost:
    WARNING("제한 턴 내 해결이 이론상 불가능할 수 있음")
```

### 24.3 공정성 검증

| ID | 검사 | 등급 |
|---|---|---|
| F-1 | 저항도와 진실값이 상관관계를 갖지 않는가 | ERROR |
| F-2 | UI DTO가 정답 Claim을 암시하지 않는가 (필드 구조 동일성) | ERROR |
| F-3 | 파트너 스킬이 진실값을 직접 알려주지 않는가 | ERROR |
| F-4 | 진실과 거짓 Claim의 문체가 통계적으로 구분되지 않는가 | WARNING |
| F-5 | 합리적 불충분과 무관한 제출이 다르게 처리되는가 | ERROR |
| F-6 | 진실·미검증·무관 Claim의 비율이 전체의 40% 이상인가 | WARNING |

**F-1의 구현**

```text
truth_score(claim) = (claim.truth_relation == CONTRADICTED_BY_WORLD) ? 1 : 0
r = pearson([c.resistance for c in claims], [truth_score(c) for c in claims])

if |r| > 0.5:
    ERROR("저항도가 진실값을 누설함. r = " + r)
```

**F-2의 구현**

```text
for pair in samePredicatePairs(claims):    // 진실 Claim과 거짓 Claim의 쌍
    dtoA = PublicDTOMapper.map(pair.truthful)
    dtoB = PublicDTOMapper.map(pair.false)
    if fieldSet(dtoA) != fieldSet(dtoB):
        ERROR("DTO 필드 구조가 진실값에 따라 다름")
```

### 24.4 AI 검증

| ID | 검사 | 등급 |
|---|---|---|
| A-1 | 모든 Claim ID가 출력 문장에 정확히 매핑되는가 | ERROR |
| A-2 | 하나의 토큰에 여러 독립 주장이 섞이지 않았는가 (`presentation_groups` 허용분 제외) | ERROR |
| A-3 | AI가 허용되지 않은 핵심 사실을 추가하지 않았는가 | ERROR |
| A-4 | AI가 진실값을 암시하지 않는가 (금지 표현 목록) | ERROR |
| A-5 | 실패 시 동일 Claim ID의 폴백으로 교체되는가 | ERROR |
| A-6 | 모든 Claim과 ReactionKey에 폴백이 존재하는가 | ERROR |
| A-7 | span이 `full_text`와 정합하며 겹치지 않는가 | ERROR |

### 24.5 검증 실패 처리

| 등급 | 빌드 | 개발 런타임 | 릴리스 런타임 |
|---|---|---|---|
| `ERROR` | 빌드 실패 | 사건 로드 거부 + 상세 리포트 | 사건 스킵 + 텔레메트리 |
| `WARNING` | 빌드 통과 + 리포트 | 콘솔 경고 | 무시 |

### 24.6 CI 통합

```text
on: pull_request (content/** 변경 시)

steps:
  1. JSON Schema 검증           (전체 사건)
  2. 참조 무결성 검사            L-7
  3. 논리 검증                  L-1 ~ L-8
  4. 진행 검증                  P-1 ~ P-6
  5. 공정성 검증                F-1 ~ F-6
  6. 자동 플레이 시뮬레이션       25.3의 회귀 테스트
  7. 리포트 코멘트 게시
```

6단계의 자동 플레이는 “가능한 모든 최단 해결 경로를 실제로 실행해 `BEST_RESOLUTION`에 도달하는지” 확인한다. 이것이 가장 강력한 검증이며, 사람이 놓치는 대부분의 문제를 잡는다.

---

## 25. 구현 순서와 수용 테스트

### 25.1 P0 구현 순서

각 단계는 앞 단계가 완료되어야 시작할 수 있다. 각 단계에 완료 조건(DoD)을 명시한다.

#### P0-1. 데이터 기반 사건 코어

```text
CaseDefinition, Entity, Event, Claim, Evidence,
ProofRule, QuestionRoute, Objective
```

**DoD**: 사건 A JSON을 로드해 메모리 객체 그래프를 구성하고, 참조 무결성 검사(L-7)를 통과한다. 아직 플레이는 불가능하다.

#### P0-2. 지식 상태

```text
Claim 공개 상태, 발언 고정 상태, 사실 검증 상태,
진술 이력, 미진술 슬롯
```

**DoD**: 3축 상태가 독립적으로 변경되고, 6.7절의 불변식 I-1~I-5가 assert로 강제된다.

#### P0-3. 공통 행동

```text
QUERY, CLARIFY, COMMIT, CONFIRM, CONTRADICT, PRESSURE, RECOVER
```

**DoD**: 각 Intent가 `ActionValidator`를 통과하고, 카드 없이 기본 행동으로 실행 가능하다.

#### P0-4. 공통 Resolver

```text
관련성, 지지·충돌 관계, 충분성, 독립성,
대안 가설, 판정 결과
```

**DoD**: 14.4절의 조합표 전 행에 대한 단위 테스트가 통과한다. 사건 데이터 없이 합성 입력으로 테스트 가능해야 한다.

#### P0-5. 전투 상태

```text
CP, 평정심, 강압 수사, 스트레스, 저항도, 필수 목표
```

**DoD**: 사건 A를 처음부터 끝까지 플레이해 `BEST_RESOLUTION`에 도달한다. AI는 아직 폴백 문장만 사용한다.

#### P0-6. AI 표현 경계

```text
Claim ID 입력, 진술문·span 출력, 검증, 캐시, 폴백, UI DTO 제거
```

**DoD**: AI를 켜고 끈 두 플레이의 판정 로그가 완전히 동일하다.

#### P0-7. 첫 번째 사건

사건 A를 공통 엔진 위에 데이터로 작성한다.

**DoD**: 25.2의 수용 테스트 전 항목 통과.

#### P0-8. 두 번째 검증 사건

구조가 다른 사건 B를 추가한다.

**DoD**: **엔진 코드 커밋 0건으로** 사건 B가 플레이 가능하다. 이 단계가 v2.0의 실질적 완료 지점이다.

### 25.2 P0 수용 테스트

v1.0 부록 E-6을 계승하고 일반화 항목을 추가한다.

#### 판정 정확성

```text
□ 보강 단서 단독 제출은 직접 모순이 되지 않는다
□ 허용된 복합 증거 조합은 직접 모순이 된다
□ 대상 Claim과 무관한 scope의 증거를 제출하면 무관 판정이다
□ 진실인 Claim을 공격하면 강압 수사가 오른다
□ 합리적이지만 불충분한 제출과 무관한 제출이 다르게 처리된다
□ 실패 피드백이 부족한 scope를 알려주되 정답 증거는 알려주지 않는다
```

#### 상태 모델

```text
□ COMMIT은 EpistemicState를 변경하지 않는다
□ COMMITTED 상태의 Claim이 반박되면 배율이 적용된다
□ 미진술 슬롯에 질문을 사용하면 새 Claim이 공개된다
□ 복합 토큰에 CLARIFY를 사용하면 최소 두 토큰으로 분해된다
□ COMMITTED된 Claim을 용의자가 수정하면 자체 모순이 생성된다
□ 압박은 어떤 Objective도 달성시키지 않는다
```

#### 정보 은닉

```text
□ UI DTO에 truth_relation과 proof_rules가 존재하지 않는다
□ 진실 Claim과 거짓 Claim의 DTO 필드 구조가 동일하다
□ 저항도와 진실값의 상관계수가 임계값 미만이다
□ 판정 미리보기가 결과를 사전 확정하지 않는다
```

#### AI 계약

```text
□ LLM 실패 시 같은 claim ID를 가진 폴백 문장으로 전투가 계속된다
□ AI 표현이 3회 달라져도 같은 조합이 같은 판정을 낸다
□ AI가 사건에 없는 인물·시각을 언급하면 검증기가 잡는다
```

#### 진행

```text
□ 필수 목표 해결 전에는 [진술 확보]가 활성화되지 않는다
□ 평정심 0에 도달하면 강제 자백으로 종료된다
□ 해결 경로가 모두 사라지면 즉시 실패로 처리된다
□ 적 기믹이 확정된 추리를 되돌리지 않는다
```

### 25.3 일반화 회귀 테스트

사건 B가 통과해야 하는 자동 테스트다. CI에서 매 커밋 실행한다.

```text
GeneralizationTests
├─ test_case_b_loads_without_engine_change
│     사건 B를 로드하고 22.8의 8개 조건을 자동 확인
│
├─ test_no_hardcoded_content_ids
│     엔진 소스에서 case_/claim_/evidence_/suspect_ 접두 문자열 리터럴 탐지
│     발견 시 실패
│
├─ test_all_intents_case_agnostic
│     각 Intent를 합성 CaseDefinition에 대해 실행
│     실제 콘텐츠 없이 전 판정 경로 통과
│
├─ test_resolution_table_completeness
│     14.4 조합표에 정의되지 않은 축 조합이 입력되면
│     명시적 예외를 던지는지 확인 (조용한 기본값 금지)
│
└─ test_deterministic_replay
      동일 시드·동일 입력 시퀀스로 두 번 실행해
      판정 로그가 바이트 단위로 동일한지 확인
```

`test_no_hardcoded_content_ids`가 가장 중요하다. 이 테스트 하나가 7.2절의 완료 기준을 지속적으로 강제한다.

### 25.4 테스트 스위트 구성

| 스위트 | 대상 | 실행 시점 |
|---|---|---|
| `SolvabilityTests` | 모든 사건이 이론상 해결 가능한가 | CI (콘텐츠 변경 시) |
| `ProofRuleTests` | 판정 조합표 전수 | CI (전체) |
| `LeakageTests` | DTO 누설, 저항도 상관 | CI (전체) |
| `CardReachabilityTests` | 필수 Intent 접근성 | CI (전체) |
| `AIContractTests` | 입출력 스키마·검증 파이프라인 | CI (전체) |
| `GeneralizationTests` | 일반화 회귀 | CI (전체) |

---

## 26. 위험 요소와 대응

### 26.1 일반화 과잉

**증상**: 두 번째 사건을 위해 만든 추상화가 세 번째 사건에서도 안 맞아서 다시 추상화한다. 스키마가 계속 커진다.

**원인**: 실제 사례 2개 미만에서 추상화를 결정함.

**대응**: 7.6절의 판단 축 Q1~Q3를 따른다. 사례가 1개일 때는 `SPECIAL` Intent나 사건 전용 Effect로 처리하고, 3개째가 나올 때 승격한다.

**판단 기준**: 새 개념을 만들기 전에 “이 개념 없이 데이터로 표현하면 몇 줄이 늘어나는가”를 센다. 20줄 미만이면 만들지 않는다.

### 26.2 스키마 비대화

**증상**: `CaseDefinition` 필드가 100개를 넘고, 기획자가 무엇을 채워야 할지 모른다.

**대응**:
- 필수 필드와 선택 필드를 명확히 구분한다
- 로더가 `predicate`에서 `required_scopes`를 자동 생성하듯, 파생 가능한 필드는 기본값을 자동 생성한다 (12.5절)
- 기획자용 작성 템플릿과 검증 리포트를 제공한다
- 사건 하나의 최소 JSON이 200줄을 넘으면 스키마 재검토

### 26.3 AI 계약 위반

**증상**: AI가 사건에 없는 사실을 추가하거나, 진실값을 문체로 누설한다.

**대응**: 18.5절의 7단계 검증 + 재시도 1회 + 폴백. MVP에서는 18.6의 축소 검사를 사용하되, 시연용 핵심 진술은 **사전 생성·검증본을 우선 사용**한다.

**판단 기준**: 검증 실패율이 10%를 넘으면 프롬프트 문제이지 검증기 문제가 아니다.

### 26.4 판정 축 조합 폭발

**증상**: 축이 7개이므로 이론상 조합이 수백 가지가 되어 표를 관리할 수 없다.

**대응**:
- 실제로 도달 가능한 조합만 표에 등재한다 (14.4는 30행 미만)
- 단축 평가로 대부분의 조합을 조기 종료한다
- 표에 없는 조합이 입력되면 조용한 기본값을 반환하지 말고 **명시적 예외**를 던진다 (25.3의 `test_resolution_table_completeness`)

### 26.5 콘텐츠 제작 비용 증가

**증상**: 하드코딩보다 사건 하나 만드는 데 시간이 더 걸린다.

**현실 인정**: 첫 번째 사건은 확실히 더 오래 걸린다. 손익 분기는 두 번째 또는 세 번째 사건이다.

**대응**:
- 23.1의 9단계 절차를 도구화한다 (스프레드시트 → JSON 변환기)
- 자동 검증 리포트로 수정 반복을 줄인다
- `predicate` 기반 기본값 자동 생성으로 필수 입력을 줄인다
- 사건 템플릿(알리바이형, 지시 책임형, 자금 흐름형)을 제공한다

### 26.6 간접 의심이 실패처럼 느껴짐

**증상**: 플레이어가 `R_INDIRECT_SUSPICION`을 “틀렸다”로 받아들이고 좌절한다.

**대응**: 이는 UX 문제이지 시스템 문제가 아니다.
- 연출을 실패가 아니라 **진전**으로 표현한다 (새 질문 경로가 열리는 것을 강조)
- 피드백에 `missing_scopes`를 명확히 표시한다
- 평정심 감소를 0이 아니라 소량이라도 주어 “효과가 있었다”를 전달한다
- 튜토리얼에서 의도적으로 이 경험을 먼저 겪게 한다

### 26.7 3축 상태 모델의 UI 복잡도

**증상**: 상태가 3개 축이라 화면에 표시할 정보가 많아지고 플레이어가 혼란스러워한다.

**대응**:
- 기본 화면에는 2축만 표시한다: 발언 고정 여부(자물쇠 아이콘)와 검증 상태(색 아닌 기호)
- `PresentationState`는 상태 표시가 아니라 시각 효과로만 전달한다
- 상세 3축은 툴팁과 사건 기록판에서만 노출한다

### 26.8 위험 요약표

| 위험 | 발생 시점 | 조기 신호 | 대응 우선순위 |
|---|---|---|---|
| 일반화 과잉 | 설계 초기 | 스키마 필드가 주 단위로 증가 | 높음 |
| AI 계약 위반 | 통합 시점 | 검증 실패율 10% 초과 | 높음 |
| 콘텐츠 비용 | 두 번째 사건 | 사건 하나에 3일 초과 | 중간 |
| 판정 조합 폭발 | 밸런싱 단계 | 표에 없는 조합 예외 빈발 | 중간 |
| UI 복잡도 | 플레이테스트 | 신규 플레이어가 상태를 오독 | 중간 |
| 간접 의심 좌절 | 플레이테스트 | 이탈률이 첫 실패 직후 급증 | 높음 |

---

## 27. 남은 의사결정 항목

다음 항목은 이 문서에서 결정하지 않는다. 구현 전 또는 플레이테스트 후 별도 결정이 필요하다.

| # | 항목 | 선택지 | 결정 기한 | 영향 범위 |
|---|---|---|---|---|
| 1 | 단일 직접 증거로 알리바이 반박을 허용할 것인가 (21.12) | (a) 시간 오차를 좁혀 허용 (b) 항상 2장 요구 | P0-7 착수 전 | 사건 데이터, 난이도 |
| 2 | 판정 조합표를 코드 상수로 둘 것인가 데이터로 뺄 것인가 | (a) 코드 (b) JSON | P0-4 착수 전 | 밸런싱 속도 |
| 3 | 덱 RNG 대책 최종안 (13.9) | A / B / C / A+B | P0-3 착수 전 | 카드 시스템 |
| 4 | `PARTIAL_WEIGHT` 독립성 가중치 값 (11.8) | 0.3 / 0.5 / 0.7 | 밸런싱 단계 | 판정 난이도 |
| 5 | `COMMITTED_MULTIPLIER` 값 (14.6) | 1.2 ~ 1.6 | 밸런싱 단계 | 전략 다양성 |
| 6 | 문체 균질성 검사(F-4)를 자동화할 것인가 | (a) 수동 샘플링 (b) 통계 자동화 | P1 | AI 파이프라인 |
| 7 | 전면 블라인드 셔플 채택 여부 (15.8) | 채택 / 폐기 / 엘리트 전용 | 플레이테스트 후 | 적 기믹 |
| 8 | 3축 상태의 UI 표현 방식 (26.7) | 2축 표시 / 3축 표시 / 단계별 해금 | P1 | UX |
| 9 | 사건 데이터의 저작 도구 | 직접 JSON / 스프레드시트 변환 / 전용 에디터 | P0-8 이후 | 제작 속도 |
| 10 | 교차 검증(다른 용의자 진술)의 MVP 포함 여부 | 포함 / P1로 이연 | P0-3 착수 전 | 콘텐츠 범위 |
| 11 | `UNRESOLVED` 상태의 회복 규칙 | 대체 증거로 회복 / 회복 불가 | P0-5 | 실패 조건 |
| 12 | 릴리스 빌드의 콘텐츠 검증 실행 여부 | 실행 / 사전 검증본 신뢰 | 출시 전 | 로딩 시간 |

---

# 부록

## 부록 A. v2.0 핵심 원칙 요약

```text
- 이 게임은 지식 상태 변환 게임이다. 평정심 감소는 결과이지 목적이 아니다.
- 사건은 코드가 아니라 다섯 개의 그래프로 구성된 데이터 패키지다.
- 계층은 다섯 개이며 의존성은 단방향이다.
- TruthGraph는 런타임에 변경되지 않는다.
- 진실과 플레이어의 지식은 반드시 분리한다.
- Claim은 자연어가 아니라 의미 단위이며, 게임 규칙은 claim_id를 대상으로 한다.
- Claim은 하나의 facet만 갖고 독립적으로 참·거짓 판정이 가능해야 한다.
- 복합 주장은 정의가 아니라 표시 상태다.
- Claim 상태는 발언·검증·표시의 세 축으로 분리한다.
- 발언 고정(COMMITTED)은 사실 확정(SUPPORTED)이 아니다.
- 저항도는 진실값과 상관관계를 가져서는 안 된다.
- 증거는 결론이 아니라 관찰 데이터이며, 입증하지 못하는 것을 명시한다.
- 증거 강도는 저장 값이 아니라 대상 주장에 따라 계산되는 값이다.
- 증거 두 장이 항상 복합 증거인 것은 아니다. 출처 독립성을 검사한다.
- Proof Rule은 의미 기반 조건과 보장된 조합의 혼합형이다.
- 판정은 결과명을 검사하지 않고 일곱 개의 축을 조합해 도출한다.
- 합리적 불충분과 무관한 제출은 반드시 다르게 처리한다.
- 게임 정답과 다르다는 이유만으로 강압 페널티를 주지 않는다.
- 카드는 개별 로직이 아니라 공통 Intent의 변형이다.
- 어떤 성장 요소도 Proof Rule과 Relation 판정을 우회할 수 없다.
- 필수 행동이 덱 운으로 사라지는 상황은 구조적으로 차단한다.
- 적 기믹은 트리거·조건·효과의 조합이며 전용 코드를 갖지 않는다.
- 적 기믹은 표현과 비용을 바꿀 뿐 진실과 확정된 추리를 바꾸지 않는다.
- 승리 조건은 수치가 아니라 Objective 데이터다.
- 사건 진행은 대사 순서가 아니라 상태 전이 그래프다.
- AI는 엔진이 결정한 Claim과 결과를 연기할 뿐 아무것도 결정하지 않는다.
- AI를 꺼도 폴백 데이터로 사건이 끝까지 진행되어야 한다.
- UI에는 화이트리스트 방식으로 정제된 DTO만 전달한다.
- 같은 입력은 항상 같은 결과를 낸다. 무작위 빗나감은 없다.
- 일반화의 완료 기준은 두 번째 사건을 코드 수정 없이 추가하는 것이다.
```

## 부록 B. 열거형 총람

### B-1. ClaimFacet (16)

```text
ACTOR  TIME  LOCATION  ACTION  OBJECT  COMPANION  OWNERSHIP  ROUTE
SEQUENCE  METHOD  MOTIVE  CAUSE  ORDER  WITNESS  SOURCE  AFTERMATH
```

### B-2. FacetGroup (6)

```text
WHO  WHEN  WHERE  WHAT  HOW  WHY
```

### B-3. TruthRelation (5)

```text
CONSISTENT_WITH_WORLD  CONTRADICTED_BY_WORLD  PARTIALLY_TRUE
UNVERIFIABLE  IRRELEVANT
```

### B-4. CommitmentState (6)

```text
UNSTATED  ASSERTED  COMMITTED  REVISED  RETRACTED  CONTRADICTED
```

### B-5. EpistemicState (6)

```text
UNKNOWN  SUSPECTED  PROVISIONAL  SUPPORTED  REFUTED  UNRESOLVED
```

### B-6. PresentationState (6)

```text
NORMAL  COMPOUND  DISTORTED  HIDDEN  LOCKED  DUPLICATED
```

### B-7. ActionIntent (10)

```text
QUERY  CLARIFY  COMMIT  CONFIRM  CONTRADICT
CROSS_CHECK  FORENSIC  PRESSURE  RECOVER  SPECIAL
```

### B-8. ProofScope (11)

```text
IDENTITY  TIME  LOCATION  PRESENCE  OWNERSHIP  ACTION
SEQUENCE  ROUTE  INSTRUCTION  MOTIVE  INTEGRITY
```

### B-9. 판정 축

```text
Validity        VALID  INVALID
Relevance       NONE  PARTIAL  FULL
Relation        SUPPORTS  CONTRADICTS  NEUTRAL  AMBIGUOUS
Sufficiency     INSUFFICIENT  PROVISIONAL  SUFFICIENT
Independence    MET  UNMET
Elimination     CLEARED  REMAINING
Procedure       FAIR  COERCIVE  FORBIDDEN
```

### B-10. ResolutionCode (23)

```text
R_DIRECT_CONTRADICTION    R_INDIRECT_SUSPICION     R_INSUFFICIENT_GROUNDS
R_TRUTH_ATTACKED          R_IRRELEVANT_EVIDENCE    R_CONFIRM_LOCKED
R_CONFIRM_PROVISIONAL     R_CONFIRM_CONFLICT       R_CONFIRM_INSUFFICIENT
R_QUERY_SUCCESS           R_QUERY_BLOCKED          R_CLARIFY_SUCCESS
R_CLARIFY_NOOP            R_COMMIT_SUCCESS         R_COMMIT_REFUSED
R_SELF_CONTRADICTION      R_CROSS_CONTRADICTION    R_FORENSIC_REVEALED
R_PRESSURE_APPLIED        R_PRESSURE_BACKFIRE      R_RECOVER_APPLIED
R_ACTION_INVALID          R_PROCEDURE_VIOLATION
```

### B-11. Trigger (11)

```text
ON_ENCOUNTER_START  ON_TURN_START  ON_ACTION_SELECTED  ON_ACTION_SUBMITTED
ON_RESOLUTION  ON_CLAIM_REVEALED  ON_CLAIM_CONFIRMED  ON_DIRECT_CONTRADICTION
ON_COMPOSURE_THRESHOLD  ON_EVIDENCE_USED  ON_TURN_END
```

### B-12. Effect (18)

```text
DISTORT_CLAIM_VIEW  DUPLICATE_CLAIM_VIEW  RESTORE_CLAIM_VIEW
LOCK_CLAIM  LOCK_ACTION  ALTER_ACTION_COST
DAMAGE_EVIDENCE  REMOVE_EVIDENCE  UNLOCK_EVIDENCE_SLOT
REDUCE_CP  ADD_TIMER  INCREASE_RESISTANCE
DRAW_CARD  DISCARD_CARD  APPLY_STRESS  APPLY_COERCION
CHANGE_PHASE  SPAWN_STATEMENT
```

### B-13. ObjectiveType (12)

```text
RESOLVE_CLAIM  CONFIRM_CLAIM  REFUTE_CLAIM  REVEAL_CLAIM
CREATE_COMMITMENT  PRESERVE_EVIDENCE  REACH_COMPOSURE_RANGE
KEEP_COERCION_BELOW  SURVIVE_UNTIL_TURN  FINISH_BEFORE_TURN
PROTECT_ENTITY  OPTIONAL_DISCOVERY
```

### B-14. OutcomeGrade (4)

```text
BEST_RESOLUTION  PARTIAL_RESOLUTION  COERCED_CONFESSION  FAILED
```

### B-15. Condition type (13)

```text
CLAIM_VISIBLE  CLAIM_COMMITMENT  CLAIM_EPISTEMIC
EVIDENCE_OWNED  EVIDENCE_INTACT  ROUTE_USED
RESOURCE_RANGE  RESOURCE_EXCEEDS  PHASE_IS  TURN_RANGE
OBJECTIVE_DONE  ALL_OF  ANY_OF  NOT
```

## 부록 C. ID 명명 규칙

```text
case_<theme>_<number>
entity_<type>_<name>            또는 ent_<case>_<name>
event_<case>_<index>
claim_<case>_<facet>_<index>    축약 시 cl_<case>_<name>
route_<case>_<facet>_<index>    축약 시 rt_<case>_<name>
evidence_<case>_<name>          축약 시 evd_<case>_<name>
observation_<evidence>_<index>  축약 시 obs_<evidence>_<index>
hypothesis_<case>_<name>        축약 시 hyp_<case>_<name>
proof_<claim>_<direction>       축약 시 prf_<claim>_<direction>
node_<case>_<phase>             축약 시 ph_<case>_<index>
modifier_<name>                 축약 시 mod_<case>_<name>
objective_<case>_<index>        축약 시 obj_<case>_<index>
outcome_<case>_<grade>          축약 시 out_<case>_<grade>
card_<intent>_<name>
relic_<rarity>_<name>
partner_<name>
reaction_<context>_<name>       축약 시 rk_<case>_<name>
loc.<case>.<object>.<field>     현지화 키
```

**규칙**

- 사건 데이터 내부에서는 축약형을 사용해도 되지만, 한 사건 안에서는 일관되어야 한다.
- 전역 리포지토리(카드·유물·파트너)는 축약하지 않는다.
- ID에 한글·공백·대문자를 사용하지 않는다.
- ID는 콘텐츠 버전이 바뀌어도 유지한다. 세이브 데이터가 참조한다.

## 부록 D. v1.0 → v2.0 용어 대응표

| v1.0 | v2.0 | 비고 |
|---|---|---|
| 진술 토큰 | `Claim` + `ClaimToken` | 의미 단위와 표시 단위 분리 |
| 복합 토큰 | `PresentationState.COMPOUND` | 정의가 아닌 표시 상태 |
| 미진술 슬롯 | `QuestionRoute` | 그래프로 구조화 |
| 저항도 방어막 | `Claim.resistance` | 동일 |
| 의심 마커 | `EpistemicState.SUSPECTED` | 상태로 통합 |
| 확인 잠금 | `CommitmentState.COMMITTED` / `EpistemicState.SUPPORTED` | 두 개념으로 분리 |
| 모순 확정 | `EpistemicState.REFUTED` | 상태로 통합 |
| 교란 | `PresentationState.DISTORTED` | 표시 축 |
| 약한 단서 | 계산된 `LEAD` | 저장 필드 아님 |
| 보강 단서 | 계산된 `CORROBORATING` | 저장 필드 아님 |
| 직접 증거 | 계산된 `DIRECT` | 저장 필드 아님 |
| 복합 증거 | 계산된 `COMPOSITE` | 증거 집합의 성질 |
| 유도 신문 | `ActionIntent.QUERY` | Intent |
| 재질문 | `ActionIntent.CLARIFY` | Intent |
| 확인 질문 | `ActionIntent.CONFIRM` | Intent |
| 모순 지적 | `ActionIntent.CONTRADICT` | Intent |
| 압박 | `ActionIntent.PRESSURE` | Intent |
| 정비·회복 | `ActionIntent.RECOVER` | Intent |
| 교차 검증 | `ActionIntent.CROSS_CHECK` | Intent |
| 결재 도장 | `CardModifier(layer=STAMP)` | Modifier |
| 형광 포스트잇 | `CardModifier(layer=POSTIT)` | Modifier |
| 서류 클립 병합 | `CardModifier(layer=MERGE)` | Modifier |
| 직접 모순 | `R_DIRECT_CONTRADICTION` | 축 조합의 결과 |
| 간접 의심 | `R_INDIRECT_SUSPICION` | 축 조합의 결과 |
| 합리적 증거 불충분 | `R_INSUFFICIENT_GROUNDS` | 축 조합의 결과 |
| 진실에 모순 지적 | `R_TRUTH_ATTACKED` | 축 조합의 결과 |
| 무관한 증거 제출 | `R_IRRELEVANT_EVIDENCE` | 축 조합의 결과 |
| 신뢰 상실 | `R_IRRELEVANT_EVIDENCE`의 누적 효과 | 별도 게이지 아님 |
| 필수 주장 목표 | `Objective(REFUTE_CLAIM / RESOLVE_CLAIM)` | 데이터 |
| 완전 진술 확보 | `OutcomeGrade.BEST_RESOLUTION` | 데이터 |
| 강제·허위 자백 | `OutcomeGrade.COERCED_CONFESSION` | 데이터 |
| 스위트 스팟 | `Objective.state_conditions.composure_*` | 데이터 |
| 취중 진술 교란 | `EncounterModifier` (`ON_DIRECT_CONTRADICTION` → `DISTORT_CLAIM_VIEW`) | 조합 |
| Truth Graph | `TruthGraph` | 동일 |
| Proof Rule | `ProofRule` | 혼합형으로 확장 |
| 사건 진실 보드 | `KnowledgeState`의 UI 투영 | — |

## 부록 E. CaseDefinition 전체 스키마 (요약)

```text
CaseDefinition {
  schema_version         string          필수
  case_id                string          필수
  metadata {
    title                string
    act                  int
    difficulty           int
    tags                 string[]
    estimated_turns      int
    author               string
    content_version      string
    validated_at         string
  }
  entities[] {
    entity_id            string          필수
    type                 EntityType      필수
    role                 EntityRole      필수
    display_name_key     string          필수
    attributes           object
  }
  events[] {
    event_id             string          필수
    time                 TimeRange       필수
    location_id          string
    participants         string[]        필수
    action               string          필수
    objects              string[]
    caused_by            string
    instructed_by        string
  }
  claims[] {
    claim_id             string          필수
    subject_id           string          필수
    predicate            Predicate       필수
    object_id            string
    time_ref             TimeRange
    facet                ClaimFacet      필수
    facet_group          FacetGroup[]    필수
    canonical_meaning_key string         필수
    truth_relation       TruthRelation   필수   [엔진 전용]
    supporting_event_ids string[]
    initial_commitment   CommitmentState 필수
    initial_visibility   VISIBLE|HIDDEN  필수
    resistance           int             필수
    is_required          bool            필수   [엔진 전용]
    presentation_group   string
  }
  inquiry_routes[] {
    route_id             string          필수
    target_slot          string          필수
    slot_label_key       string          필수
    facet                ClaimFacet      필수
    allowed_intents      ActionIntent[]  필수
    preconditions        Condition[]
    reveals              string[]        필수
    unlocks_routes       string[]
    creates_commitment   bool            필수
    commitment_level     CommitmentState
    coercion_risk        int             필수
    composure_delta      int             필수
    single_use           bool            필수
  }
  evidence[] {
    evidence_id          string          필수
    type                 EvidenceType    필수
    title_key            string          필수
    source_id            string          필수
    independence_group   string          필수
    derived_from         string[]
    acquisition { node_ids string[], guaranteed bool }
    integrity  { state IntegrityState, tamper_probability float }
    observations[] {
      observation_id     string          필수
      subject            string          필수
      predicate          ObsPredicate    필수
      object             string          필수
      time               TimeRange       필수
      scopes             ProofScope[]    필수
      confidence         float           필수
    }
    not_proven           string[]        필수
    alternative_hypotheses string[]      필수
    forensics { available bool, reveals string[] }
  }
  hypotheses[] {
    hypothesis_id        string          필수
    label_key            string          필수
    eliminated_by_scopes ProofScope[]
    eliminated_by_evidence string[]
  }
  proof_rules[] {
    rule_id              string          필수
    target_claim_id      string          필수
    direction            SUPPORT|CONTRADICT  필수
    requirements {
      required_scopes    ProofScope[]    필수
      minimum_confidence float           필수
      minimum_independent_sources int    필수
      require_integrity  bool
      eliminate_hypotheses string[]
    }
    guaranteed_evidence_sets  string[][]        [엔진 전용]
    known_insufficient_sets   string[][]        [엔진 전용]
    partial_credit { scopes_covered_ratio float, result EpistemicState }
  }
  encounter {
    initial_composure    int             필수
    initial_cp           int             필수
    cp_per_turn          int             필수
    initial_phase        string          필수
    turn_limit           int|null
  }
  flow_nodes[] {
    node_id              string          필수
    enter_conditions     Condition[]     필수
    reveal_claim_ids     string[]
    revise_claim_ids     string[]
    open_route_ids       string[]
    activate_modifiers   string[]
    deactivate_modifiers string[]
    reaction_key         string          필수
    resource_delta       object
    is_terminal          bool            필수
  }
  modifiers[] {
    modifier_id          string          필수
    trigger              Trigger         필수
    condition            Condition|null
    effect {
      type               EffectType      필수
      target_selector    object
      ...effect별 파라미터
    }
    counterplay { allowed_intents ActionIntent[], partner_skills string[],
                  always_available bool }
    activation_limit     int             필수
    priority             int             필수
  }
  objectives {
    required[]           Objective       필수
    optional[]           Objective
    state_conditions     object          필수
  }
  outcomes[] {
    outcome_id           string          필수
    grade                OutcomeGrade    필수
    priority             int             필수
    conditions           Condition[]     필수
    rewards              object
  }
  dialogue {
    speaker_profiles     object          필수
    fallbacks            object          필수
    forbidden_expressions string[]
  }
}
```

## 부록 F. 판정 조합 전체표 (요약 참조)

14.4절의 표를 코드에서 참조하기 위한 압축 표기다. 형식은 다음과 같다.

```text
INTENT | RELEVANCE | RELATION | SUFFICIENCY | INDEP | HYPO → CODE
```

```text
CONTRADICT | FULL    | CONTRADICTS | SUFFICIENT   | MET   | CLEARED   → R_DIRECT_CONTRADICTION
CONTRADICT | FULL    | CONTRADICTS | SUFFICIENT   | UNMET | *         → R_INDIRECT_SUSPICION
CONTRADICT | FULL    | CONTRADICTS | SUFFICIENT   | MET   | REMAINING → R_INDIRECT_SUSPICION
CONTRADICT | FULL    | CONTRADICTS | PROVISIONAL  | *     | *         → R_INDIRECT_SUSPICION
CONTRADICT | PARTIAL | CONTRADICTS | INSUFFICIENT | *     | *         → R_INSUFFICIENT_GROUNDS
CONTRADICT | PARTIAL | AMBIGUOUS   | INSUFFICIENT | *     | *         → R_INSUFFICIENT_GROUNDS
CONTRADICT | FULL    | SUPPORTS    | SUFFICIENT   | *     | *         → R_TRUTH_ATTACKED
CONTRADICT | FULL    | SUPPORTS    | INSUFFICIENT | *     | *         → R_INSUFFICIENT_GROUNDS
CONTRADICT | NONE    | *           | *            | *     | *         → R_IRRELEVANT_EVIDENCE
CONFIRM    | FULL    | SUPPORTS    | SUFFICIENT   | MET   | *         → R_CONFIRM_LOCKED
CONFIRM    | FULL    | SUPPORTS    | PROVISIONAL  | *     | *         → R_CONFIRM_PROVISIONAL
CONFIRM    | FULL    | CONTRADICTS | SUFFICIENT   | *     | *         → R_CONFIRM_CONFLICT
CONFIRM    | PARTIAL | *           | INSUFFICIENT | *     | *         → R_CONFIRM_INSUFFICIENT
CONFIRM    | NONE    | *           | *            | *     | *         → R_IRRELEVANT_EVIDENCE
QUERY      | ROUTE_OK| —           | —            | —     | —         → R_QUERY_SUCCESS
QUERY      | NO_ROUTE| —           | —            | —     | —         → R_QUERY_BLOCKED
CLARIFY    | SPLITTABLE | —        | —            | —     | —         → R_CLARIFY_SUCCESS
CLARIFY    | NONE    | —           | —            | —     | —         → R_CLARIFY_NOOP
COMMIT     | ASSERTED| —           | —            | —     | —         → R_COMMIT_SUCCESS
COMMIT     | RESIST  | —           | —            | —     | —         → R_COMMIT_REFUSED
CROSS_CHECK| SAME_SPEAKER | CONTRADICTS | SUFFICIENT | * | *          → R_SELF_CONTRADICTION
CROSS_CHECK| OTHER_SPEAKER| CONTRADICTS | SUFFICIENT | * | *          → R_CROSS_CONTRADICTION
CROSS_CHECK| *       | NEUTRAL     | *            | *     | *         → R_INSUFFICIENT_GROUNDS
FORENSIC   | TARGET_OK | —         | —            | —     | —         → R_FORENSIC_REVEALED
PRESSURE   | TARGET_OK | —         | —            | —     | —         → R_PRESSURE_APPLIED
PRESSURE   | HIGH_RESIST_TRUTH | — | —            | —     | —         → R_PRESSURE_BACKFIRE
RECOVER    | —       | —           | —            | —     | —         → R_RECOVER_APPLIED
*          | (Validity = INVALID) | | | |                            → R_ACTION_INVALID
*          | (Procedure = FORBIDDEN) | | | |                         → R_PROCEDURE_VIOLATION
```

`*`는 임의 값, `—`는 해당 없음을 뜻한다. 이 표에 없는 조합이 입력되면 조용한 기본값을 반환하지 말고 명시적 예외를 던진다.

---

## 문서 마무리

### 최종 일반화 정의

이 게임은 구현 관점에서 다음과 같이 정의된다.

> **숨겨진 사건 모델을 기반으로, 플레이어가 제한된 행동과 근거를 사용해 공개 지식 상태를 변화시키는 결정론적 심문 엔진이다.**

```text
사건 패키지
= TruthGraph + InquiryGraph + ProofGraph + EncounterFlow + OutcomeRules

플레이어 행동
= Intent + Target + Grounds + Modifiers

판정
= Validity + Relevance + Relation + Sufficiency
+ Independence + Alternative Elimination + Procedure

게임 진행
= Claim 공개 → 질문 → 발언 고정 → 근거 수집 → 지지·반박
→ 후속 Claim 공개 → 목표 해결 → 심리 상태 조절 → 사건 결과
```

### 세 가지 구현 원칙

1. **자연어가 아니라 Claim ID가 게임 규칙의 기준이다.**
2. **카드는 개별 로직이 아니라 공통 Intent의 변형이다.**
3. **새 사건은 코드가 아니라 데이터 패키지로 추가한다.**

이렇게 구성하면 현재의 알리바이 사건뿐 아니라 횡령·살인·산업재해·납치·부실 공사·내부 고발·조직 범죄 사건을 모두 동일한 엔진 위에서 구현할 수 있다.
